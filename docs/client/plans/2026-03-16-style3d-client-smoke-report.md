# 客户端自动化冒烟结果（2026-03-16）

> 文档状态：历史回归记录。
> 这是 2026-03-16 当时的自动化冒烟结果，不等于今天最新回归结论。

## 本轮执行

执行命令：

```bash
cd podi-client-web
npm run lint
npm run build
npm run test:ui
```

## 结果

### lint

- 通过

### build

- 通过

### Playwright smoke

- 通过

共 18 项：

1. 首页：通过
2. 研发设计页：通过
3. 钱包页：通过
4. 任务中心：通过
5. 素材库：通过
6. 工具箱页：通过
7. 商拍页：通过
8. 项目详情页：通过
9. 首页登录弹窗与白板入口：通过
10. 工作台页：通过
11. 顶部快捷搜索弹窗：通过
12. 工作台智能体跳转：通过
13. 首页智能体跳转：通过
14. 首页建议词跳转：通过
15. 首页最近任务跳转：通过
16. 快捷搜索键盘唤起：通过
17. 项目详情页交互跳转：通过
18. 演示态任务结果预览：通过

## 说明

这轮 smoke 的意义是：

- 先确保页面和基础结构不炸
- 先确保关键入口存在
- 先确保当前改动不会把首页/模块页/钱包页/任务中心打坏

这不等于完整业务验收，
但它已经形成了客户端当前版本的最小自动化回归手段。

## 补充

当前客户端还额外具备两条可复用自查命令：

- `npm run selfcheck:local`
  - lint + build + UI smoke
- `npm run selftest:remote`
  - 远端真实样本链路自测

最近一次本地自查结果：

- `npm run selfcheck:local`：通过
- `npm run selfcheck:full`：通过

补充说明：

- `selfcheck:full` 当前已验证：
  - 本地 lint：通过
  - 本地 build：通过
  - 本地 UI smoke：18/18 通过
  - 远端同步样本（百度 / 火山）：通过
  - 远端异步样本（KIE / ComfyUI）：通过

## 2026-03-17 增补说明

这次在自动化之外，又补做了浏览器人工真链路走查：

- `/toolbox/upscale`
  - 真实登录状态下完成 OSS 上传、同步调用、结果回填
- `/design/fusion`
  - 真实 3 图上传成功
  - 真实多图融合任务成功收口
- `/design/local-redraw`
  - 真实异步任务成功回填结果图
- `/tasks`
  - 真实任务列表加载正常

也就是说，当前客户端已经不只是“页面 smoke 通过”，而是补上了关键业务动作的浏览器端实测。

## 2026-03-17 再补一条

本轮又补了任务中心闭环验证：

- 真实任务列表加载：正常
- “查看结果” 跳回工作台：正常
- “再来一次” 带原参数回填：正常
- “按功能筛选” 真过滤：正常
- 任务结果预览弹窗：正常

这一点很重要，因为它说明客户端开始具备“任务回来继续做”的产品闭环，而不是单纯展示日志。

## 2026-03-17 再补一轮界面走查

这轮重点不是接口，而是客户端门面与承接层：

- `/home`
  - Hero 视觉升级：正常
  - 智能体工作台区增强：正常
  - 白板区阶段信息展示：正常
- `/workbench`
  - 今日主线 / 推荐路径 / 承接层展示：正常

这说明当前版本不只是“能跑”，也开始朝“可交付测试的产品界面”靠拢。

## 2026-03-17 高频入口补充

这轮又补了几个容易被用户第一眼点到的入口：

- 顶部搜索：
  - 已从空按钮变成快捷入口弹窗
- 工作台智能体卡片：
  - 已能直接跳转功能页
- 首页最近任务卡片：
  - 已能直接跳到任务中心
- 首页建议词按钮：
  - 已能跳到对应功能线
- 结果区最近结果按钮：
  - 已能跳到素材库
- 项目详情页：
  - 时间线和素材区都已具备可点击行为
- 过期登录态：
  - 已自动失效，不再持续打 401 任务请求
