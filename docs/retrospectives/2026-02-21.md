# 复盘纪要（2026-02-21）

> 目标：完成休假后首轮文档全量校正，消除过期信息对后续开发与联调的误导。

## 一、本次范围
- 时间：2026-02-21
- 范围：`docs/` 全目录 + 根目录架构文档
- 重点：接口路径、鉴权方式、历史模块标注、固定地址示例化、索引可追溯

## 二、已完成事项
1) **接口与鉴权口径统一**
- 明确当前主链路：`/api/auth/login`、`/api/abilities/*`、`/api/ability-tasks`、`/api/agent/*`。
- 将已下线接口（如 `/api/os|op/v1/*`、`/api/tasks/v1/*`）统一标注为“历史/规划”。

2) **错误/过期信息修正**
- 修复文档中的错误路径：`/api/ability_tasks/{task_id}` → `/api/ability-tasks/{task_id}`。
- 清理与修正历史文档中的时间戳与版本信息，避免“看起来像现行规范”。

3) **运维可变信息处理**
- 固定服务器地址/节点地址统一标注为“示例，以管理端配置为准”。
- 减少因主服务器更换导致的文档误导。

4) **索引与追踪完善**
- 更新 `docs/README.md`，加入本次复盘记录并刷新最后更新时间。

## 三、经验与教训
1) 文档过期的风险不低于代码缺陷：会直接影响联调效率与决策正确性。
2) “历史方案”必须显式加标签（历史/规划/未落地），不能与现行规范混排。
3) 接口示例必须优先使用可执行且现行的路径，否则会造成错误传播。

## 四、后续约束（继续执行）
1) 任何接口变更必须同日更新：模块文档 + `docs/api/INDEX.md` + 错误码总表。
2) 任何固定主机/IP 只作为示例，必须附“以管理端配置为准”。
3) 每次上线前至少执行一次文档抽检（接口路径、鉴权、错误码、端口）。

## 五、下一步
1) 持续推进 ComfyUI 管理与中台加固功能开发。
2) 新功能落地时同步补齐“请求 + 响应 + 错误”文档结构。
