# 战略待办池（唯一入口，2026Q2）

说明：

- 本文档是战略执行唯一任务池。
- 旧任务如未迁移，不进入本轮实施。

状态字段：

- `todo`：待开始
- `doing`：进行中
- `blocked`：阻塞
- `done`：已完成
- `archived`：归档

当前执行焦点（2026-06-08）：

- v0.6 已以 `516ba656` 作为 114 线上封版基线，完成 smoke、真实业务巡检、图编辑抽检、测评端 production 巡检和页面回归；v0.6/v0.6.3/v0.5 文档转为阶段记录。
- 当前正式进入 `v0.7 中台能力产品化与 Agent 编排版` 规划期，方案见 `docs/strategy/v0.7-ability-productization-agent-orchestration-plan.md`。
- 2026-06-09 会议决策包见 `docs/strategy/v0.7-kickoff-meeting-2026-06-09.md`，用于会前收敛目标选择、P0 范围、非目标、验收门槛和会后动作；最终版本内容仍需会议确认。
- 2026-06-10：Vidu 飞书资料已通过浏览器读取，方法论吸收方向为“产品展示短视频 / 一键营销成片 / 参考视频复刻 / Ad 广告模型”四类视频路径；同时把“出组图/营销套图”升级为 `product_image_set` 待办能力，不再只作为客户端后续流程里的泛称。
- 2026-06-10：产品商业化测评端暴露 P0 交互和业务口径问题，整改记录见 `docs/strategy/product-commercialization-remediation-2026-06-10.md`。截至 `564e6006`，产品文案入口已完成线上可用整改：真实接口走 `volcengine_chat`、`fallback=false`，页面能展示模型证据、文案结果、配图建议、渠道建议和结构化审核提示；视频只作为后续可联动能力，不再混入文案交付。下一步进入质量打磨：平台文案策略、VL 图片/JSON 一致性提示、真实配图质量闭环、下载包交付形态和产品视频独立验收。
- 2026-06-11：参考旧项目 `/Volumes/MAC 1/shipin/` 的 AI 摄影棚方法论，新增市场端 AI 能力规划 `docs/strategy/market-side-ai-capability-plan-2026-06-11.md`。后续产品生产后的商业化能力按“文案 / 组图 / 模特图 / 多角度 / 视频 / 营销 Agent”能力族推进，明确生产端素材能力与市场端商业化能力的边界。
- 2026-06-11：新增市场端 AI 技术方案 `docs/strategy/market-side-ai-technical-plan-2026-06-11.md`。关键修正：产品导出 JSON 改为可选说明材料，产品图/设计图是最高优先级事实源；产品视频改为视频素材包能力，脚本、分镜、首尾帧、分段视频和可选合成片都要作为可追踪资产保存，合成失败不能废掉已成功素材。
- 2026-06-11：从旧 AI 摄影棚项目补充吸收“AI 中间稿、确认门、素材包、统一轮询、单主 CTA、分段素材不作废”规则；这些规则已经写入市场端能力规划和技术方案，后续不能继续把脚本/分镜/配图建议做成只读展示后直接生成。
- 2026-06-11：新增市场端正式能力契约草案 `docs/strategy/market-side-ai-ability-contracts-2026-06-11.md`，明确 `product_image_set`、`model_scene_image`、`promo_video` 的待实现接口、输入输出、错误、成功口径和 golden cases；业务 API 文档已标注这些路径尚未开放，避免误接入。
- 2026-06-12：推进两条视频路线并拆清边界：`product_commercialization` 大模型视频支持 `productImages` 图组输入、每段参考图选择和统一 runId 查询；新增 `product_3d_render_video` 方案预览能力，记录 1660 杯子、2551 笔记本电脑背包模型资产、材质槽、UV、镜头预设和渲染 worker 待接入状态。两者不能混为一个按钮或一个接口。
- 2026-06-12：根据验收反馈纠偏 `product_3d_render_video`：测评端改为“3D 贴图渲染 · 技术预览”，主流程调整为选模型、选固定材质槽、上传贴图、选镜头/场景、检查资产准备度；删除用户侧“文字描述生成视频”口径。当前不能验收贴图重合效果或 MP4 输出，下一步必须接 Three.js 画布预览与渲染 worker。
- 当前不直接开写新功能代码；先完成 v0.7 子版本拆分、范围边界、验收标准、交互治理计划、Agent Runtime v2 schema 和首批能力产品化决策。
- 中台主语保持不变：只有能力、能力版本、路由、调用、结果、质量、成本、错误、证据和审计。项目/工作单仍属于客户端业务组装概念，不进入中台当前主线。
- 对话式改图继续暂放在图编辑分类下用于发现，但它的长期归宿是 Agent Runtime；可视化/画布式图编辑和对话式 Agent 改图必须在命名、交互、API 边界和能力定义上保持拆分。
- 客户端继续按独立团队或新 agent 并行推进；中台只输出能力边界、业务 API、调用上下文、禁止项和验收标准，不负责客户端业务流程组装。
- v0.5 方案 `docs/strategy/platform-polish-v0.5-decision-plan.md`、v0.4.1 方案 `docs/strategy/business-stability-observability-v0.4.1-plan.md`、v0.4 方案和 v0.6/v0.6.3 方案均保留为阶段记录，不再作为当前开发 TODO。
- 当前真正待办以本节下方“当前执行单（2026-06-08 v0.7 正式规划）”为准。
- 文档降噪台账见 `docs/strategy/doc-cleanup-inventory-2026-05-18.md`；过期迁移方案、旧客户端资料和阶段过程文档不再作为当前执行入口。

历史进展摘要（只用于追溯，不作为当前 TODO）：
- 2026-05-14 线上版本 `1485d93f` 已完成 114 控制面发布、smoke、ComfyUI 双节点健康复核和业务路由预览；当前不再围绕事故救火，转入版本固化、清理和下一阶段业务 API / 前端产品化。
- 当前开发节奏：先做线上早检、数据导出、项目清理审计和文档固化，再进入新一轮业务 API 与管理端/测评端体验优化；非紧急改动先本地完成一批后统一发版。
- 2026-05-15 已把三个对外业务接口的正式交付口径收敛为“一个接口一个目录、默认只给 JSON 样例、不交付 Python 脚本、两个裂变接口固定一请求一图”，并完成 114 更新到 `d6c23053`。
- 2026-05-15 已完成业务运行列表性能版本发布到 `3b941d08`：列表默认轻量摘要、详情按需加载、24 小时窗口、自动刷新开关；线上 200 条业务列表返回体从约 10MB 降到约 0.65MB。
- 2026-05-15 走查确认当前已有 API Key 调用记录，但还不是完整“接口调用中心”；后续 P0 调整为先补接口调用清单、枚举契约和对外 API 文档门禁，再继续做新业务能力。
- 2026-05-15 中台业务主线已固化为 `调用方 -> 业务入口 -> runId -> 业务版本/编排 -> 处理步骤 -> 结果/回调/计费`，详见 `docs/standards/business-mainline-contract.md`；后续页面和测试必须先按这条线审查。
- 2026-05-16 走查确认：封版判断不能再使用“原始失败数”直接阻塞，必须区分“历史失败”和“未恢复失败”。同业务同版本后续已有成功输出时，旧失败只保留追溯；只有未被复测/后续成功覆盖的问题才进入封版阻塞或需复核。
- 2026-05-16 管理端总览与业务能力页完成一轮降噪：移除大块说明式流程卡，优先展示“封版判断 / 业务入口总控 / 接口任务清单”；业务汇总接口改为轻量字段加载，避免总览扫描步骤明细导致页面刷新慢或失败。
- 2026-05-16 起，本轮版本按“目标完成 -> 本地门禁和页面走查 -> 输出更新步骤/影响范围/回滚口径 -> 再更新 114”的节奏推进；非紧急问题不再边开发边频繁更新 114。
- Coze 保留为业务接入与快速实验入口，但高并发业务调度、默认版本、灰度、统计和回滚逐步收归中台。
- 管理端第一阶段“拆文件/降包体/主路径梳理”已经完成；第二阶段重点是业务可读性、风险判断、API 对外暴露、操作闭环和视觉降噪。
- 账单/套餐/成本当前只保留内部运营骨架，真实支付、自动开票和商业套餐策略后置。

## 当前执行单（2026-06-08 v0.7 正式规划）

说明：本节是当前唯一有效 TODO。当前进入 v0.7 正式规划，不等于立即实现所有功能。第一阶段先把版本目标、子版本切片、验收标准、角色动线、Agent Runtime 边界和首批能力产品化决策写清楚；经用户确认后再逐项进入代码实现。

方案：`docs/strategy/v0.7-ability-productization-agent-orchestration-plan.md`

会议决策包：`docs/strategy/v0.7-kickoff-meeting-2026-06-09.md`

0. `doing` v0.7 入口切换与开工定义
- 目标：把当前主线从 v0.6 收口切到 v0.7 正式规划。
- 范围：总文档入口、战略索引、唯一 TODO、v0.7 方案文档、v0.6 封版基线。
- 进展（2026-06-08）：v0.6 已以 `516ba656` 完成线上封版；v0.7 方案从“后续路线储备”升级为“正式规划中”，并补入封版基线、版本基本信息和子版本建议。
- 进展（2026-06-08）：新增 2026-06-09 kickoff 会议决策包，按“事实基线、选择题、P0 候选范围、非目标、六顶思考帽、角色视角、会上必须拍板的问题、会后行动”组织，供明天确认下阶段目标。
- 验收：新 agent 或新同事只看 `docs/README.md`、`docs/strategy/README.md` 和本 TODO，就能判断当前主线已切到 v0.7 正式规划。

1. `todo` v0.7.0 开工验收标准
- 目标：按 `docs/standards/version-acceptance-template.md` 为 v0.7.0 写清开工标准，避免先开发后补验收。
- 范围：响应速度、并发、真实业务、错误路径、页面走查、Agent 回归、线上观察、封版结论模板。
- 验收：形成 v0.7.0 开工验收表；每个 P0 子任务都能映射到明确证据。

2. `todo` 全局交互治理评审
- 目标：先从业务方、运营、运维、能力工程师、接入开发者、平台负责人六类视角复核管理端和测评端，不继续局部补丁式改页面。
- 范围：能力首页、能力工作台、Agent 对话页、任务追踪、质量复盘、能力治理、执行节点/队列、接口调用排障。
- 验收：输出 v0.7 交互问题清单、第一批整改顺序、每页主路径和状态模型。

3. `todo` Agent Runtime v2 方案细化
- 目标：把对话式改图从 MVP 推进为可扩展 Agent Runtime 样板。
- 范围：会话状态、上下文压缩、当前基准图、结果入消息流、新任务隔离、工具白名单、schema 校验、置信度、成本/风险、幂等、审计。
- 验收：完成 route schema、golden cases、错误契约和后端控制边界；不允许只靠提示词决定最终工具调用。

4. `todo` 首批能力产品化决策
- 目标：把客户端未来需要的能力先按中台契约定义清楚，不把客户端业务流程硬塞进中台。
- 范围：`product_design` 推荐门槛、`product_commercialization` 产品设计后文案/配图建议/视频分镜 MVP、`product_image_set` 出组图/营销套图能力、`model_shot`、`promo_video` 的输入输出、错误、成本、样例和实现优先级。
- 进展（2026-06-10）：吸收 Vidu 资料里的电商方法论，把视频能力拆成产品展示短视频、一键营销成片、参考视频复刻和 Ad 广告模型四类路径；后续 `promo_video` 不再只按“单图生成短视频”设计。
- 进展（2026-06-10）：新增 `product_image_set` 明确待办：输入为产品设计图/商品图、可选多角度/细节图、产品导出字段、目标市场和平台场景；输出为主图、细节图、场景图、多角度图、营销图等成组资产，并保留 prompt、模型/版本、质量标签和 OSS 证据。
- 进展（2026-06-10）：产品商业化入口先进入整改而非继续扩张，P0 关闭项包括：测评端可滚动、产品导出 JSON 扩展、图片/字段一致性确认、大模型商品内容包、文案场景选择、显式生成配图、图文包下载、KIE/Vidu 供应商选择和 Vidu 待接入模型说明。
- 进展（2026-06-10）：产品文案内容包完成线上试点修复并发布到 `564e6006`。验收证据：`copyGeneration.method=volcengine_chat`、`fallback=false`、`keywordPack` 已归一化为字符串数组，页面审核区不再裸显 JSON，文案/视频边界已在页面和文档中拆清。
- 进展（2026-06-10）：纠正产品文案事实优先级：产品图是最高优先级视觉事实源，导出 JSON 是可选说明材料；后端 schema/prompt 新增 `imageFactAssessment`，测评端展示图像主判断、字段冲突、缺失字段推断和置信度。
- 进展（2026-06-10）：线上错配样例复盘后修正事实守卫：模型无依据改变商品类型仍回退，模型明确报告图片/JSON 冲突时以产品图为准放行，并输出 `PRODUCT_IMAGE_FIELD_CONFLICT` 人工复核提示；同时兼容火山模型常见的 `fieldConflicts` 字符串、`confidenceScore` 和 `[{en, zh}]` 文案数组输出。
- 进展（2026-06-11）：产品商业化配图默认路由纠正为 GPT Image 2，测评端视觉走查完成一轮降噪：移除内部版本词、接口说明默认折叠、配图区明确 `GPT Image 2 配图`、桌面/移动端无横向溢出。新增验收门禁 `docs/testing/2026-06-11-product-commercialization-acceptance-gate.md`。
- 进展（2026-06-11）：真实 Vidu 5 秒链路已生成并回填 OSS，但发现单参考图生视频比例跟随输入图/首帧，不能把 `aspectRatio` 当成 Vidu 直接执行参数。后端新增 `videoPlan.aspectPolicy`，测评端和 API 文档同步标明“Vidu 跟随首帧比例”；固定画幅进入首帧归一化/首尾帧控制待办。
- 进展（2026-06-11 追加）：市场端技术方案已固化：`productFields`/导出 JSON 不再作为核心入口，缺失或冲突都不能阻断主流程；视频能力从“最终片生成”改为“视频素材包”，默认先产出脚本、分镜、首尾帧/关键帧和分段素材，最终合成为可选动作。
- 进展（2026-06-11 追加）：补充 `backend/scripts/patrol_product_commercialization.py` 专项门禁脚本。默认只跑非成本预览校验；线上验收窗口可显式打开 `--include-live-visual/--include-live-video` 触发 GPT Image 2 配图和视频素材包真实链路。本机默认巡检因 vendor-api-ops allowlist 拒绝而失败，不能作为线上能力结论。
- 进展（2026-06-11 追加）：`product_image_set`、`model_scene_image`、`promo_video` 三个正式市场端能力契约草案已完成。注意：这是待实现契约，不代表接口已上线；短期线上仍通过 `product_commercialization` 聚合入口试验。
- 进展（2026-06-11 追加）：产品视频测评端补齐脚本/分镜确认门。AI 规划稿可编辑，编辑脚本、切换产品图或修改参数后确认状态失效；未确认不得触发视频素材包成本任务，避免把旧规划直接提交给 KIE/Vidu。
- 进展（2026-06-11 追加）：114 控制面已部署 `c59fa6f4+workspace-product-commercialization-20260611` 产品商业化试验补丁；默认预览巡检 `/srv/pod/reports/product-commercialization-preview-patrol-20260611-fixed2.json` 通过 `total=3 passed=3 failed=0`。真实 GPT Image 2 配图 runId `83f4dd35a8e44d02b946e8a090ae49fd`、真实 Vidu 8 秒单段素材包 runId `24858339ccd94e588866018ab2c49963` 均已回填 OSS。同步修复兜底时图片/字段未完成视觉核验却不提示冲突的问题，后续凡有产品图 + 导出字段但 VL/LLM 兜底，必须输出人工复核风险。
- 下一步：先按 `docs/strategy/market-side-ai-technical-plan-2026-06-11.md` 改当前 `product_commercialization`，再拆正式能力。P0 顺序：① schema 和文档把 `productFields` 明确为可选；② 后端事实解析图片优先；③ `preview` 输出 `videoAssetPackagePlan`；④ `/runs` 保存 `videoAssetPackage` 分段素材和可选合成结果；⑤ 测评端视频页面改成脚本/分镜/关键帧/分段视频/合成的分阶段交互；⑥ 补错误码、测试和真实链路证据；⑦ 在线上 114 或已加白后端环境执行产品商业化专项门禁。
- 验收：每个候选能力至少有一页能力契约和固定样例计划；是否实现由样例质量和业务优先级决定。`product_commercialization` 先验证大模型文案证据、语言、海外文案、详情介绍、配图策略、KIE/Vidu 单段视频和 OSS 沉淀；`product_image_set` 至少准备 3 类固定样例：服饰/配件、家居软装、节日营销图。

5. `todo` 质量迭代与观测方案
- 目标：把“效果不好/不稳定”转成可统计、可治理、可复盘的问题。
- 范围：固定样例包、质量标签、问题标签、分流/调参/换 LoRA/workflow/model、默认版本切换证据、控制面预聚合指标。
- 验收：能回答“哪类图效果不好、哪类图不稳定、下一步改什么”。

## 已完成/封版执行单（2026-06-07 v0.6 收口整改与标准化版）

说明：本节保留 v0.6 收口阶段记录，不再作为当前开发 TODO。v0.6 已于 2026-06-08 以 `516ba656` 完成封版验证；未完成的长期项进入 v0.7 正式规划。

方案：`docs/strategy/v0.6-closure-standardization-plan.md`

0. `done` 版本口径回收
- 目标：暂停 v0.7 当前执行，把主线回收到 v0.6 收口整改与标准化。
- 范围：唯一 TODO、战略索引、总文档入口、v0.6 收口方案、v0.7 后续储备。
- 进展（2026-06-07，当时口径）：新增 `docs/strategy/v0.6-closure-standardization-plan.md`，当时主线回到 v0.6 收口，v0.7 规划仅作为后续路线储备；该口径已在 2026-06-08 封版后切换为 v0.7 正式规划。
- 验收：新 agent 或新同事只看 `docs/README.md`、`docs/strategy/README.md` 和本 TODO，就能判断当前仍在 v0.6 收口阶段。

1. `done` 全量盘点缺失项
- 目标：先盘清问题，再进入整改，避免继续凭局部体验补丁推进。
- 范围：能力缺失、接口缺失、交互缺失、流程缺失、文档缺失。
- 进展（2026-06-07）：新增 `docs/strategy/v0.6-closure-inventory-2026-06-07.md`，完成首轮 P0/P1/P2 盘点。P0 聚焦提交成功和 `Q1001/Q1002` 错误契约、交互状态模型、`product_design` 样例质量闭环、API/错误文档同源校准。
- 验收：输出 v0.6 收口盘点清单，按 P0/P1/P2 分级；每个问题必须有归类、影响对象、证据入口和建议处理方式。

2. `doing` 业务逻辑和处理机制标准化
- 目标：把长期靠经验判断的流程固化成标准契约。
- 范围：任务提交成功口径、`runId/taskId` 生命周期、能力版本关系、执行节点路由、`Q1001/Q1002` 边界、队列/节点/网络波动处理、OSS 回填、回调、轮询、失败重试、Agent/直接图编辑边界。
- 进展（2026-06-07）：已完成第一批契约硬化：明确提交成功必须同时满足 HTTP 2xx、真实 `runId/taskId`、`queued/running` 状态、无 `ERR|...`、无 `errorCode`；补齐 `Q1001` 队列满与 `Q1002 / COMFYUI_EXECUTOR_UNAVAILABLE` 执行器不可用的边界。
- 验收：每条关键机制都有文档位置；关键机制有回归测试、巡检脚本或可验证样例覆盖。

3. `doing` 交互治理专项
- 目标：解决“能跑但不好用、每页局部修、没有整体交互规划”的问题。
- 范围：能力首页、能力工作台、Agent 对话页、任务追踪、质量复盘、管理端能力治理页、执行节点/队列页；统一首屏主任务、状态模型、结果区域、历史区域、排障区域和角色动线。
- 进展（2026-06-07）：新增 `docs/standards/eval-ability-interaction-state-model.md`，把测评端分类首页、能力工作台、AI 图片助手、直接图编辑、花纹提取、产品设计的空态、上传、提交、排队、执行、成功、失败、历史和排障状态固化为统一标准；下一步按该模型做浏览器路径走查和页面整改。
- 进展（2026-06-07）：完成首轮本地浏览器走查，记录见 `docs/testing/2026-06-07-v0.6-closure-eval-interaction-audit.md`。图编辑分类首页、AI 图片助手空态、直接图编辑工作台、产品设计、花纹提取均按能力组织；发现并修复产品设计工作台仍显示旧 `ComfyUI 执行面 / Coze 输出追踪` 的口径问题，统一为 `中台业务编排 · GPT Image 2 / 中台 runId 追踪`。
- 验收：核心页面都有用户动线和状态模型；页面整改后必须用浏览器模拟用户操作；每次页面改动说明删掉、折叠、保留或上移了什么。

4. `doing` 接口文档和错误契约补齐
- 目标：让业务方和客户端团队能按文档正确接入，不依赖聊天记录或人工解释。
- 范围：请求字段、响应字段、成功判断、`runId/taskId` 状态、错误码、队列/节点/超时/依赖/供应商失败、轮询/回调/回填/重试、示例 JSON。
- 进展（2026-06-07）：已把 `Q1002` 同步到错误码总表、队列与错误规范、接口一致性准则、业务 API 文档、Coze API 文档、API overview、管理端/测评端错误消息映射，并补充 `ERR|Q1002|...` 解析回归测试。
- 验收：修改过的接口同步 API 文档、错误码总表和接口一致性规范；`ERR|...`、`errorCode`、HTTP 状态码和业务状态的判断口径一致。

5. `doing` 能力缺口和样例质量补齐
- 目标：把客户端未来会用到的能力先标准化，不急于全部实现。
- 范围：`product_design` 固定样例、线上实测、效果复盘、推荐版本门槛；`product_image_set`、`model_shot`、`promo_video` 的能力定义、输入输出、错误、样例和成本。
- 进展（2026-06-07）：新增 `backend/app/services/business_quality_sample_seed.py` 与 `backend/scripts/seed_default_quality_samples.py`，为 `product_design` 提供 5 类默认固定样例（服装/面料、家纺软装、箱包、包装、电商主图）；已加回归测试验证 dry-run、重复导入、业务 key 和 `designBrief/productType/scene/quality` 参数。注意：代码层已有 seed，不等于 114 已导入，部署后仍需执行脚本并跑真实链路。
- 验收：每个候选能力至少有定义、样例、错误契约和是否进入实现的判断标准；不把客户端流程硬塞进中台。

6. `todo` 全链路回归和封版报告
- 目标：收口完成后用真实业务证明稳定，而不是只靠单元测试或局部页面走查。
- 范围：所有 Coze 接口、所有自有业务接口、每个核心接口至少 5 个真实案例、关键提交接口 3-5 并发、管理端排障链路、测评端真实动线、接口调用中心、任务状态、OSS 回填一致性。
- 验收：输出 v0.6 收口封版报告，包含已覆盖错误场景、剩余风险、回滚方式和下一阶段建议。

7. `todo` 复盘沉淀
- 目标：把这段时间踩过的坑变成规则，不再反复靠人工提醒。
- 范围：事故改进日志、交付方法论、接口一致性准则、当前 TODO、文档入口和下一阶段建议。
- 验收：v0.6 收口结束后，再评估是否进入 v0.7。

## 已迁移到当前 v0.7 的历史路线储备

说明：本节原为 v0.6 收口期的后续路线储备。2026-06-08 起，v0.7 已进入正式规划；实际执行以文件顶部“当前执行单（2026-06-08 v0.7 正式规划）”为准。

方案：`docs/strategy/v0.7-ability-productization-agent-orchestration-plan.md`

候选方向：

- 能力产品化与多能力 Agent 编排。
- Agent Runtime v2。
- 长期质量驾驶舱和控制面预聚合。
- 客户端交接刷新和下一阶段能力扩张。

## 已完成/封版执行单（2026-06-04 v0.6.3 中台能力控制面硬化版）

说明：本节保留 v0.6.3 阶段进展，不再作为当前开发 TODO。v0.6.3 不重开中台定义，不进入客户端实现；它承接 v0.6.2 封版后的真实验证结果，聚焦控制面承压、Agent Runtime 样板、能力治理降噪、能力缺口验收和文档门禁。

封版结论（2026-06-07）：v0.6.3 控制面硬化发布已完成；`88d48dce` 完成控制面和 Agent 验收，`6ba720e3` 完成测评端能力首页/版本列表/辅助导航收口并已上线。未完成的长期项已迁移到当前 v0.6 收口执行单。

方案：`docs/strategy/v0.6.3-control-plane-hardening-plan.md`

0. `done` 开工验收标准落地
- 目标：把响应速度、并发、真实业务回归、错误路径、文档维护和回滚线作为版本开工前门禁。
- 范围：唯一 TODO、发布 SOP、测试报告模板、封版记录。
- 进展（2026-06-05）：已把 `88d48dce` 的 Agent 五项验收、控制面 loopback/公网 20 并发、业务汇总、接口调用中心和保留风险写入 `docs/testing/2026-06-05-v0.6.3-88d48dce-agent-control-plane-validation.md` 与 `docs/releases/CHANGELOG.md`；后续每个补丁仍按同一门禁补证据。
- 进展（2026-06-05）：新增 `docs/standards/version-acceptance-template.md`，把开工验收、角色视角、响应速度、真实业务、错误路径、页面走查、线上观察和封版结论固化成每版可复制模板。
- 验收：本版所有开发项都能映射到明确验收标准；封版报告必须包含真实业务报告路径、p95、20 并发、线上观察和剩余风险。

1. `done` 控制面读接口预聚合与承压治理
- 目标：继续治理 v0.6.2 暴露的看板实时统计压力，避免高频页面依赖全量 run/step JSON 扫描。
- 范围：业务使用摘要、接口调用中心、能力版本指标、质量样例统计、ComfyUI 队列摘要的轻量化和短 TTL 缓存。
- 进展（2026-06-04）：接口调用中心 `/api/admin/business/api-key-usage` 已抽出查询函数并加入 12 秒管理端短 TTL 缓存；导出接口仍保持实时查询。新增 `backend/scripts/control_plane_read_benchmark.py`，用于固定跑 `/health`、业务能力列表、usage summary、api usage 和 ComfyUI queue summary 的顺序/20 并发 p95。
- 进展（2026-06-04）：`BusinessRunService` 原有 dashboard cache 与业务接口调用中心新缓存均已收窄锁范围，避免缓存未命中时在全局锁内执行查询。`get_comfyui_queue_summary` 已加入 8 秒短 TTL 与同 key 在途请求合并，避免管理端、Coze 插件、测评端高频刷新时同时打所有 ComfyUI 节点；健康回填仍由原始计算路径执行。
- 证据（2026-06-04）：当前线上 `c638c269` 经公网 20 并发基线 `output/benchmarks/control-plane-114-20c-20260604203805.json` 显示请求成功率 100%，但 `/health` p95 451ms、`comfyui_queue_summary` p95 5941ms 超阈值；重试 `output/benchmarks/control-plane-114-20c-retry-health-queue-20260604203853.json` 仍为 `/health` p95 441ms、`comfyui_queue_summary` p95 5656ms。后续发布新代码后必须在 114 本机 loopback 与公网各跑一轮，区分网络 RTT 与服务端耗时。
- 证据（2026-06-04）：`2654efb4` 发布后，114 本机 loopback 20 并发报告 `/srv/pod/reports/control-plane-2654efb4-loopback-20260604214139.json` 全部通过：`/health` p95 85.51ms，`business_capabilities` p95 272.25ms，`business_usage_summary` p95 75.68ms，`business_api_usage` p95 95.58ms，`comfyui_queue_summary` p95 70.49ms。公网 20 并发报告 `output/benchmarks/control-plane-114-20c-2654efb4-public-20260604214348.json` 显示 `comfyui_queue_summary` p95 459.63ms 已回落到阈值内；公网 `/health` p95 403.86ms 仍高于 300ms，但 loopback 正常，归因到公网 RTT/波动，不作为服务端阻断。
- 证据（2026-06-05）：`88d48dce` 线上补充验收后，114 本机 loopback 20 并发报告 `/srv/pod/reports/control-plane-88d48dce-loopback-20260605.json` 全部通过：`/health` p95 57.47ms，`business_capabilities` p95 447.98ms，`business_usage_summary` p95 120.57ms，`business_api_usage` p95 101.55ms，`comfyui_queue_summary` p95 63.28ms。公网报告 `/srv/pod/reports/control-plane-88d48dce-public-20260605.json` 中业务读接口全部通过，公网 `/health` p95 392.00ms；重试 `/srv/pod/reports/control-plane-88d48dce-public-health-retry-20260605.json` p95 501.11ms，因 loopback 正常，继续归因为公网 RTT/波动，不作为服务端阻断。
- 进展（2026-06-07）：v0.6.3 控制面短 TTL、并发折叠和 20 并发门禁已完成封版；长期后台预聚合进入当前 v0.6 收口盘点和标准化，后续再评估是否作为新版本实现。
- 验收：核心只读接口 20 并发成功率 100%，p95 <= 3000ms；高频只读接口 p95 <= 1500ms；列表接口默认不加载大字段和完整步骤明细。

2. `done` Agent Runtime MVP 整改
- 目标：把 AI 图片助手从“聊天式任务提交”整改成 Agent Runtime 最小样板。
- 范围：聊天瀑布流、结果图片进入消息流、上一轮结果续改、新任务隔离、结构化计划、路由证据、低置信追问、确认幂等、golden cases。
- 进展（2026-06-04）：补齐消息级 `requestId` 幂等控制：`POST /api/business/image-edit-chat/sessions/{sessionId}/messages` 在同一 `sessionId + requestId` 下只返回原方案，不重复生成计划；并通过 `business_agent_messages(session_id, request_id)` 唯一索引兜底并发重试。测评端追加消息已开始传 `requestId`，接口文档和错误码已同步。
- 进展（2026-06-05）：完成 AI 图片助手首轮体验降噪：空态去掉重复 Alert，右侧上下文栏只保留添加主图、当前状态和粘贴图片链接；工程化“调试信息”改为有会话后才出现的折叠“排障编号”。本地浏览器走查确认路由证据可见：基准=原始主图、路由=图编辑、置信度=84%，截图见 `output/playwright/20260605-image-edit-agent-empty-polished.png`、`output/playwright/20260605-image-edit-agent-plan-polished.png`。
- 进展（2026-06-05）：`88d48dce` 已完成线上 5 组 Agent 验收，报告 `/srv/pod/reports/agent-v063-88d48dce-20260605.json`：首轮真实执行 succeeded、二轮续改基于上一轮输出 succeeded、新任务隔离 succeeded、模糊意图返回 `409 AGENT_PLAN_REQUIRES_CLARIFICATION` 且未创建下游 run、消息 `requestId` 幂等返回同一 plan。
- 进展（2026-06-05）：新增 `docs/standards/agent-runtime-regression-matrix.md`，把 Agent MVP 的 5 组 golden cases、路由稳定性、并发幂等、失败路径和前端交互验收固化为后续每次改 Agent 必跑的矩阵。
- 进展（2026-06-07）：Agent Runtime MVP 已完成 v0.6.3 验收；多能力路由、方法论流水线和上下文压缩增强暂作后续路线储备，当前先在 v0.6 收口版补齐边界、文档、交互和回归标准。
- 进展（2026-06-08）：按质量优先策略再次纠偏 AI 图片助手：普通单张花纹提取类诉求默认走 `business.image_edit`，由 GPT-5.5 规划并交给 GPT Image 2 执行；只有用户明确要求批量、快速、低成本或专项能力时才分流到 `business.pattern_extract`。后端 planner JSON、新增 `routeType`、候选专项能力证据和文档回归矩阵已同步升级为 7 组 golden cases。
- 验收：7 组真实对话全链路通过；二轮续改默认基于最新成功输出；新任务不污染旧上下文；普通单张花纹提取返回 `routeType=image2_quality_first` 并保留 `specializedAbilityCandidate`；明确批量/快速/低成本返回 `routeType=ability_accelerated`；每次执行可在排障详情看到 `routeReason/confidence/baseImageRole/parentRunId`。

3. `done` 能力治理和测评端降噪
- 目标：按能力组织测评端和治理页，让业务方、运营、运维、能力工程师都能先看到当前结论和下一步动作。
- 范围：推荐版本区保持主注意力、版本列表展开、辅助工具导航独立、首次使用示例/图例、清理项目主语。
- 进展（2026-06-05）：先补齐版本验收模板中的四类角色视角和页面交互标准，作为后续测评端/管理端降噪的验收基线：业务方看能力选择和结果，运营看样例与质量复盘，运维看路由/队列/节点/错误，能力工程师看版本/参数/回滚。
- 进展（2026-06-05）：测评端首页完成第一轮 P0-3 降噪实现：当前分类增加“样例起步”区，固定样例或轻量图例在版本列表前常显；版本列表改为普通常显 section，不再使用可收起 details；辅助工具改成独立紧凑导航，不再用大面积分类卡片抢主路径。生产构建通过，浏览器走查图编辑/花纹提取桌面和移动端无 console error，截图见 `output/playwright/20260605-eval-home-image-edit-quickstart.png`、`output/playwright/20260605-eval-home-pattern-quickstart.png`。
- 进展（2026-06-07）：`6ba720e3` 已完成测评端能力首页收口并上线：推荐能力区保持主注意力，版本列表常显，辅助工具独立导航，图编辑和花纹提取页面已在线走查。
- 验收：业务方第一次进入能判断用哪个能力、上传什么、看哪里；运维能看到路由/队列/节点/错误；运营能看到样例、质量标签和复盘入口。

4. `archived` 首批能力缺口闭环
- 目标：把客户端未来需要的业务组装能力转成中台能力契约，而不是把流程塞进中台。
- 范围：`product_design` 线上实测和质量样例；`product_image_set`、`model_shot`、`promo_video` 的能力定义、输入输出、错误码、成本和验收样例。
- 迁移（2026-06-07）：该项作为 v0.6.3 未完成长期项迁移到当前 v0.6 收口执行单的“能力缺口和样例质量补齐”。
- 验收：每个候选能力至少有能力定义、推荐入口、错误契约、固定样例和封版门槛；是否实现由样例和业务优先级决定。

5. `archived` 文档与规则清理
- 目标：保持当前主线文档简洁、可恢复、无旧概念误导。
- 范围：战略索引、唯一 TODO、能力方案、API 文档、错误码、发布 SOP、事故改进日志、旧客户端和旧项目主语清理。
- 进展（2026-06-05）：新增 `docs/testing/2026-06-05-v0.6.3-88d48dce-agent-control-plane-validation.md`，并在 `docs/releases/CHANGELOG.md` 记录 `88d48dce` 当前线上验收、Agent 报告路径、控制面基准和受控失败归因；仍需继续清理历史文档中的过期 WIP 入口。
- 进展（2026-06-05）：新增版本验收模板和 Agent Runtime 回归矩阵，并接入 `docs/README.md`、`docs/strategy/README.md`、`docs/standards/release-sop.md` 与当前 v0.6.3 方案，避免后续验收标准继续散落在会话里。
- 迁移（2026-06-07）：持续文档清理和客户端交接刷新进入当前 v0.6 收口执行单的“复盘沉淀”。
- 验收：新 agent 或新同事只看 `docs/strategy/README.md` 和本 TODO 就能恢复主线；历史方案不会被误读为当前任务。

## v0.6 阶段执行单（2026-06-02 中台能力治理版，保留进展）

说明：本节保留 v0.6 阶段进展。v0.6 主线仍然是中台能力治理与测评端能力验证；客户端负责业务流程组装，但当前中台侧只输出方案、边界和能力清单，不进入客户端实现。中台主语是能力，调用上下文只作为证据索引。未完成事项已收敛到当前 v0.6 收口执行单继续推进。

0. `done` v0.6 方案与文档入口切换
- 目标：把当前主线从 v0.5 平台打磨切到中台能力治理、能力调用证据和客户端支撑底座。
- 方案：`docs/strategy/mid-platform-completeness-v0.6-plan.md`
- 能力治理：`docs/strategy/ability-governance-operating-model-v0.6.md`
- 能力定义：`docs/strategy/ability-definition-v0.6.md`
- 对象/API 草案：`docs/strategy/end-to-end-business-object-api-v0.6.md`
- 能力缺口：`docs/strategy/ability-api-gap-v0.6.md`
- 后端实施：`docs/strategy/project-context-backend-design-v0.6.md` 仅作为历史兼容实现记录，当前口径改为调用上下文。
- 客户端交接：`docs/strategy/client-parallel-preview-v0.6-handoff.md`
- 进展（2026-06-02）：已明确 v0.6 主版本仍是中台；客户端从本阶段并行启动并负责业务组装，但不接管中台、不直连原子能力、不复活旧客户端代码。
- 进展（2026-06-02）：根据边界复核，已将方案从“中台端到端编排”修正为“中台组装能力，客户端组装业务”。
- 进展（2026-06-02）：根据二次评审纠偏，补充 `ability-governance-operating-model-v0.6.md`，明确“能力是中台产品，客户端是能力的业务化使用界面”。
- 进展（2026-06-03）：根据再次评审，明确中台和测评端只围绕能力；项目/工作单只属于客户端业务组装语言，现有 `projectId` 只能作为兼容上下文字段，不作为中台主概念。
- 验收：恢复上下文时能直接看出下一步是“能力定义 + 能力 API 完整性 + 能力治理 + 测评端能力验证 + runId/资产/质量/成本证据”，不是继续只做 v0.5 页面打磨，也不是让中台硬编码客户端流程或转成项目管理产品。

1. `done` 调用上下文与资产证据兼容底座
- 目标：定稿调用上下文、Asset、RunLink、Selection、DeliveryEvidence 等能力调用证据字段；历史 Project 命名只作兼容。
- 范围：对象字段、状态枚举、错误码、鉴权边界、租户范围、客户端上下文写入权限。
- 当前动作：基于 `end-to-end-business-object-api-v0.6.md` 做字段评审，并决定首批 Alembic 表结构和 API 路径。
- 进展（2026-06-02）：新增 `project-context-backend-design-v0.6.md`，这是历史兼容实现记录；当前只把 `projectId/flowStepKey` 视为调用上下文字段，资产沉淀失败不改写已成功 run。
- 进展（2026-06-02）：首批后端实现已落地到业务项目模型、迁移、服务、API、错误码和接口文档；这些命名不再作为中台产品主语继续扩展。
- 验收：对象能支撑“客户端上下文 -> 素材 -> 业务 run 关联 -> 选择 -> 交付证据”，且客户端声明的 `flowStepKey` 可追溯但不由中台强制编排。

2. `done` 资产证据底座
- 目标：新增资产、run 关联、选择记录和导出包的后端模型、迁移、服务和基础 API；不把项目作为中台产品主语。
- 范围：`backend/app/models`、`schemas`、`routers/business.py` 或独立业务项目 router、测试和错误码。
- 进展（2026-06-02）：新增 `business_projects`、`business_project_assets`、`business_project_run_links`、`business_project_selections`、`business_export_packages` 以及对应业务 API；作为调用上下文兼容底座使用。
- 验收：能登记素材、关联 run、查询证据详情，错误路径覆盖缺参、非法 URL、越权、上下文不存在。

3. `done` 业务 run 调用上下文扩展
- 目标：让现有花纹提取、图裂变、扩图、图编辑等业务运行可写入客户端上下文、`flowStepKey` 和输入资产。
- 范围：业务提交接口上下文字段、RunLink 写入、输出资产自动沉淀、上下文 run 列表。
- 进展（2026-06-02）：`BusinessRunCreateRequest` 已支持 `projectId/flowStepKey/flowStepName/flowTemplateId/inputAssetIds/clientRequestId`；业务 run 创建时写入调用上下文关联，终态成功后自动把 `imageUrls/videoUrls` 登记为输出资产，资产同步失败不会改写业务 run 终态。
- 验收：客户端调用任一业务 API 时可传调用上下文；成功后可按 runId/上下文看到对应输出资产。

4. `done` 候选选择与质量证据
- 目标：支持客户端记录候选选择，并保留质量标签、问题标签和选择备注。
- 范围：Selection API、资产 selected 状态、质量复盘表复用、管理端展示。
- 进展（2026-06-02）：新增候选选择 API，可按 `assetIds/sourceFlowStepKey/targetFlowStepKey/note` 记录选择并把资产标记为 selected；质量等级、输入标签、问题标签随输出资产保存。
- 验收：用户可选择一个裂变候选并声明目标 `flowStepKey`；中台能追溯选择人、选择时间、来源步骤和来源 run。

5. `done` 交付证据包
- 目标：支持按能力调用上下文生成交付包，包含最终图、视频、参数摘要、runId 和质量证据。
- 范围：导出包 manifest、文件清单、脱敏规则、下载地址、失败重试。
- 进展（2026-06-02）：第一版先支持生成 `ready` 状态的 manifest/summary 草稿，包含资产清单、runId 和质量摘要。
- 进展（2026-06-02）：已补齐交付 ZIP 生成和 `/download` 下载接口；ZIP 内包含 `manifest.json`、`summary.json`、`assets.json`、`run_ids.json` 和 `README.txt`，媒体二进制暂不打入包内，继续通过自有 URL 引用。
- 验收：至少能生成一个包含资产清单和 runId 证据的交付包草稿；密钥、内部 URL、厂商原始响应不外泄。

6. `done` 管理端能力治理入口
- 目标：管理端优先解释每个业务能力的版本、路由、质量、成本、失败卡点和优化动作，再下钻到 runId、能力调用日志和资产证据。
- 范围：能力总览、能力详情、默认/候选/回滚版本、路由和执行节点、质量样例、失败分布、成本、关联 runId、资产列表、交付包证据。
- 进展（2026-06-02）：管理端“业务能力”模块新增默认页签“能力治理”，一屏聚合每个业务能力的默认版本、候选/回滚版本、近窗口调用、质量风险、成本、上线门禁、验收证据和当前要做；原“治理记录”改为“变更治理”，继续承载审批、对比、回滚和操作日志。
- 验收：管理员先按能力判断“是否稳定、是否值得优化、如何回滚”，再用 runId 或客户端上下文下钻看到步骤、输出资产、错误码和质量证据；管理端不编辑客户端流程。

7. `done` 客户端边界方案与接口缺口治理
- 目标：给客户端团队或新 agent 提供稳定边界方案，统一收口接口缺口；当前不进入客户端实现。
- 范围：客户端只能消费 `/api/business/*`、能力调用上下文/资产证据 API 和受控 media API；不能调用 admin/evals/abilities/coze/comfyui。
- 进展（2026-06-02）：客户端任务包已从“项目主视角”纠偏为“能力工作台主视角”；`projectId` 只作为兼容上下文字段。当前不继续实现客户端，只保留方案边界。
- 验收：客户端方案清楚写明页面、API、mock 范围、禁止项和联调标准；接口缺口进入中台能力 TODO，不由客户端绕路实现，也不把客户端做成项目管理系统。

8. `done` AI 图片助手 P0
- 目标：把对话改图作为独立 Agent 入口接入，不改造中台、不绕开业务 API；底层可调用图编辑，但产品入口和直接图编辑拆开。
- 范围：`image_edit_chat`、底层 `agent.image_edit_assistant`、会话/消息/方案/工具调用证据表、`/api/business/image-edit-chat/*`、测评端对话改图面板、错误码和 API 文档。
- 进展（2026-06-02）：新增轻量 Agent Runtime，支持规则 planner 兜底；配置 GPT-5.5/Responses Key 后可切模型 planner；确认前只生成方案，确认后调用 `image_edit` 业务 run。
- 进展（2026-06-02）：业务运行完整详情新增 `agentTrace`，管理端 run 详情可直接看到 Agent 会话、方案、工具调用、执行边界时间和下发参数，Agent 创建的普通业务 `runId` 已纳入同一条排障证据链。
- 进展（2026-06-03）：产品边界修正为“AI 图片助手”和“直接图编辑”两个功能；新增推荐路径 `/api/business/image-edit-chat/*`，旧 `/agents/image-edit/*` 仅保留技术兼容。
- 进展（2026-06-03）：测评端图编辑两个工具页完成操作优先降噪；首屏先展示 AI 图片助手/直接图编辑真实操作区，流程、版本、接口和验收焦点默认折叠到操作区之后。
- 进展（2026-06-03）：补充定义口径：对话改图可暂时放在图编辑分类下，但长期归宿是 Agent；可视化改图与对话改图是两条方向，前者面向标注和精确提交，后者面向自然语言、多轮追改和未来多能力编排。
- 进展（2026-06-07）：纠正 Agent 入口命名和执行心智：对用户是 AI 图片助手/图片版 Codex，不是改图表单；`confirm` 只作为后端幂等执行边界，普通单步图片能力可在计划满足条件后自动提交；花纹提取已纳入当前白名单而非后续储备。
- 验收：测评端可完成“聊天输入图片任务诉求 -> AI 图片助手回复计划 -> 自动进入后端执行边界或继续追问 -> 查看 runId/结果”的闭环；Agent 工具调用可追溯且只允许当前白名单业务能力。

9. `archived` 首批能力缺口决策
- 目标：从中台视角审查客户端未来业务组装需要哪些业务能力包装，而不是由中台决定客户端流程。
- 范围：产品设计图、组图、多角度、模特图、推广视频的能力定义、输入输出、错误码、样例和验收标准。
- 进展（2026-06-02）：新增 `ability-api-gap-v0.6.md`，确认现有可复用能力为花纹提取、图裂变、文字裂变、裂变评分、扩图、图编辑；v0.6 缺口为 `product_design`、`product_image_set`、`model_shot`、`promo_video` 等业务包装，其中 `product_design` 建议 P0。
- 进展（2026-06-03）：`product_design` 首版业务包装已补齐，包含业务能力 seed、`/api/business/product-design/runs`、route-preview、OpenAPI、错误码、测评端能力卡和管理端核心能力治理映射；下一步做线上实测、固定质量样例和效果复盘。
- 迁移（2026-06-07）：能力缺口产品化进入当前 v0.6 收口执行单的“能力缺口和样例质量补齐”。
- 验收：能形成 v0.6 MVP 的能力缺口清单、输入输出、样例图和验收标准；客户端流程顺序由客户端方案维护，不进入中台实现主线。

10. `done` 对话改图 Agent MVP 整改与路由稳定性
- 目标：把对话改图从“能跑通的聊天功能”整改成 Agent Runtime 最小样板，支撑后续方法论流水线和多能力路由。
- 范围：聊天瀑布流、新任务隔离、上一轮结果续改、当前基准图状态、结构化计划、路由证据、低置信追问、后端幂等、队列状态、错误契约、golden case 路由回归。
- 原则：前端只负责体验和确认；后端负责会话、上下文压缩、方法论、能力路由、任务队列、成本、Key 池和审计。不得让前端直接决定底层工具或调用厂商接口。
- 进展（2026-06-03）：已把长期边界补入 `ability-definition-v0.6.md` 与 `business-agent-runtime-v0.6.md`；下一步按 MVP 稳定性门禁整改代码和测试。
- 进展（2026-06-05）：测评端 AI 图片助手补齐聊天流结果回填：当前 run 会被合成为 tool 气泡，轮询成功后结果图直接出现在对话消息中，继续输入默认携带 `baseImageRole=previous_result` 与 `previousRunId`；新增 Playwright 回归固定“结果在聊天流、二轮续改基于上一轮输出、新建任务保留历史线程”。
- 进展（2026-06-07）：v0.6 Agent MVP 已按 v0.6.3 验收收口；路由契约硬化进入当前 v0.6 收口执行单，多能力 Agent 演进暂作后续路线储备。
- 验收：同一会话多轮改图默认基于最新成功输出；新任务不污染旧上下文；每次执行能看到 routeReason/confidence/baseImageRole/parentRunId；模糊意图会追问；并发、超时、队列满、供应商失败都有明确状态和错误；golden cases 多次运行路由稳定。

## 已完成/封版执行单（2026-05-26 v0.5 平台打磨版）

说明：本节保留为 v0.5 阶段记录，不再作为当前开发 TODO。v0.5 不做大业务能力扩张，重点从“能跑通”切到“看得懂、用得顺、能复盘、可持续小步优化”。

0. `done` v0.5 方案与文档入口切换
- 目标：把当前主线从 v0.4.1 稳定性封版切到平台打磨和迭代治理。
- 方案：`docs/strategy/platform-polish-v0.5-decision-plan.md`
- 当前动作：更新唯一 TODO、战略索引和当前回顾；确认 v0.5 MVP 和决策项。
- 进展（2026-05-26）：已新增半年会回顾和 v0.5 决策方案，并将唯一 TODO、总文档入口、战略索引切换到 v0.5；待评审后进入首轮页面走查和实现。
- 进展（2026-05-26）：用户确认开始 v0.5，当前执行单正式生效。
- 验收：恢复上下文时能直接看出下一步是“业务流可视化 + 流程监控 + 效果复盘 + 页面降噪”，不是继续只做稳定性观察。

1. `doing` 平台使用式体检机制
- 目标：后续每轮不只看代码和接口，要固定打开管理端/测评端，模拟运营、运维、业务方和能力工程师操作。
- 范围：管理端总览、业务能力、接口调用、ComfyUI、测评端首页、核心功能工作台。
- 进展（2026-05-26）：已启动首轮本地管理端走查并接 114 后端；页面可打开，114 `/health` 正常，但管理端登录接口返回 `INVALID_CREDENTIALS`，真实数据截图待有效登录态补齐。记录见 `docs/testing/2026-05-26-v0.5-platform-polish-start.md`。
- 进展（2026-05-27）：已使用 `admin / admin123` 完成同版后端真实登录走查；管理端总体概览、业务能力、接口调用、ComfyUI 资源，以及测评端首页、图编辑工作台、任务追踪、LoRA 批量回归、接口文档、维护配置均完成浏览器复测。走查发现 v0.5 质量表未迁移会导致质量面板 500，已补读路径兼容并记录在 `docs/testing/2026-05-26-v0.5-platform-polish-start.md`。
- 验收：每轮输出平台打磨走查报告，包含截图、首屏噪音 Top 3、已删减/折叠/保留内容和角色影响。

2. `doing` 管理端总览状态结论页
- 目标：管理员 30 秒内判断平台是否可用、哪里阻塞、下一步做什么。
- 范围：真实风险、数据未加载、待人工确认、正常状态分层；首屏只保留当前结论、关键阻塞和最近变化。
- 进展（2026-05-26）：总览首屏已改为“状态结论 + 当前关注 + 下一步”三段式，稳定性信号和平台功能入口地图默认折叠；浏览器复核截图见 `output/playwright/platform-polish-v0.5-20260526/overview-status-conclusion.png` 与 `overview-status-conclusion-expanded.png`。
- 验收：首屏不再混淆加载态和风险态；不再出现无解释的 `主业务 4/3` 类指标。

3. `doing` 测评端推荐业务入口与核心工作台打磨
- 目标：测评端从能力市场变成业务方试用/验收工作台。
- 范围：首页只展示推荐业务；图裂变/图编辑工作台优先完成“一次测试 -> 当前结果 -> 历史复盘”分层。
- 进展（2026-05-26）：测评端首页已收敛为推荐业务入口，默认展示图裂变、图编辑、扩图、花纹提取、文字裂变；版本列表和更多工具默认折叠。工具页已新增“一次测试 / 当前结果 / 历史复盘”三段路径，并把图裂变、图编辑真实浏览器路径跑通，截图见 `output/playwright/platform-polish-v0.5-20260526/eval-business-entry-home.png`、`eval-workbench-flow-fission.png`、`eval-workbench-flow-image-edit.png`。
- 进展（2026-05-26）：测评端已接入固定样例库只读接口 `/api/evals/business/quality-samples`，推荐业务卡片和功能工作台可显示样例数量并一键套用样例图片、提示词和默认参数，避免业务方手填样例 URL。
- 进展（2026-05-26）：管理端固定样例库补齐 OSS 直传、批量导入 upsert、dryRun 预检查和样例版本历史，运营维护样例不再只依赖手填 URL。
- 进展（2026-05-27）：功能工作台新增“业务验收焦点”，把固定样例、当前结果、历史待打标和角色下一步合并到首屏；输入变更后会隐藏旧结果，避免业务方把上一条输出误判成本次验收结果。截图见 `output/playwright/platform-polish-v0.5-20260526/eval-business-acceptance-focus-sample-ready.png`、`eval-business-acceptance-focus-running.png`。
- 进展（2026-05-27）：历史复盘区继续降噪为“复盘优先级 + 汇总按钮 + 历史明细下钻”。普通进入默认不展开逐条历史和大图对比；点击失败、未打分等汇总后自动展开对应明细。截图见 `output/playwright/platform-polish-v0.5-20260526/eval-history-review-collapsed.png`、`eval-history-review-filter-expanded.png`。
- 进展（2026-05-27）：任务追踪页新增“任务追踪结论 + 六项任务汇总 + 最近任务明细”，把运行中、失败、缺结果、未评分等状态转成下一步动作；明细表保留 debug_url、中台任务、回填和成本证据。截图见 `output/playwright/platform-polish-v0.5-20260526/eval-task-tracking-focus-running.png`、`eval-task-tracking-focus-failed.png`。
- 验收：业务方第一次进入能判断用哪个能力，不需要理解历史版本、workflow、executor 或专家参数。

4. `doing` runId 七阶段业务流可视化
- 目标：任何业务方反馈一个 runId，都能按主线看到问题发生在哪一段。
- 阶段：提交入口、版本命中、输入预处理、路由/分流、主执行、结果入库、回调/计费。
- 进展（2026-05-26）：管理端业务调用详情已从原“版本/能力/节点/结果/回调/计费”收敛为七阶段视图，复用 `apiUsage`、`flowSummary`、`steps`、`routeInfo` 和计费/回调字段派生阶段状态；管理端 `npm run lint` 和 `npm run build` 已通过，真实数据截图待有效登录态补齐。
- 进展（2026-05-27）：runId 详情顶部新增“流程监控结论”，把当前卡点、质量证据、治理动作和阶段完整度合并到首屏；出图标注会直接影响质量证据结论，风险样本会提示沉淀治理项或分流/换 LoRA/workflow。
- 验收：一个 runId 出问题时，1 分钟内能看到卡点、耗时、关键证据、错误码和下一步动作；原始 JSON 和长日志默认折叠。

5. `doing` 业务能力流程监控摘要
- 目标：每个核心业务都能持续观察近 24 小时/7 天流程健康，而不是只看成功失败。
- 范围：图裂变、图编辑、扩图、花纹提取、文字裂变；覆盖 failed、running 超时、queued 堆积、回填中卡住、已恢复历史失败。
- 进展（2026-05-26）：业务能力页已新增“流程监控摘要”，按七阶段汇总近窗口入口样本、版本/参数/执行/结果/回调/计费问题、排队/运行中和未恢复问题；核心业务口径同步扩为花纹提取、图裂变、图编辑、扩图、文字裂变。`/api/admin/business/usage-summary` 已新增 `flowEvidence`，可展示阶段耗时、路由命中、候选命中、LoRA 命中和 workflow 命中；真实数据截图待同版后端部署后补齐。
- 进展（2026-05-27）：流程监控摘要继续降噪为“当前卡点 + 七阶段流程轨道 + 阶段明细折叠”。主屏新增运营/运维两类处理动作，运维先看链路卡点，运营先看样例/版本/业务方下一步；七阶段耗时和证据默认收起。
- 验收：每个核心业务能看到阶段耗时、主要卡点、ComfyUI 队列/回填和第三方厂商状态。

6. `doing` 出图效果复盘与质量标签体系
- 目标：进入迭代期后定期看每个功能的出图效果，把“效果不好”变成可统计的问题。
- 范围：质量档位、问题标签、输入图标签、固定样例池、质量问题 TopN。
- 进展（2026-05-26）：业务能力页已新增“出图效果复盘”面板，按五个核心业务展示固定样例池、可抽检样本、本轮建议抽检数、输入标签、问题标签和下一步动作；质量标签方案见 `docs/strategy/output-quality-review-v0.5.md`。
- 进展（2026-05-26）：新增 `business_output_reviews` 表和管理端接口，支持按 runId/outputIndex 保存质量档位、输入标签、问题标签、建议动作和备注；新增近窗口质量复盘汇总接口，业务页可读取已标注数量、质量档位分布、业务 TopN 输入/问题标签；runId 详情已接入单张出图标注控件，运营可在结果预览旁直接落标注。
- 进展（2026-05-26）：默认版本切换门禁已接入质量证据，五个核心业务候选版本近 168 小时至少需要一张 `excellent` 或 `usable` 输出标注；缺标注、缺可用样本会阻断默认切换，风险样本会在上线证据中提示复核。
- 进展（2026-05-26）：TopN 输入/问题标签已接入样例下钻，复盘统计可打开对应 runId，并在 runId 详情中定位到具体输出序号。
- 进展（2026-05-26）：质量汇总新增版本维度，业务页已接入“候选版本对照”面板，按核心业务聚合默认/候选/草稿版本，展示路由、workflow、LoRA、模型、最近运行和版本级质量证据。
- 进展（2026-05-26）：固定样例复跑已升级为批量样例集，支持同一业务一次选择多个样例和多个版本，提交时写入统一 `batchId`；质量标注完成后通过 `byBatch` 形成同批结果对照。
- 进展（2026-05-26）：新增 `business_quality_samples` 固定样例库，支持按业务保存、复用、停用和归档样例 URL、提示词、默认参数；候选版本面板会优先用样例库预填批量复跑，库不可用时退回内置样例类型。
- 进展（2026-05-26）：固定样例库新增测评端只读复用入口，运营维护一次样例后，业务方验收页可直接用同一批样例跑推荐版本，减少“管理端有样例、测评端还要手工复制 URL”的断点。
- 进展（2026-05-27）：出图效果复盘继续降噪为“本轮关注 + 业务行 + 标签体系与样例下钻”。首屏直接告诉运营抽检哪类图、能力工程师按什么问题标签准备分流/调参/换 LoRA；输入标签、问题标签和最近质量样例默认折叠。
- 进展（2026-05-27）：质量复盘相关表缺失时，管理端和测评端读接口已返回空汇总/空列表，不再 500；上线 v0.5 前仍必须执行 Alembic 迁移，兼容逻辑只保证发布前后页面可用。
- 验收：能回答哪类图效果不好、哪类图稳定性不高、建议调参数/分流/换 LoRA/换 workflow/暂停推荐；运营能复用固定样例跑同批版本对照。

7. `doing` 分流规则与 LoRA 候选治理
- 目标：把“哪类图效果不好就分流、哪类图不稳定就换 LoRA”做成受控小迭代。
- 范围：图裂变分流规则台账；扩图、花纹提取 LoRA / workflow 候选记录；候选状态 draft/candidate/validated/default/paused/rejected。
- 进展（2026-05-26）：已完成第一步“候选版本对照”视图，先把默认/候选/草稿版本、route/workflow/LoRA/model、最近运行和质量样例放到同一处；固定样例批量复跑会把 batchId、样例类型、输入图和候选版本写入提交 metadata，方便后续沉淀分流规则和 LoRA 候选台账。
- 进展（2026-05-26）：新增 `business_quality_action_rules` 质量治理台账，支持按业务记录问题标签、输入标签、治理动作、目标候选版本/LoRA/workflow 引用、状态和证据；管理端候选版本面板可查看并新增治理项，但暂不自动改线上路由。
- 进展（2026-05-26）：流程监控摘要已把真实路由/候选/LoRA/workflow 命中从业务运行与步骤记录中聚合出来，用于后续按命中项分组抽检出图效果；该统计只观察，不自动切换线上策略。
- 进展（2026-05-27）：测评端“批量回归”页新增“LoRA 批测结论 + 六项批次摘要 + 批测规则折叠”，把运行中、失败、上传失败、未入库和待标注状态转成下一步动作；截图见 `output/playwright/platform-polish-v0.5-20260526/eval-lora-batch-focus-default.png`。
- 验收：默认版本切换前必须有候选记录、样例对照、影响范围和回滚方式；没有样例证据不切默认。

8. `doing` 视觉降噪、历史版本折叠和图编辑交付体验
- 目标：减少冗余内容，让关键结论一眼可见，同时明确图编辑业务方如何使用和更新。
- 范围：状态色、标签、表格、加载态、空态统一；历史版本默认折叠；图编辑托管组件/源码组件更新方式说明。
- 进展（2026-05-27）：业务能力页“候选对照与固定样例复跑”已做首轮降噪：每个业务默认只展示结论、下一步、默认版本、候选路线、质量证据和最近运行；固定样例、质量治理台账、版本明细、同批质量对照默认折叠，运营/运维需要下钻时再展开。
- 进展（2026-05-27）：业务版本列表新增历史版本默认折叠：默认/草稿/候选版本保留在主判断区，已停用、历史路线或被新版本替代的版本收进“历史版本”下钻，并在业务卡片标题只保留数量提示。
- 进展（2026-05-27）：测评端图编辑入口新增“图编辑交付方式”简表，明确业务方优先使用托管 `/image-edit` 自动获得更新；只有自有页面深度嵌入时才使用源码组件，并通过 `component-config` 判断是否需要更新组件包。
- 进展（2026-05-27）：测评端“接口文档”新增“文档结论 + 五项文档摘要 + 联调准则折叠”，默认先判断文档是否可交付、是否缺 Schema、错误码覆盖是否完整；“维护配置”新增“维护结论 + 四项维护摘要 + 明细下钻”，先提示 Token、Schema 缺口和下一步动作。截图见 `output/playwright/platform-polish-v0.5-20260526/eval-docs-delivery-focus-default.png`、`eval-maintenance-focus-default.png`。
- 进展（2026-05-27）：测评端快速切换视图时的请求中止不再打 `ApiRequestError` console 噪音，真实页面走查保留业务接口 4xx/5xx 为 0 的判断口径。
- 进展（2026-06-03）：交付方法论准则新增“多视角批判与六顶思考帽评审”，后续平台打磨、能力接入、交互改版、封版和事故复盘必须从业务方、运营、运维、能力工程师、接入开发者和平台负责人等视角轮换审查，禁止只用开发者视角证明可用。
- 验收：核心页面能说明删掉、折叠或下移了哪些冗余内容；图编辑接入首页不超过 1 屏半。

## 已完成执行单（2026-05-26 v0.4.1 稳定性监控与管理端降噪版）

说明：v0.4.1 已完成封版验证，保留为阶段记录，不再作为当前开发 TODO。

0. `done` v0.4.1 方案与文档入口切换
- 目标：把当前主线从 v0.4.0 功能封版切到稳定性监控与管理端降噪。
- 方案：`docs/strategy/business-stability-observability-v0.4.1-plan.md`
- 当前动作：更新唯一 TODO、战略索引、版本记录和发布 SOP。
- 进展（2026-05-26）：已新增 v0.4.1 方案，战略索引、版本记录、发布 SOP 和事故改进日志已同步。
- 验收：恢复上下文时能直接看出下一步是“稳定性监控 + 页面降噪”，不是继续扩新能力。

1. `done` 连接池与后台线程治理
- 目标：避免 backend 再次因后台线程膨胀或 DB pool 过小导致业务接口整体超时。
- 范围：`backend/app/core/config.py`、`backend/app/core/db.py`、service 构造副作用审计、相关测试。
- 进展（2026-05-26）：数据库连接池新增显式配置 `DATABASE_POOL_SIZE`、`DATABASE_MAX_OVERFLOW`、`DATABASE_POOL_TIMEOUT`、`DATABASE_POOL_RECYCLE`；默认从原 SQLAlchemy 隐式 `5+10` 提升为显式 `20+20`。
- 进展（2026-05-26）：`cd2b8964` 已发布到 114，发布后 backend 日志观察未再出现 `QueuePool` 或业务 finalize loop 回归。
- 验收：本地测试覆盖 engine options；线上发布后观察 backend 线程数和 `QueuePool` 日志不再异常增长。

2. `done` 监控与发版门禁补强
- 目标：让连接池/finalize loop 类事故在发版和定时巡检中自动暴露。
- 范围：`backend/scripts/podi_release_smoke.py`、`scripts/run_podi_health_watch.sh`、`docs/standards/release-sop.md`。
- 进展（2026-05-26）：发布 smoke 新增 backend journal 回归扫描，默认检查近 30 分钟 `QueuePool` 和 `business run finalize loop failed`，命中即阻断；本地测试已覆盖命中阻断和 journalctl 不可用时跳过。
- 进展（2026-05-26）：正式发布脚本改为从 `.release_time` 开始扫描 backend journal，避免部署前旧错误误判为新版本回归。
- 进展（2026-05-26）：修复 `.release_time` ISO 格式导致 `journalctl --since` 解析失败的问题，最终发布 smoke 中 `backend_log_regression` 实际扫描通过，`matches=0 max=0`。
- 验收：114 发版 smoke 能在近期 backend 连接池错误未清零时失败；health-watch 报告包含可追溯结果。

3. `done` ComfyUI running 状态收敛
- 目标：治理“中台 running 但 ComfyUI 队列为空”的残留诊断。
- 范围：队列汇总、任务 finalize、history 补偿、OSS 回填补偿、超时标记。
- 进展（2026-05-26）：队列汇总新增 `backendRunningInvisibleServers`、`backendRunningSettlingServers`、`backendRunningStaleGraceSeconds` 和单线路 `feedAction`；5 分钟宽限窗口内标记为“观察中”，超过窗口才计入 `backendBlockedServers` 并阻断 smoke。
- 进展（2026-05-26）：管理端 ComfyUI 任务衔接诊断和执行节点卡片展示最早执行时间、已持续时长和下一步动作，避免短暂回填窗口被误判为卡死。
- 进展（2026-05-26）：114 发布后真实业务巡检和 production 测评巡检均完成，ComfyUI 双节点 queue summary 显示 `blocked=0 settling=0 diagnostics=[]`。
- 验收：真实巡检结束后，backend running 与 ComfyUI 队列能在合理窗口内收敛；残留任务可定位到 taskId/runId、执行节点和下一步处理动作。

4. `done` 管理端视觉与文案降噪
- 目标：首页、业务能力、接口调用、ComfyUI 资源页优先展示状态结论和下一步动作。
- 要求：少展示长说明和内部字段；技术详情折叠到排障信息；错误提示使用“发生了什么 / 影响什么 / 下一步”结构。
- 进展（2026-05-26）：ComfyUI 资源页先做一轮降噪：顶部行动项区分“疑似卡住”和“观察中”，线路表直接给出下一步排查动作。
- 进展（2026-05-26）：管理端概览页顶部新增“线上稳定性与封版判断”合并视图，把自检守护、后端连接池日志扫描、业务运行异常和 ComfyUI 队列收敛成四个可点击信号，先判断线上是否稳定，再进入封版阻塞项。
- 验收：非研发用户打开管理端能判断“现在能不能用、哪个环节异常、下一步做什么”。

5. `done` v0.4.1 封版验证
- 目标：稳定性代码、监控门禁和页面降噪完成后再更新 114。
- 要求：后端测试、管理端/测评端类型检查和构建、release smoke、business live patrol、eval production patrol、页面走查和 30-60 分钟观察窗口均记录。
- 进展（2026-05-26）：`cd2b8964` 已发布到 114；release smoke、三条主业务真实巡检、6 个 production 测评工作流巡检和即时观察均通过，记录见 `docs/testing/2026-05-26-v0.4.1-114-seal-validation.md`。
- 进展（2026-05-26）：已安排 30 分钟后复查 health、health-watch、业务运行、ComfyUI 队列、接口调用中心和 backend journal，再给出最终封版结论。
- 进展（2026-05-26）：30 分钟观察复查通过，backend 关键错误日志匹配数为 0，业务运行 failed/running/queued/unresolved 均为 0，ComfyUI blocked/settling 均为 0，接口调用中心近 1 小时无 4xx/5xx。
- 验收：能明确说明 v0.4.1 是否可封版、已覆盖的错误场景、未覆盖风险和回滚方式。

## 已完成执行单（2026-05-19 v0.4 业务编排工作台产品化版）

说明：v0.4 已完成 114 封版，保留为过程记录，不再直接作为开发清单。

0. `done` v0.4 方案与文档入口切换
- 目标：把 v0.4 的目标、范围、非目标、验收标准和当前执行入口写清楚。
- 方案：`docs/strategy/business-orchestration-workbench-v0.4-plan.md`
- 当前动作：总索引、战略索引、版本记录和唯一 TODO 切换到 v0.4；v0.3 保留为已完成阶段记录。
- 已完成（2026-05-19）：v0.4 方案已新增，文档索引和版本记录同步。
- 验收：恢复上下文时能直接看出下一步是“业务工作台产品化”，不是继续扩展 v0.3 的基础能力。

1. `done` 业务命名与版本族整理
- 目标：让业务能力页先按业务入口聚合，再展示版本族、具体版本、继承来源、发布时间、更新时间和更新说明。
- 范围：花纹提取、图裂变、扩图、裂变评分；图裂变优先。
- 要求：Coze 版、自有 ComfyUI 版、GPT Image 2 版、颜色锁定版等不再平铺成用户难以区分的技术名；除“四方连续裂变”等明显新业务分类外，默认按同一业务的版本迭代处理。
- 完成（2026-05-19）：后端业务版本响应新增 `versionFamily` 摘要，管理端版本卡片优先使用后端版本族、父版本、替代版本、归属判断和更新说明，减少前端按技术名二次推断；业务页加载时优先回填业务版本/调用/统计数据，避免等待全部后台接口导致短暂显示“未设置版本”。
- 验收：用户不看技术字段，也能判断当前线上默认版本、测试版本和历史版本的关系。

2. `doing` 编排图交互闭环
- 目标：让编排图从展示图变成业务工作台入口。
- 要求：节点可点击查看输入、输出、路由、最近运行和错误；默认版本只读；草稿版本可编辑受控字段；节点可跳转最近失败 runId 或成功样本。
- 技术路线：继续使用 `@xyflow/react`，不引入新的重型工作流引擎。
- 进展（2026-05-19）：业务链路图增加加载保护，业务版本数据未返回时不再把“加载中”误显示为“缺默认入口”；节点详情补充上下游、业务建议、字段摘要和路由摘要，降低只看底层 JSON 的排查成本。
- 进展（2026-05-22）：业务链路图节点详情接入版本级运行证据，能看到近 24 小时总量、成功、失败、排队/运行，以及最近一次、成功样本、失败样本；后端按 `BusinessRunStep` 归集节点证据，前端可从节点直接打开对应 runId。
- 进展（2026-05-22）：业务链路页接入草稿编辑闭环；线上默认版本保持只读，可一键复制为草稿，草稿版本可在链路页直接编辑步骤和能力绑定，保存走后端草稿配方接口，不影响当前业务默认版本。
- 验收：在图裂变业务里能围绕图完成“看步骤 -> 看参数 -> 复制草稿 -> 修改受控字段 -> 验证”的闭环。

3. `done` 接口调用中心与 runId 排障产品化
- 目标：业务方调用一个接口后，管理端能按 runId 看清整条链路。
- 要求：展示入口请求、业务版本、处理步骤、子能力调用、结果回填、回调或轮询、计费记录；能力调用只作为子步骤证据，不再和业务任务平铺混看。
- 性能要求：列表默认只加载摘要，步骤详情和长请求响应按需展开。
- 完成（2026-05-19）：业务调用清单顶部补充接口调用中心摘要，直接展示当前筛选、排队/执行、失败、入口证据和回调异常；列表增加“接口证据”列，按 runId 显示提交、查询、回调和失败计数，减少必须打开详情才能判断入口是否命中的问题。
- 验收：业务方给一个 runId，可以快速判断是入口、版本、VL、ComfyUI/OpenAI、回填、回调还是计费阶段的问题。

4. `doing` 控制点继续去重
- 目标：减少“一个参数改多处”的问题。
- 范围：业务版本 schema、测评端展示、OpenAPI、交付文档、发布 smoke、前端兜底。
- 要求：字段、枚举、默认值、状态词优先从 `BusinessCapability`、组件目录和错误码总表派生；前端硬编码只允许作为兼容兜底。
- 进展（2026-05-19）：新增 `docs/strategy/control-point-and-file-index-2026-05-19.md`，按问题类型补齐排查入口、真源文件、派生边界和尺寸参数反例；后续修改业务参数必须先按该索引定位真源，再改派生面。
- 进展（2026-05-19）：ComfyUI 颜色锁定裂变的 `variation_preset` 预设配置收敛到 `backend/app/constants/business_api_contract.py`，业务 API 只在颜色锁定版本线上展开缺失参数，测评端从后端 presentation 读取预设，前端硬编码降为兼容兜底。
- 验收：新增或修改一个业务参数时，有明确真源和派生路径，不再靠聊天记录记忆。

5. `doing` 对外接口文档与样例门禁
- 目标：对外业务 API 和 Coze 工具箱接口分清楚，交付材料可直接给业务方阅读。
- 要求：每个接口独立说明请求地址、认证方式、必填参数、可选参数、枚举值、返回字段、状态枚举、错误码和 JSON 样例。
- 进展（2026-05-25）：复核业务接口文档、业务枚举文档和错误码总表，补齐 `canvas_outpaint` 三个画布错误码在业务接口常见错误列表中的入口；`scripts/check_error_catalog.py` 与 `scripts/check_doc_entry_references.py` 通过。
- 验收：发布前检查能发现文档缺少参数说明、状态枚举或错误码的情况。

6. `todo` 页面文案与视觉降噪
- 目标：首页、业务能力页、接口调用页和测评端不再堆叠无意义说明。
- 要求：主文案使用业务语言；技术详情折叠到排障信息；错误主提示给下一步动作；裂变类参数统一使用“重绘幅度”等既定口径。
- 验收：非研发用户能在页面上判断“现在能不能用、当前版本是谁、失败在哪、下一步做什么”。

7. `done` v0.4 封版前回归
- 目标：完成本地门禁、视觉走查、接口真实调用和 smoke 检查后再进入 114 更新。
- 要求：后端测试、管理端/测评端类型检查和构建、业务接口真实调用、Coze 工具箱抽测、测评端核心功能走查都记录结果。
- 进展（2026-05-25）：本轮本地门禁已通过：后端定向回归 112 passed；管理端/测评端 `npm run lint` 与 `npm run build` 均通过。真实 GPT Image 2 低质量样例、Coze 工具箱抽测和页面视觉走查仍未完成，暂不进入 114 更新。
- 进展（2026-05-25）：已按线上版本完成图编辑真实业务 API 与测评端用户动线走查，记录见 `docs/testing/2026-05-25-online-image-edit-ux-walkthrough.md`；功能链路 8/8 成功，交互侧发现并本地修复结果残留、重复提交和管理端内部任务文案噪音，仍需构建后再做上线验证。
- 进展（2026-05-25）：补齐 114 候选发布记录 `docs/testing/2026-05-25-image-edit-release-candidate-114.md`，明确本次只更新控制面，不更新 4090/5090/ComfyUI 能力机；上线后重点复测 `/image-edit` 动线和管理端文案降噪。
- 完成（2026-05-25）：`v0.4.0` 已按线上版本完成封版验证，基线 commit `8033c843`；核心业务真实巡检、图编辑真实样例、158 扩图节点复核、发布 smoke、systemd 真实巡检和管理端主要页面走查均通过。封版记录见 `docs/testing/2026-05-25-v0.4.0-114-seal-validation.md`。
- 验收：能明确说明本版本是否具备上线条件、剩余风险是什么、更新和回滚步骤是什么。

8. `done` 文字强化裂变两步式新能力
- 目标：接入 2026-05-19 ComfyUI 交付包，形成“VL 生成可编辑提示词 -> 用户确认 -> ComfyUI 文生图”的稳定业务接口和测评体验。
- 范围：`/api/business/text-fission/prompts`、`/api/business/text-fission/runs`、`qwen2512_text2img_text_allowed` 工作流、测评端两步交互、OpenAPI 与错误码文档。
- 关键约束：第二步不再二次调用 VL；`editable_prompt` 原样送入文生图节点；单个 run 固定 1 张图；默认负向词不能压制文字、字母和数字。
- 验收：后端契约测试覆盖 OpenAPI、参数映射和缺提示词错误；测评端可先生成提示词、编辑后提交；`width/height` 不填时跟随原图尺寸，用户手动填写时才覆盖。
- 已知质量风险：纯文生图对中文密集图的逐字复刻不稳定；后续需要评估 OCR/版式叠字或质量门禁，不应把该问题误判为中台链路故障。
- 已完成（2026-05-19）：114 已部署 `a47e877b`；线上真实业务测试 `runId=3c4e71a0cff64ac8a62a1a7c6360712f` 验证输入图和输出图均为 `2752x1536`，不再回退到 `1024x1024`；复盘见 `docs/releases/retrospectives/2026-05-19-text-fission-retrospective.md`。
- 已完成补充（2026-05-19）：114 已部署 `7a21e6f0`；修复第二步重复产生 VL 子任务的问题，线上真实业务测试 `runId=557ac9b903b84e8f9a2622aadf48c818` 验证步骤为 `prompt_draft=succeeded/confirmed`、`primary=succeeded`。后续图编辑能力接入前，必须先按文字强化裂变复盘里的功能定性卡和检查表执行。

9. `doing` 图编辑组件型业务能力 v1
- 目标：把 GPT Image 2 图编辑封装成“组件工作台 + 中台业务 API + 原子能力调用”的通用改图业务，内部客户可用源码组件或托管组件接入。
- 范围：`/api/business/image-edit/runs`、`/api/business/image-edit/component-config`、`gpt-image2-editor-v1` 业务版本、测评端“图编辑”组件工作台、管理端业务版本和接口调用链路。
- 关键约束：业务方不直接调用 OpenAI；默认一次一张图；`selectionHints` 是软标注，`maskUrl` 只能是一个最终 Alpha mask；参考图替换和补色校正必须提供参考图；删除修补必须提供标注或 mask。
- 验收：四种模式均有接口契约、错误码、OpenAPI、测评入口和逐功能检查表；后端契约测试覆盖 payload 编译、参考图缺失、非法尺寸；上线前必须真实跑 GPT Image 2 四模式样例并导出原图、参考图、标注/mask、编译提示词、结果图、耗时和成本。
- 进展（2026-05-19）：已完成后端业务层、业务版本 seed、测评端分类与组件入口、管理端分类、OpenAPI、错误码总表、业务枚举和逐功能上线检查表；待补真实模型回归和托管组件视觉走查后再进入 114 更新。
- 进展（2026-05-20）：今日主线固定为图编辑组件型业务能力；已完成测评端图编辑入口的旧链路文案兜底，图编辑不再因线上 seed 未刷新而显示 ComfyUI 执行面或 Coze 追踪口径，统一展示为“中台业务编排 · GPT Image 2 / 中台 runId 追踪”。
- 进展（2026-05-20）：补齐 `/image-edit` 托管组件入口，直接进入图编辑工作台并默认展开接入文档；后续还需真实 GPT Image 2 四模式回归和交付包整理。
- 进展（2026-05-20）：图编辑前端开始按“可交付组件能力”收敛，新增 `podi-eval-web/src/features/image-edit/model.ts`、`ImageEditWorkbench.tsx` 和 `image-edit.css`；测评端入口复用该组件，枚举、尺寸、质量、区域标注序列化、任务摘要和组件样式从组件目录派生，后续源码交付与托管组件使用同一套边界。
- 进展（2026-05-20）：图编辑组件参数口径完成一次对齐，前端质量档位改为中台业务值 `auto/preview/production/premium`，并补充自定义尺寸输入和源码嵌入最小示例。
- 进展（2026-05-25）：收到图编辑扩展画布交付包后，先按“方案审查门禁”完成审查，不直接进入实现；审查记录见 `docs/strategy/image-editor-canvas-outpaint-review-2026-05-25.md`。结论：作为 `image_edit` 单图编辑模式 `canvas_outpaint` 接入，不替代批量扩图，也不新增平行业务入口。
- 进展（2026-05-25）：`canvas_outpaint` 已完成后端编译、测评端交互、OpenAPI、错误码、业务枚举和测试覆盖；支持目标画布/mask 生成、16 倍数取整、原图位置校验、轻量结果后处理贴回原图区域。待真实 GPT Image 2 低质量样例回归后再封版。
- 进展（2026-05-25）：发布巡检脚本补入 `canvas_outpaint` 四类样例（四边、单边、横向、纵向），并新增单测校验巡检 case 清单和扩图 payload，避免封版前只跑旧四类图编辑模式。
- 旁路观察（2026-05-25）：独立 ComfyUI 扩图 `flux2_klein_9b_outpaint` 近 10 小时失败集中在 158/5090，233/4090 同期可跑通。直接对比 `/object_info` 后确认 5090 的 `DrawMaskOnImage` 节点要求必填 `opacity`，而 4090 版本不要求；当前 workflow 节点 104 未写 `opacity`。本问题按 ComfyUI 节点/插件同构处理，暂不把扩图固定到 4090，也不修改中台路由。
- 进展（2026-05-25）：线上真实图编辑巡检已覆盖 8 个样例并全部成功，其中 `canvas_outpaint` 覆盖四边、单边、横向、纵向；测评端手工提交扩展画布成功，关联中台任务 `25ce8e4757a4479fba287e8790432775`。本地同时补测评端提交锁、结果区脏态提示，以及管理端内部/测评任务文案降噪。
- 进展（2026-05-25）：补齐图编辑独立业务交付包 `docs/api/examples/image-edit-business-delivery/`，包含请求、提交返回、轮询 running/succeeded/failed 与错误码；发布门禁 `business_delivery_docs` 从 3 套扩展为 4 套交付包。线上已登记图编辑、GPT Image 2 裂变、ComfyUI 颜色锁定裂变和裂变评分的真实链路验收记录；除图编辑交付包待发版同步外，逐功能审计 blocked 已收口为 warning/doing。
- 进展（2026-05-25）：图编辑 `canvas_outpaint_all_sides` 已作为 `v0.4.0` 封版样例在线上成功，runId `759f0310c01c4eaf9787aaec4ff93f95`；本轮优先推荐业务方使用托管组件 `/image-edit`，源码组件更新方式继续作为后续交付体验优化。
- 当前推进顺序（2026-05-25）：`v0.4.0` 封版后先做 30-60 分钟线上观察；随后继续处理页面文案与视觉降噪、业务方图编辑组件更新方式、业务运行列表/摘要性能风险和第三方能力治理。

10. `done` 当前项目回顾与文档降噪
- 目标：整理当前待办、复盘近期工作方法，删除已经被唯一 TODO 和正式方案替代的旧文档，减少上线文和恢复上下文噪音。
- 完成（2026-05-22）：新增 `docs/strategy/project-current-review-2026-05-22.md`，重写 `docs/README.md` 与 `docs/strategy/README.md`，删除旧 backlog、WIP 草稿和过期文档清理台账。
- 验收：恢复开发时先读当前回顾、唯一 TODO 和 v0.4 方案；旧 backlog/WIP 不再进入当前阅读路径。

## 已完成执行单（2026-05-19 v0.3 业务编排工作台与控制点去重版）

说明：v0.3 已完成本地开发和验证，保留为阶段记录，不再作为当前开发 TODO。

0. `done` v0.3 方案与文档入口切换
- 目标：把 v0.3 的目标、范围、非目标、验收标准和当前执行入口写清楚。
- 方案：`docs/strategy/business-orchestration-workbench-v0.3-plan.md`
- 当前动作：总索引、战略索引和唯一 TODO 切换到 v0.3；v0.2 保留为已完成阶段记录。
- 已完成（2026-05-19）：v0.3 方案、总索引、战略索引、版本记录和文档清理台账已同步。
- 验收：恢复上下文时能直接看出下一步是“编排工作台 + 控制点去重”，不是继续修 v0.2 的假待办。

1. `done` 控制点盘点矩阵
- 目标：找清楚每个业务参数和规则目前散落在哪些位置。
- 范围：花纹提取、图裂变、扩图、裂变评分。
- 输出：按字段列出后端 seed、业务版本、测评入口、OpenAPI、交付文档、前端兜底、发布门禁的保留/派生/删除/兼容结论。
- 已完成（2026-05-19）：输出 `docs/strategy/business-control-point-matrix-2026-05-19.md`，明确 `BusinessCapability` 为运行时真源、seed 为初始化真源、前端映射为展示兜底、交付文档短期静态但必须由 smoke 校验。
- 验收：任一参数要调整时，能先查矩阵判断应该改哪里、哪些地方只能由真源派生。

2. `done` 业务组件目录
- 目标：把业务编排中的 VL、ComfyUI、第三方模型、image-ops、评分、回填、回调、计费、验收抽成平台组件。
- 要求：每类组件定义业务名称、输入、输出、错误、路由、可编辑字段和不可编辑字段。
- 已完成（2026-05-19）：新增后端受控组件目录 `GET /api/admin/business/component-catalog`，覆盖 10 类业务组件，明确默认版本只读、只能草稿编辑、不允许任意代码/HTTP、重执行必须外置。
- 验收：业务链路图节点不再只是后端步骤名，而是有稳定组件类型和用户能理解的说明。

3. `done` 受控编排草稿能力
- 目标：线上默认版本只读，允许复制为草稿并编辑已有步骤的受控字段。
- 技术路线：复用管理端现有 `@xyflow/react`，不引入新的重型编排引擎。
- 已完成（2026-05-19）：新增 `POST /api/admin/business/capabilities/{id}/drafts` 和 `PATCH /api/admin/business/capability-drafts/{draftId}/recipe`，默认版本继续只读；草稿记录来源版本、血缘关系、修改摘要和最近修改历史。
- 验收：复制图裂变当前版本为草稿，修改重绘幅度默认值或 profile，保存草稿但不影响线上默认。

4. `done` 草稿门禁与发布闭环
- 目标：草稿必须经过校验、真实测试、验收登记后才能切默认。
- 要求：未验收版本不能发布；回滚只能切回已验收历史版本；门禁能展示草稿与当前默认的差异。
- 已完成（2026-05-19）：新增草稿发布前校验和发布入口，阻断未真实测试、未验收、编排不可用的草稿；发布成功后草稿自动成为默认版本并保留原默认作为历史版本。
- 验收：发布动作不再靠人工口头确认，页面能显示“为什么可以/不可以切默认”。

5. `done` runId 父子步骤排障视图
- 目标：一次业务调用下面能看到业务步骤和底层能力调用的父子关系。
- 要求：接口提交、轮询、VL、ComfyUI/OpenAI、评分、结果回填、回调、计费都归到同一个 runId 下。
- 已完成（2026-05-19）：业务运行返回 `traceSummary`，按父子链路组织业务入口、处理步骤、结果回填、回调和计费；管理端详情页新增“业务父子链路”卡片，排障优先看 runId 主线。
- 验收：业务方给一个 runId，可以看出失败发生在业务步骤还是底层能力步骤，不再平铺混看。

6. `later` 完整低代码编排器与前端整体重构
- 说明：自由拖拽建图、组件市场、复杂条件分支、完整前端视觉重构放到后续版本；v0.3 只做受控草稿和控制点去重。

## 已完成执行单（2026-05-18 v0.2 业务控制面收敛版）

说明：v0.2 已完成本地开发和验证，保留为阶段记录，不再作为当前开发 TODO。

0. `done` v0.2 方案和文档降噪
- 已完成（2026-05-19）：当前执行单、版本规划、文档索引和过期文档清理台账已收敛为同一入口。

1. `done` 业务配方唯一真源
- 已完成（2026-05-18）：核心业务 seed 增加显式 `versionLine/versionLineage`，后端业务能力响应优先读取业务配方元数据，不再只靠 provider、版本号或名称推断版本族；相关后端测试已补齐。
- 已完成（2026-05-19）：管理端业务链路和版本卡片已按版本族展示，内部技术版本号降为辅助信息。

2. `done` 业务链路工作台收敛
- 已完成（2026-05-19）：管理端业务结论、上线门禁、业务入口概览、业务链路图和版本管理卡片统一改为“业务名 + 版本族 + 角色”展示；内部版本号降为辅助信息，长技术名称不再作为主标题。

3. `done` 接口调用中心产品化
- 已完成（2026-05-19）：接口调用中心的 runId 聚合结果已关联业务任务状态、业务版本和结果数量；页面可直接看到接口是否打进来、任务是否成功、是否有图片/视频/文字结果，再下钻业务详情。

4. `done` 逐功能上线门禁固化
- 已完成（2026-05-19）：后端交付审计接口新增逐功能门禁结果，按 GPT Image 2 裂变、ComfyUI 颜色锁定裂变、裂变评分、旧 Coze 裂变分别检查交付材料、业务版本、真实运行、结果回填和验收门禁；管理端接口页改为读取该动态审计结果，发布 smoke 新增 `per_feature_release_audit` 结构检查。
- 已修正（2026-05-19）：业务版本最近运行摘要补齐文字/结构化结果计数，避免裂变评分这类非图片输出被误判为“成功但无结果”。
- 已验证（2026-05-19）：管理端页面可看到三项新能力“真实运行已通过但缺验收登记”的阻断结论，旧 Coze 链路显示需巡检复核。

5. `done` 测评端当前问题收口
- 已复核（2026-05-19）：源码扫描确认测评端没有继续把裂变参数展示为“相似度”；业务接口类条目名称、角标、发布时间/更新时间、结果组浏览和右侧本图参数面板已按当前口径收敛。
- 已验证（2026-05-19）：`podi-eval-web` 类型检查和构建通过，后端 eval seed / 去重 / 公开目录回归测试通过。

---

## 历史执行单（2026-05-14 稳定化与下一阶段）

说明：本节保留为过程记录，不再直接作为开发清单。

0. `doing` 控制权收敛与业务编排可视化
- 目标：把业务入口、业务版本、编排配方、参数 schema、路由策略、测评展示和交付文档的控制权收敛到明确真源，减少一个问题多处修改。
- 规划：`docs/strategy/business-orchestration-control-plane-v1.md`
- 版本规则：`docs/standards/version-control-rules.md`
- 当时基线：`v0.1.0` / commit `904f9a2a`
- 第一阶段任务：先做只读业务编排图和 runId 全链路排障，不急着做拖拽编辑。
- 第二阶段任务：把业务版本、参数 schema、路由策略、测评入口和文档一致性纳入发布门禁。
- 发布节奏：本轮目标达成后先做本地验证并确认 114 更新步骤，再进入线上更新；未达到候选版本标准前不碰生产控制面。
- 已完成（2026-05-16）：清理顶层旧 Coze、旧部署、旧任务、旧错误码和旧工作流说明，统一归档到 `docs/archive/202605/`，根 README 与 docs 总索引不再指向过时入口。
- 已完成（2026-05-16）：后端业务版本和业务运行详情新增只读 `orchestration_graph`，从同一份 `BusinessCapability.recipe` 和 run steps 生成入口、处理步骤、结果回填节点与连线，为管理端后续编排图打底。
- 已完成（2026-05-16）：管理端业务入口地图、业务版本底层信息和业务 run 详情接入同一份 `orchestrationGraph`，可直接看到入口、VL/生图/评分等处理步骤、当前状态、输出数量、执行节点和排障编号。
- 已完成（2026-05-16）：修正业务运行轻量列表对结构化输出和“已忽略问题”的误判，避免成功记录在列表里被误归因为“结果回填问题”。
- 已完成（2026-05-16）：只读业务编排图节点补齐“参数 / 路由”展开详情，节点可看到能力字段摘要、默认参数、路由策略、执行节点和本步骤输入输出；本地业务页走查确认 15 个编排图、59 个节点、19 个节点可展开详情。
- 已完成（2026-05-17）：发布 smoke 新增 `business_truth_source_consistency` 第一版真源一致性门禁，覆盖业务版本、业务 OpenAPI、测评入口、编排图主能力/路由摘要和业务枚举文档，避免参数和入口改漏后进入封版。
- 已完成（2026-05-17）：业务运行详情补齐入口调用证据，按 `runId/requestId/traceId` 聚合 API Key 使用记录，直接展示提交、查询、失败、最近调用和轮询异常，让单个任务排障不再依赖手动跳转接口调用页筛选。
- 已完成（2026-05-17）：管理端业务能力页把“业务链路图”提升到主视角，按业务入口展示版本、处理组件、执行能力和最近结果；无后端编排图时会用业务配方自动生成入口 -> 处理步骤 -> 结果回填的只读节点图。
- 已完成（2026-05-17）：接口任务清单筛选区从多字段堆叠改为高频筛选 + “更多筛选”折叠，高级条件（来源、业务方、计费、回调、排障编号）不再默认占用主操作区。
- 已完成（2026-05-17）：测评目录里的业务接口类条目改为从 `BusinessCapability` 派生展示名称、版本、参数 schema、输出 schema 和业务角标；测评端兼容 `imageUrl/image_url/url` 主图字段，先收敛“业务版本和测评入口各维护一套参数”的重复控制。
- 已完成（2026-05-17）：测评端业务接入文档的参数说明和返回字段说明改为读取当前 schema，不再硬编码 `bili/quality/size/maskUrl` 等具体参数，减少交付文档与业务版本漂移。
- 已完成（2026-05-17）：业务 OpenAPI 提交 schema 改为“兼容字段 + `BusinessCapability.input_schema` 自动叠加”，新参数会自动进入 `/api/business/openapi.json`，已有兼容字段的说明、枚举和 required 顺序不被降级。
- 已完成（2026-05-17）：三个业务交付包 README 明确引用 `business-api-enums.md` 和 `error-catalog.md`；发布 smoke 的交付材料检查会校验 README、样例、枚举字段和错误码总表一致，不再只看单个交付文档自证。
- 已完成（2026-05-17）：管理端接口页“三个交付接口逐项检查”改为读取后端实时审计结果，后端复用发布 smoke 交付材料规则校验 README、样例、枚举和错误码，页面不再只依赖前端静态清单判断可交付。
- 已完成（2026-05-17）：管理端业务链路图在非 compact 场景接入 React Flow 只读交互画布，支持缩放、拖动画布、节点选中和右侧参数/路由详情；表格内仍保留轻量卡片，编辑能力暂不开放。
- 已完成（2026-05-17）：后端禁止直接修改线上默认业务版本的主能力、配方、业务标识、版本号、输入/输出 schema 或取消默认，统一返回 `BUSINESS_DEFAULT_VERSION_CONTROL_FIELDS_IMMUTABLE`；管理端新增业务版本默认进入草稿，编辑默认版本时提供“复制为草稿配方”。
- 已完成（2026-05-17）：新增管理端专用草稿试运行入口 `/api/admin/business/capabilities/{capability_id}/draft-run`，可按业务版本 ID 生成真实 `runId` 和步骤记录；正式 `/api/business/*` 仍只选择 active 版本，避免业务方误命中草稿。
- 已完成（2026-05-17）：管理端业务版本编辑器新增“草稿编排”最小闭环，支持按步骤编辑 VL/能力组件、上下移动、增删步骤、启停步骤，并直接写回同一份 `BusinessCapability.recipe`；默认版本仍不可编辑，必须先复制为草稿。
- 已完成（2026-05-17）：业务版本卡片的“试运行草稿/跑一次验证”改为页面内弹窗，支持填写原图 URL、评分生成图 URL、提示词和附加 JSON 参数；不再用浏览器 prompt，便于后续沉淀试运行模板。
- 已完成（2026-05-18）：管理端业务能力页按业务主线拆成“业务调用 / 业务链路 / 版本管理 / 治理记录”四个工作区，默认进入业务调用清单；业务调用页固定以 `runId` 为主线，处理步骤和原子能力调用仅作为下钻证据。
- 已完成（2026-05-18）：业务链路页从多业务小画布列表改为“先选业务，再看单业务大画布”的编排工作台；当前可按花纹提取、图裂变、裂变评分、扩图切换，React Flow 画布用于查看组件、参数和路由详情。
- 已完成（2026-05-18）：管理端能力域组件改为按需加载，`IntegrationDashboard` 主产物从约 960KB 降到约 646KB；`npm run build` 已无 Vite 大包体警告，本地自动化验证业务页和能力页均可打开。
- 已完成（2026-05-18）：业务命名和版本归属准则已补充到 `version-control-rules.md`、`business-interface-taxonomy.md` 和 `business-mainline-contract.md`；后续默认把同一业务目标的模型替换、工作流修补、参数默认值调整归为版本升级，只有类似“裂变四方”这种同时具备新业务特性的能力才新建分类。
- 已完成（2026-05-18）：业务能力页调整为“稳定业务名 + 版本族标签 + 同路线关系”的表达方式；业务调用页显示入口概览，业务链路页只保留单业务编排图，版本管理页展示 ComfyUI 自研线、商业模型线、保底回滚等版本族，避免把内部版本号当成功能名。
- 已完成（2026-05-18）：后端业务版本响应新增 `version_line`，管理端优先读取该字段展示版本族，前端推断仅作为旧数据兜底，避免版本路线规则继续散落在多个页面。
- 已完成（2026-05-18）：业务版本响应新增 `version_lineage`，管理端编辑器可记录来源版本、替代版本、更新说明、是否破坏兼容和版本归属判断；版本管理页按版本族分组展示，并在卡片上显示继承关系，避免同一业务的版本迭代被误看成多个新功能。
- 已完成（2026-05-18）：业务版本归属判断补齐后端默认规则，历史数据里 `unknown/待确认` 也会自动归为“同一业务版本升级”或“保底回滚版本”；管理端版本管理页已本地走查，未再出现 `待确认/未命名业务/未定版本`。
- 已完成（2026-05-18）：业务链路图支持在同一业务下切换具体业务版本，默认看线上版本，也能直接查看 ComfyUI、商业模型、Coze 旧链路或保底版本的编排图、主执行能力和版本关系。
- 已完成（2026-05-18）：业务链路图默认选中第一个真实执行组件，而不是信息量较低的“业务入口”节点；右侧详情首屏直接展示主执行能力、参数、路由和排障信息。
- 已完成（2026-05-18）：业务链路图隐藏 React Flow 默认水印框，减少画布噪音；后端业务图 schema 摘要和前端兜底均将裂变类 `bili/similarity` 统一展示为“重绘幅度”，不再在业务链路页出现“相似度”误导。
- 已完成（2026-05-18）：业务版本治理扩展到所有可执行步骤，提前识别步骤能力缺失、未启用、步骤编号重复、主步骤和 `primaryAbilityId` 不一致，减少“页面有编排图但运行时中间节点断链”的问题。
- 已完成（2026-05-18）：业务 API 使用记录补齐内部请求、服务令牌和平台登录三类入口上下文；真实 ComfyUI 裂变巡检 `runId=72372d88e2c34ece852f2081da4763c2` 已验证提交 1 条、轮询 2 条均进入接口调用清单，不再出现 `NO_ENTRY_LOG`。
- 验收：打开任意业务 runId，能按编排图看到 VL、生成、评分、OSS、回调等子步骤；修改一个参数或路由策略时，只有一个真源，其余位置自动读取或由门禁提示漂移。

1. `doing` 当前线上版本固化与每日早检
- 目标：每天开始开发前先确认线上业务、能力、测评和 Key 使用没有新增异常。
- 已完成（2026-05-14）：114 后端、管理端、测评端健康；Coze OpenAPI server 地址正确；ComfyUI 158/233 双节点健康，后端队列汇总识别容量 20、空闲 20。
- 已完成（2026-05-14）：导出 2026-05-13 运营早检包，业务运行 188 条、业务失败 0 条；能力需关注 6 条，测评需关注 1 条。
- 已完成（2026-05-15）：114 更新到 `3b941d08` 后完成发布走查，后端测试 75 条通过，管理端/测评端类型检查与构建通过，远端预检和 smoke 通过；走查报告见 `docs/testing/20260515-business-api-observability-release-walkthrough.md`。
- 已完成（2026-05-15）：近 24 小时业务运行 131 条，成功 129 条，失败 2 条；API Key 调用 3982 次，主要为 `/api/business/runs/get` 轮询，未发现上一轮内部参数未传入问题再次批量发生。
- 已完成（2026-05-16）：近 24 小时图裂变 2 条失败均为 `comfyui-vl-control-v2` 历史失败，后续同版本已有 3 条成功输出覆盖；GPT Image 2 旧白名单失败也已由后续同版本成功覆盖。当前不再按这些旧失败阻塞封版，但保留在调用记录中追溯。
- 已完成（2026-05-16）：业务汇总接口性能优化，本地连接当前数据验证 `/api/admin/business/usage-summary?window_hours=24` 返回约 0.41 秒，不再触发总览页长时间等待；72 小时汇总约 5.36 秒，后续如数据继续增长再做缓存/预聚合。
- 下一步：把早检异常按“真实业务失败 / 上游限流 / 历史 pending 收口 / 巡检参数误报”分类沉淀，避免把巡检误报当成业务事故。
- 验收：当天开发前能拿到一份早检报告，并明确是否阻塞当天开发。

2. `doing` 项目脏内容清理与保留策略
- 目标：清理不会影响运行的本地构建、缓存、旧日志和临时产物；数据库和 OSS 只做审计，不做无备份删除。
- 已完成（2026-05-14）：线上只读清理审计显示数据库需复核分组 0 类、OSS 候选对象 0 个；本地安全可删产物主要是 `dist`、`__pycache__`、旧日志和发布临时目录。
- 下一步：先清理本地可重建产物；数据库/OSS 后续只在有明确引用关系和备份策略后处理。
- 验收：仓库根目录和发布目录不再堆积临时压缩包、无用构建产物和旧日志；不误删交付包、样本包和报告。

3. `done` 三个对外业务接口交付再整理
- 目标：两个图裂变和一个裂变评分接口能作为独立交付材料给业务方，示例、参数、错误、Key、轮询方式都清楚。
- 范围：GPT Image 2 受控裂变、ComfyUI 颜色锁定裂变、裂变生成图评估。
- 要求：每个接口独立 README、请求示例、轮询示例、提交返回、轮询排队中/成功/失败示例、参数表、枚举表、错误码；统一说明 `runId` 查询和旧 Coze `tasks/get` 兼容方式。
- 已完成（2026-05-14）：新增 `docs/api/examples/fission-business-delivery/` 三接口独立交付模板；Key 授权口径明确为生成接口 `fission`、评分接口 `fission_evaluate`。
- 已完成（2026-05-15）：交付模板升级为 v4，不再默认提供 Python 调试脚本；三个接口全部改成独立 JSON 样例，补齐字段含义、枚举值、错误返回和轻量查询返回；两个裂变接口固定一请求一张图，后端忽略 `count/n/batch_size/generateCount/variantCount` 等批量字段。
- 验收：业务开发不需要理解 Coze 或 ComfyUI，即可按文档提交任务、轮询结果、处理错误。

4. `doing` 管理端接口调用和 Key 使用记录产品化
- 目标：中台自有业务 API、原子能力 API、Coze 工具箱 API 三类入口在管理端能清楚区分；业务 API Key 能看到调用记录。
- 下一步：继续把接口示例、鉴权、错误码、最近调用和 Key 使用统计收敛到“接口调用”入口；Key 先不强制限流，但使用记录和后续限流机制必须可见。
- 进展（2026-05-14）：接口页补齐裂变生成图评估接口、GPT Image 2 v2/ComfyUI 颜色锁定 v2 示例、业务 Key 摘要和失败记录提示；默认 Key 范围补齐 `fission_evaluate`。
- 进展（2026-05-15）：线上接口页可见“中台自有业务 API / 原子能力 API / Coze 工具箱”、API Key 列表、最近调用记录和 API 清单；但调用记录仍被轮询淹没，缺少筛选、分页、导出、按 `runId` 聚合和业务运行详情跳转。
- 进展（2026-05-15）：接口调用中心第一版已完成代码实现，后端 `/api/admin/business/api-key-usage` 支持时间窗口、API Key、业务、接口动作、状态、路径、错误码、`runId/requestId/traceId` 筛选，返回分页、汇总统计和按 `runId` 聚合；管理端接口页已展示筛选区、提交/轮询/异常/关联任务指标、按任务聚合表和调用明细分页。
- 走查记录（2026-05-15）：本地视觉走查发现默认 `apiKeyId=all` 被误传给后端导致列表为空，已修复；该问题说明后续类似页面必须做浏览器级走查，不能只看类型检查。
- 进展（2026-05-15）：接口调用中心补齐 CSV 导出、按 `runId` 聚合的异常提示（存在错误、只有查询无提交、轮询过频）和页面内打开业务运行详情；后端新增 `/api/admin/business/api-key-usage/export`，文档同步记录查询参数和导出列。
- 进展（2026-05-15）：业务动线标准已新增 `docs/standards/business-mainline-contract.md`，明确接口调用清单以 `runId` 为主线，VL、ComfyUI、OpenAI、评分、回填、回调、计费全部作为处理步骤或证据下钻。
- 进展（2026-05-15）：业务能力页已把接口任务清单调整为一次业务调用视角，列表直接显示 `runId`，详情页先展示“这次业务调用”，再展示处理步骤和底层排障数据。
- 进展（2026-05-15）：业务详情支持 `#nav=business&businessRunId=...` 定位，接口调用中心打开任务后刷新页面仍能回到同一条记录；页面主文案统一为“这次业务调用/处理步骤/业务处理顺序”。
- 进展（2026-05-15）：`podi_release_smoke.py` 新增 `business_api_usage_center` 检查，发版后会验证接口调用中心可访问、分页结构正常，并且有业务任务时能按 `runId` 聚合。
- 进展（2026-05-15）：业务能力页“主业务版本地图”升级为“业务入口和处理地图”，固定按“业务入口 / 当前版本 / 处理步骤 / 最近结果”展示，并纳入裂变评分入口；本地页面走查确认“业务处理顺序、业务入口和处理地图、接口任务清单、裂变评分、四段式步骤”均可见。
- 进展（2026-05-15）：接口调用页新增“三个交付接口逐项检查”，按 GPT Image 2 受控裂变、ComfyUI 颜色锁定裂变、裂变生成图评估分别显示文档路径、6 类 JSON 样例、枚举和错误码覆盖情况；同时把裂变评分独立成业务分类，不再混在图裂变卡片里。
- 进展（2026-05-16）：业务入口总控改用 `unresolvedByBusiness` 判断当前风险，不再用原始失败数误报；图裂变历史失败显示为“历史失败”，后续同版本成功覆盖后仍可接入；裂变评分成功样本可见但缺验收记录时显示“待验收”，不再误标为技术阻塞。
- 进展（2026-05-16）：管理端视觉走查通过本地候选环境验证，总览移除“运营驾驶舱/今日处理顺序”等说明式大块内容，业务页移除“业务处理顺序/runId 主线说明”大卡片，页面主入口收敛为业务入口总控和接口任务清单。
- 进展（2026-05-17）：业务运行详情已嵌入接口调用证据卡，复用接口调用中心的使用记录口径，支持按 `runId/requestId/traceId` 识别提交、查询、回调、失败、只有查询无提交和轮询过频。
- 进展（2026-05-17）：接口调用页“三个交付接口逐项检查”补齐页面级缺口提示和检查依据，能直接看到缺哪个 JSON 样例、枚举字段或错误码，并固定关联 `business-api-enums.md` 与 `error-catalog.md`。
- 进展（2026-05-18）：接口调用页默认前置“业务接口运行速览”，把提交、轮询、异常、关联任务和问题 `runId` 作为首屏指标，并展示最近 5 个 runId；完整调用中心保留筛选、导出和明细分页，Key 创建、停用和范围维护折叠为低频管理区。
- 进展（2026-05-18）：修复非 API Key 入口缺少使用记录的问题；内部巡检、服务令牌和平台登录调用现在也会写入 `BusinessApiKeyUsageLog`，管理端可以按 `runId/requestId/traceId` 看到提交与轮询证据。
- 进展（2026-05-22）：接口调用页新增“业务方反馈编号”快速定位区，支持粘贴 `runId/requestId/traceId` 后取消时间窗口直接查询；新增“当前优先排查”卡和排障摘要复制，便于把业务、版本、状态、提交/轮询/回调/异常次数、结果和下一步提示一次性发给业务或排障同学。
- 下一步：继续让接口页和业务交付包引用同一份枚举真源，并把接口调用中心的筛选、聚合、导出纳入发布后视觉走查。
- 验收：用户能在页面上判断“业务方调用了哪个接口、参数是否正确、轮询是否异常、失败码是什么、对应哪条业务运行”。

5. `doing` 对外业务 API 枚举与契约固化
- 目标：业务方接口的任务状态、返回字段、参数枚举、错误码、计费状态、回调状态都有统一文档和页面口径。
- 已完成（2026-05-15）：新增 `docs/standards/business-api-enums.md`，先固化 `/api/business/*` 对外任务状态、查询返回字段、图裂变参数、裂变评分结果、路由预览、计费/回调状态和常见错误码。
- 进展（2026-05-15）：业务 OpenAPI 已补 `profile/mode/variation_preset/pattern_risk_type/output_format/selectedStatus/selectedBy` 显式枚举；接口调用中心枚举写入 `docs/standards/business-api-enums.md`。
- 进展（2026-05-17）：`business_truth_source_consistency` 已把业务枚举文档纳入发布 smoke，先阻断状态、核心业务版本、参数枚举和常见错误码缺失。
- 进展（2026-05-17）：业务运行详情入口调用证据统一复用接口调用中心枚举，轮询过频固定为 `POLLING_TOO_FREQUENT`，并纳入 smoke 枚举文档检查，避免页面与文档漂移。
- 进展（2026-05-18）：业务 API 枚举抽为后端结构化真源 `backend/app/constants/business_api_contract.py`；业务 OpenAPI、交付审计接口、发布 smoke 和管理端接口页已接入同一份枚举，前端静态枚举降级为离线兜底。
- 下一步：让接口页和业务交付包继续减少重复文案，逐步由同一份枚举真源生成页面提示。
- 验收：业务方不需要问中台，也能知道所有状态值、参数可选值和错误处理方式。

6. `doing` 测评端前端继续产品化
- 目标：测评端按业务分类和版本迭代展示，不把内部代号当主名称；结果对比区优先展示图片，错误详情默认收起。
- 下一步：继续优化图裂变、多图测试、结果并排/滑块/大图弹层和历史记录分页；新增或更新版本必须保留原功能名，版本变化用角标和更新时间表达。
- 进展（2026-05-14）：修正业务接入文档生成口径，GPT Image 2 裂变示例统一为 `gpt-image2-vl-v2`、`same_series`、`preview`，不再展示 `count`；表单层把 `bili/similarity` 统一展示为“重绘幅度(%)”，避免再次把裂变幅度写成相似度。
- 进展（2026-05-14）：ComfyUI 颜色锁定裂变按交付包 `15_2026-05-14_comfyui_fission_object_variation_interface_pack_v4` 做原版本修补，不新增测评端功能卡；默认切到 `pattern_risk_routed_v4`，`bili` 不再做硬区间拦截，测评端增加“高幅度默认 / 保守稳定 / 对象变化更强 / 配色更自由”预设，文档和示例同步为 `reference_lock`、`color_lock` 可选控制项。
- 进展（2026-05-17）：测评端结果工作台继续按 KB 项目口径收敛，默认滑块且滑块位置 100%；左侧明确为结果组列表，排队或无结果组显示“等待结果”并保留可切换；右侧改为“本组信息 / 本图参数”。功能摘要中 `similarity` 也统一显示为“重绘幅度”，避免裂变页面再次出现“相似度”误导。
- 进展（2026-05-18）：测评端再次补齐前端兜底，表单字段、业务接口说明、历史记录参数预览和沉浸式大图参数面板都会把 `bili/similarity/相似度` 统一展示为“重绘幅度”；图裂变目录和文字增强裂变入口已本地浏览器走查，不再出现“相似度”文案。
- 验收：测试同学不需要理解 workflow id、内部版本号或长错误栈，也能完成上传、生成、对比、评分和复制排障信息。

7. `later` 账单/套餐/商业化深水区
- 当前只保留运营骨架、成本记录、Key 使用记录和未来扣费口径。
- 真实支付、自动开票、商业套餐和强限流策略后置，不阻塞当前中台主链路。

---

## P0（先做，2-3 周）

0. `done` 2026-04-27 事故整改优先包（已转为发版守护项）
- 背景：Coze 工具箱因 `INTERNAL_ONLY` 不可用，服务健康但业务链路停摆；同时暴露出 ComfyUI 队列利用率不可见、工作流目录难分辨、ComfyUI 成功但无图回填等问题。
- 产物：
  - 事故复盘：`docs/retrospectives/2026-04-27-coze-toolbox-internal-only-incident.md`
  - 发布后 smoke 脚本：`backend/scripts/podi_release_smoke.py`
  - 全量测评巡检脚本：`backend/scripts/patrol_eval_workflows.py`
  - ComfyUI 队列压测脚本：`backend/scripts/comfyui_capacity_probe.py`
  - 发布前门禁升级：`docs/release-preflight.md`
- 验收：
  - 每次发版后能跑完整 active 工作流巡检，并输出成功/失败/中台任务 ID/Coze 执行 ID。
  - 能验证 ComfyUI 单机 10、双机 20 的实际队列承载，不再只看理论配置。
  - ComfyUI `success` 但无图时不能长期 `running`，必须明确失败并给出 `COMFYUI_IMAGES_EMPTY`。
  - 任何 `INTERNAL_ONLY`、工具箱导入失败、主工作流失败都必须阻断发版。
- 进展（2026-04-27）：已修复 Coze 同机调用、ComfyUI 空输出无限 running、背景抠图缓存空 history；114 上 smoke 通过，背景抠图 4 任务分流到两台 ComfyUI 且全部成功。
- 进展（2026-04-27）：评测端首页已接入链路健康卡片、失败原因汇总、最近失败样本、ComfyUI 队列汇总；`check_eval_operations_health.py` 已纳入 ComfyUI 执行节点健康，能识别 `COMFYUI_EXECUTOR_UNREACHABLE` / `COMFYUI_NO_AVAILABLE_EXECUTOR` 并进入发版门禁。
- 进展（2026-04-28）：ComfyUI 队列汇总已回写执行节点健康状态；测评端工作流接口开始输出 `presentation/usage/deprecation/governance` 真源，卡片可区分“生产主入口 / 灰度对照 / 历史保留 / 辅助工具”，并按主入口优先排序。
- 进展（2026-04-28）：工作流目录角色已接入管理端编辑入口，可在页面上维护“生产主入口 / 灰度对照 / 历史保留 / 辅助工具”、排序和业务说明；后端统一保存到工作流元数据，不再依赖改代码。
- 进展（2026-04-28）：公开评测端可见性已绑定目录角色，仅展示“生产主入口”和“灰度/对照版本”；历史保留、辅助工具、已停用默认只保留在管理/内部视角。
- 进展（2026-04-28）：发布 smoke 已增加公开目录治理检查，能阻断空目录、缺少生产主入口、以及历史/辅助/停用角色误出现在公开评测端。
- 进展（2026-04-28）：目录角色默认策略收紧，只有主入口清单或人工指定才是“生产主入口”；其他 active 工作流默认降为“灰度/对照版本”，避免主入口标签泛滥。
- 进展（2026-04-28）：发布 smoke 增加“单分类生产主入口数量”门禁，默认每个业务分类最多 2 个生产主入口，防止主入口标签再次泛滥。
- 进展（2026-04-28）：公开评测目录按 workflow_id 全局去重，并在发布 smoke 中阻断重复 workflow_id，避免同一功能在公开目录重复出现。
- 进展（2026-04-28）：评测运行健康检查补齐“最近无评测业务”和“最近无成功样本”信号，避免服务存活但业务链路长期没有真实成功记录。
- 进展（2026-04-28）：评测运行健康检查补齐并发快照，直接展示评测总并发、ComfyUI 评测并发、单任务裂变并发、ComfyUI 队列容量和当前队列，避免只凭页面列表猜测 GPU 是否吃满。
- 进展（2026-04-28）：ComfyUI 容量探测脚本补齐峰值队列、峰值运行/排队、每台执行节点峰值和最低验收阈值，压测不达标会直接失败。
- 进展（2026-04-28）：全量测评巡检补齐结果判定，`succeeded` 但没有图片/结构化结果会以 `EVAL_SUCCEEDED_WITHOUT_OUTPUT` 阻断，同时报告 `INTERNAL_ONLY`、`COZE_WORKFLOW_ERROR` 等事故分类。
- 进展（2026-04-28）：补充 `podi-eval-health-watch` systemd 定时任务模板，可在 114 上每 15 分钟自动检查评测链路健康；暂未自动启用，避免未经确认改变线上巡检频率。
- 进展（2026-04-28）：补充健康巡检一键安装脚本 `scripts/install_eval_health_watch.sh`，启用前会先运行预检，critical 默认阻断安装。
- 进展（2026-04-28）：健康巡检 systemd 服务将 warning 退出码 1 视为已完成，只有 critical 退出码 2 才标记失败，避免已知余额/部分线路问题造成误判。
- 进展（2026-04-29）：114 发版演练暴露出生产 Alembic 版本与 `origin/main` 漂移、macOS 打包混入 `._*` 两类发布风险；已新增 `scripts/release_source_preflight.sh`，发布源检查覆盖 `origin/main` 对齐、工作区干净、Alembic 单 head、AppleDouble 文件扫描，并补充生产库当前 revision 检查方式。
- 进展（2026-04-29）：管理端“发布前门禁 / 完整巡检确认 / 战略指标快照 / 周报记录”后端接口已补齐，先用本地 JSON/Markdown 记录，避免为轻量治理功能新增数据库迁移；已补接口回归测试。
- 进展（2026-04-30）：管理端总览页补齐“上线结论”卡片，把业务入口、轻量门禁、完整巡检、模型与能力风险汇总成一个可读结论；上线前不再只看零散页面，必须在总览先确认是否“可以上线 / 待验收确认 / 暂不能上线”。
- 进展（2026-04-30）：管理端补齐“上线结论登记”，可记录可上线、暂缓或阻塞结论，并关联最近轻量门禁与完整巡检；本地已验证登记暂缓闭环，避免上线判断只停留在页面临时状态。
- 进展（2026-04-30）：完整巡检报告导入会归一化每条工作流的健康证据，自动生成 `failedItems`、`abilityHealthEvidence`、无回填统计和问题分类；管理端总览页可直接看到最近巡检中哪些能力通过、失败或成功但无回填。
- 进展（2026-04-30）：补充 Coze 容器级工具箱 smoke 脚本 `scripts/smoke_coze_container_backend.sh`，上线后直接从 `coze-server` 容器访问 backend OpenAPI 与 `tasks/get`；已在 114 上验证 OpenAPI `200`、`tasks/get` `404 TASK_NOT_FOUND`，比单纯本机 curl 更贴近真实工作流链路。
- 进展（2026-04-30）：维护后的 ComfyUI 服务器已复核，`117.50.216.233:8079` 与 `117.50.80.158:8079` 的 `/system_stats`、`/queue`、`/object_info` 均可访问，队列为空；当前结论是节点连通和资源查询正常，后续发版前仍需跑真实工作流生成测试。
- 进展（2026-05-02）：114 线上 Coze 业务自检确认 `podi_release_smoke.py` 通过，`INTERNAL_ONLY` 未复发；但一次性巡检 22 条 active 工作流会触发 `COMFYUI_QUEUE_FULL(limit=10, current=10)` 并造成部分评测记录长期 running。已将 `patrol_eval_workflows.py` 改为默认只跑 `production` 主入口且 `--max-in-flight 1` 限流，全量覆盖需显式 `--role all`。
- 进展（2026-05-03）：新增 114 业务链路定时自检：`podi-business-health-watch.timer` 每 15 分钟做轻量检查，`podi-business-live-patrol.timer` 每天单并发跑花纹提取、图裂变、扩图和 production 测评工作流，避免服务存活但核心业务长期不可用。
- 进展（2026-05-03）：按业务流程复核花纹提取 / 图裂变 / 扩图三条主链路，确认两台 ComfyUI 路由已修复；新增体检文档 `docs/strategy/core-business-chain-review-2026-05-03.md`。本轮发现花纹提取已在业务版本库中但缺公开业务 API，已补齐 `/api/business/pattern-extract/runs` 与 route-preview，并纳入 OpenAPI、文档和契约测试。
- 进展（2026-05-03）：管理端总览新增“线上自检守护”状态卡片，直接读取 114 上业务轻量自检、业务真实巡检、评测健康定时器和最近执行结果；后端接口固定白名单读取 systemd，不接受前端传入命令，避免再次出现“定时任务装了但页面不可见”的盲区。
- 进展（2026-05-03）：测评端全量巡检补齐非图片输出判定，`patrol_eval_workflows.py` 会识别视频、文字/VL 和结构化结果，避免后续生视频或文字能力被误判为“成功但无输出”。
- 进展（2026-05-04）：管理端“任务衔接诊断”补齐 active ComfyUI 工作流兼容性检查，发版 smoke 默认阻断缺节点、缺模型和路由绑定 warning；内置 workflow binding seed 已补齐 158/233 双机配置。
- 进展（2026-05-05）：发版 smoke 新增三大主业务默认版本治理门禁，花纹提取、图裂变、扩图必须有 active 默认版本、主能力、可执行配方，且默认不允许底层阻塞或治理 warning 带入上线。
- 进展（2026-05-05）：管理端“发布前门禁”同步补齐业务能力治理与账号权限上线检查，总览页新增“核心上线门禁”卡片，避免命令行 smoke 与页面上线结论口径不一致。
- 进展（2026-05-05）：业务版本新增人工验收记录，管理端可在版本卡片直接记录“验收通过”，后端保留最近验收结论、证据链接和勾选项，切默认/回滚前不再只凭口头确认。
- 进展（2026-05-06）：业务版本新增 `releaseGate` 上线结论，默认切换申请和直接设默认必须先有“验收通过”记录；发版 smoke 也会阻断核心默认版本未验收，管理端卡片直接显示“暂不能上线 / 需复核 / 可上线”。
- 外部阻塞（2026-05-04）：KIE 商业模型余额不足导致 2 条图裂变失败，暂不阻塞中台结构开发；余额或 Key 恢复后重新巡检即可。

1. `doing` 战略指标面板落地
- 产物：北极星与 5 个 KPI 的数据定义、口径说明、查询来源
- 验收：每周可自动出一次指标快照
- 进展（2026-04-25）：业务调用、vendor-api 调用、能力健康、业务版本调用统计已具备数据基础；还缺统一战略指标面板与每周快照导出。
- 进展（2026-04-30）：补齐战略指标快照脚本 `backend/scripts/create_strategy_snapshot.py`、周报 systemd 模板 `podi-strategy-weekly-report.*` 与安装脚本 `scripts/install_strategy_weekly_report.sh`；114 后续可一键安装每周一 09:30 自动生成战略周报。

2. `done` 用户分层与主路径图
- 产物：三类用户 Top3 路径图
- 验收：评审通过后作为 IA 设计输入
- 说明（2026-04-25）：当前先按“业务方 / 管理员 / 开发接入方”三类推进；正式主路径图仍需补文档和页面验证。
- 进展（2026-04-30）：已新增 `docs/strategy/user-segments-main-paths-2026q2.md`，明确业务方、平台管理员、开发接入方三类用户的主路径、页面权限边界和前端整改验收口径。

3. `doing` 信息架构 V1
- 产物：一级导航、二级页面、主工作区定义
- 验收：侧边栏与页面框架统一
- 进展（2026-03-04）：已完成 strategy 文档骨架与管理端响应式第一轮改造；ComfyUI 管理已落地“分类 + 模块”二级导航，并完成“同步发布”三步面板、单主 CTA、次级面板折叠、失败后建议文案；已补超窄屏自适配与状态列命名统一，下一步做 IA 评审与模块拆分图
- 进展（2026-03-05）：已完成“能力详情/测试并入能力目录”迭代1：能力目录内新增统一工作区，旧 `ability-tests` 入口保留兼容提示，路由可回退不影响联调。
- 进展（2026-03-05）：已完成“能力详情/测试并入能力目录”迭代2：旧侧栏入口下线，保留 `#nav=ability-tests` 兼容跳转到能力目录测试 Tab，并同步迁移文档说明。
- 进展（2026-04-25）：管理端已新增“业务能力”“模型弹药库”“能力健康/调用统计”等视角，正在从底层执行器配置页转向业务可理解的管理入口；下一步要继续降低概念密度。
- 进展（2026-04-28）：测评端目录治理先落一版，工作流卡片展示业务角色和版本变体，减少同名卡片辨识成本；下一步把相同口径同步到管理端工作流维护页面。
- 进展（2026-04-30）：管理端新增模块操作指南真源 `moduleGuides`，每个模块顶部直接显示“适合谁、先看什么、下一步做什么、风险提示”；顶部状态从节点/工作流等技术指标改为主业务、能力异常、模型风险、运行线路，减少非技术用户理解成本。
- 进展（2026-04-30）：管理端窄屏/收起侧栏改为固定短标签按钮（如 OV/BZ/MD/CF），避免只显示空白图标卡片；本地浏览器已走查业务能力、模型弹药库、ComfyUI 资源、能力目录、调度监控、账号权限入口。
- 进展（2026-04-30）：管理端前端整理 Phase 1 已启动，方案见 `docs/plans/2026-04-30-admin-frontend-cleanup-phase1.md`；`IntegrationDashboard` 非首屏模块和 ComfyUI 子面板改为按需加载，主业务块从约 `544 kB` 降到约 `326 kB`。
- 进展（2026-04-30）：管理端懒加载面板声明已抽离到 `lazyPanels.tsx`，主页面继续向“页面编排 + 分域状态容器”方向收口。
- 进展（2026-04-30）：管理端导航解析、页签、默认表单和分页/定价基础配置已抽离到 `integrationDashboardConfig.ts`；下一步开始拆业务能力或 ComfyUI 状态域。
- 进展（2026-04-30）：管理端业务能力页的灰度读取、视觉辅助读取、业务/版本筛选、版本对比派生状态、编辑表单映射和保存载荷构建已抽离到 `businessDashboardState.ts`；下一步继续拆业务能力请求动作或 ComfyUI 状态域。
- 进展（2026-04-30）：管理端业务能力请求动作已抽离到 `businessDashboardActions.ts`，默认版本申请/审批、启停、对比、回滚、运行记录刷新、导出和回调重试不再散落在主页面；下一步转向 ComfyUI 状态域拆分。
- 进展（2026-04-30）：管理端 ComfyUI 页签配置、分区文案、同步发布步骤、轻 Agent 动作文案和可见节点/任务统计派生状态已抽离到 `comfyuiDashboardConfig.ts` 与 `comfyuiDashboardState.ts`；下一步继续拆 ComfyUI 请求动作或模型弹药库状态。
- 进展（2026-05-01）：管理端 ComfyUI 模型、版本、插件资源目录请求动作已抽离到 `comfyuiResourceCatalogActions.ts`，资源清单刷新/保存/删除/版本同步不再散落在主页面；下一步继续拆 ComfyUI 服务器、轻 Agent、任务请求动作。
- 进展（2026-05-01）：管理端 ComfyUI 任务下发、任务推送、任务事件、运行监控汇总与队列汇总动作已抽离到 `comfyuiTaskActions.ts`；下一步继续拆 ComfyUI 服务器、轻 Agent 与清单发布动作。
- 进展（2026-05-01）：管理端 ComfyUI 轻 Agent 注册、保存、删除、主节点设置和令牌签发动作已抽离到 `comfyuiAgentActions.ts`；下一步继续拆 ComfyUI 清单发布、服务器纳管和桌面接入动作。
- 进展（2026-05-01）：管理端 ComfyUI 清单发布、回滚、差异检测和修复任务动作已抽离到 `comfyuiManifestActions.ts`；下一步继续拆 ComfyUI 服务器纳管和桌面端接入动作。
- 进展（2026-05-01）：管理端 ComfyUI 桌面端注册码和安装包版本动作已抽离到 `comfyuiDesktopActions.ts`；下一步继续拆 ComfyUI 服务器纳管、告警和差异日志动作。
- 进展（2026-05-01）：管理端 ComfyUI 服务器刷新、新增、告警、差异日志和对齐快照保存动作已抽离到 `comfyuiServerActions.ts`；下一步拆 LoRA/模板动作或转入模型弹药库状态域。
- 进展（2026-05-01）：管理端 ComfyUI LoRA 清单刷新、表单重置、保存和删除动作已抽离到 `comfyuiLoraActions.ts`；下一步谨慎拆模板编辑动作，或转入模型弹药库状态域。
- 进展（2026-05-01）：管理端 ComfyUI 工作流模板输入/输出映射、节点参数写回、导入、克隆和保存动作已抽离到 `workflowTemplateActions.ts`；ComfyUI 管理域第一阶段动作拆分基本完成。
- 进展（2026-05-01）：管理端模型弹药库加载、火山同步、出网检查、模型保存和密钥保存动作已抽离到 `vendorModelActions.ts`；下一步处理权限/账单动作拆分或阶段性页面走查。
- 进展（2026-05-01）：管理端账号权限的用户、会话、邀请码刷新、调整、失效和踢出动作已抽离到 `authActions.ts`；下一步处理账单动作拆分或阶段性页面走查。
- 进展（2026-05-01）：管理端账单框架的数据刷新、导出、套餐、订单、发票、月结、通知和扣费修复动作已抽离到 `billingActions.ts`；管理端第一阶段动作拆分可以转入页面走查。
- 进展（2026-05-03）：能力调用日志补齐 `output_summary`，管理端可稳定区分图片、视频、文字和普通资源；后续接入生视频、VL 分析、文字能力时，不再只按图片预览链路展示。
- 进展（2026-05-03）：业务调用详情新增“业务链路判定”，按版本选择、能力下发、执行节点、输出回填、业务回调五段展示红黄绿状态，减少排查时在日志字段里来回翻找。
- 进展（2026-05-03）：模型弹药库新增“适合场景 / 上线前判断”，按图片、视频、文字/多模态、图像理解区分模型，并直接提示缺计价、需要出网、未启用等风险，减少用户只看到技术字段却不知道能否上线的问题。
- 进展（2026-05-03）：业务能力页新增“核心业务入口总览”，固定展示花纹提取、图裂变、扩图的默认版本、启用数量、最近结果、发布时间和下一步建议，避免用户先进入版本列表再自行判断主链路是否完整。

4. `done` 状态与错误口径统一落地检查（转发版守护）
- 产物：状态映射核对表 + 缺口清单
- 验收：不存在“上游失败标成功”
- 进展（2026-03-04）：已新增 `docs/strategy/status-error-audit-2026q2.md`，完成第一版核对矩阵与缺口优先级；已补 `docs/testing/COMFYUI_TASK_STATE_REGRESSION_PLAN.md` 与错误映射在管理端/评测端的首轮落地。
- 进展（2026-03-05）：已新增 `backend/tests/test_eval_review_progress_contract.py`，补齐批量评测标注进度的关键契约单测（页码归一、默认值、403/404 错误码）。
- 进展（2026-03-05）：已补齐错误码总表缺口（`ABILITY_TASK_FAILED/CANCELLED`、`RUN_CREATE_FAILED`、`KIE_ABILITY_NOT_CONFIGURED`、`KIE_TASK_FAILED`），并通过 `scripts/check_error_catalog.py` 校验。
- 进展（2026-03-05）：已产出状态/错误回归报告 `docs/testing/STATUS_ERROR_REGRESSION_REPORT_2026-03-05.md`，明确已覆盖项与剩余线上风险。
- 进展（2026-03-05）：已补 API 层契约回归 `backend/tests/test_eval_review_api_contract.py`，覆盖 `review-groups/review-progress` 的 400/409 关键错误场景与分页归一逻辑。
- 进展（2026-03-05）：已新增线上冒烟清单 `docs/testing/STATUS_ERROR_ONLINE_SMOKE_CHECKLIST.md`，用于发布后快速核对状态/错误语义一致性。
- 进展（2026-03-05）：已新增 `scripts/status_error_regression.sh`，并接入 `scripts/deploy_preflight.sh`（可通过 `RUN_STATUS_ERROR_CHECKS=1` 打开专项回归）。
- 进展（2026-03-05）：已补“失败兜底错误码”映射（`ABILITY_TASK_FAILED/ABILITY_TASK_CANCELLED/CALLBACK_FAILED`）与 Coze 查询接口 snake_case 兼容字段，降低联调歧义。

5. `doing` 文档治理上线
- 产物：归档目录、文档 owner、每周回顾机制
- 验收：新增任务全部进入本待办池
- 进展（2026-03-04）：已新增 owner 清单（`docs/strategy/doc-governance-owners-2026q2.md`）与每周回顾模板（`docs/strategy/weekly-review-template.md`）。
- 进展（2026-04-25）：`docs/README.md`、`docs/architecture.md`、`docs/api/modules/business.md`、`docs/plans/2026-04-24-vendor-api-ops-mvp.md` 已同步当前中台/Coze/执行面边界；仍需持续清理旧待办入口。
- 进展（2026-04-30）：已完成一轮文档入口去脏、凭证文档去敏、历史客户端/交接资料口径校正；发布源预检新增入口文档本地路径检查，回归记录见 `docs/testing/RELEASE_REGRESSION_REPORT_2026-04-30.md`。

## P1（商用骨架，3-5 周）

1. `doing` 统一认证与角色模型第二阶段
- 注册登录、基础角色、会话策略
- 下一步：第一阶段账号/会话/邀请码已完成，继续做上线迁移验证、角色边界、业务方隔离和对外 API 鉴权口径。

2. `doing` 充值与账单 MVP
- 余额、充值、扣费明细、账单导出
- 进展（2026-03-04）：钱包余额、支出流水、使用量统计、充值订单、账单和成本快照已有基础接口；支付网关和面向用户的完整账单体验延期。
- 当前缺口（2026-04-25）：缺租户/用户体系承接，缺管理端账单页，缺发布前回归报告。
- 进展（2026-05-04）：管理端账单页后端闭环补齐，新增总览、用户详情、套餐发放、套餐订单、月结出账、发票申请、通知记录和流水导出；当前定位为运营骨架，真实支付网关/自动开票/外部通知推送保留到后续商业化阶段。
- 进展（2026-05-04）：业务任务计费补齐“套餐优先、钱包兜底”扣减链路，成功且可计费任务会自动写入统一 `billingSettlement`，退款会按原扣减方式回补套餐或钱包。

3. `doing` 成本核算规则
- 按模型、任务、输出类型计算成本并落库
- 进展（2026-04-25）：vendor-api-ops 与业务运行记录已预留并回填耗时、成本、额度字段。
- 进展（2026-05-04）：套餐消耗和钱包扣费已统一到业务任务结算口径；模型目录 `costPolicy` 和能力元数据 `pricing/costPolicy` 已能回填业务任务成本，减少“成功但未定价”的空洞。
- 进展（2026-05-04）：套餐定价规则新增“套餐目录”后端、迁移和管理端入口；订单/人工发放只传套餐标识时可自动套用名称、业务、额度、金额和有效期。
- 进展（2026-05-04）：模型目录计价规则增加后端校验和管理端中文表单，支持维护计费单位、单价、币种、套餐消耗、数量字段和定价版本。
- 进展（2026-05-04）：商业化报表雏形已补齐，按账期汇总套餐订单收入、任务成本、套餐消耗、扣费完成度和计费风险，并在管理端账单页直接给出下一步处理建议。
- 进展（2026-05-04）：发布 smoke 新增商业化报表门禁，默认阻断未定价成功任务和计费异常，避免计费问题在发版前被忽略。
- 进展（2026-05-04）：商业化报表补齐 CSV 导出，导出内容按摘要、业务明细、风险任务三段组织，便于运营和财务留档复核。
- 进展（2026-05-07）：计费门禁区分真实业务风险和内部巡检样本；`business-api-patrol`/`podi-internal-patrol`/`metadata.patrol=true` 的成功任务归入“免计费”，仍记录成本但不再污染商业化阻断项，真实成功未扣费继续阻断。
- 下一步：继续收敛账单页文案；真实支付/开票网关继续后置。

4. `doing` 关键页面 UX 重构
- 管理端、测评端、后续客户端的主路径和页面体验重构
- 说明：前端整体整改已从管理端开始；第一阶段只做结构、加载边界和主路径文档，不改业务接口。
- 顺序：管理端优先，其次测评端，客户端暂不进入本轮开发。
- 第一阶段范围：信息架构、页面分组、用户主路径、文案口径、组件拆分策略、Vite 大包体治理方案。
- 进展（2026-04-30）：已新增 `docs/plans/2026-04-30-admin-frontend-cleanup-phase1.md`，并完成管理端非首屏面板按需加载第一轮。

5. `doing` 文案系统 V1
- 错误文案、空状态文案、操作提示文案统一
- 进展（2026-03-04）：已新增 `docs/standards/copywriting-system-v1.md` 与 `docs/standards/error-message-map-v1.md` 初稿，并已在管理端/评测端部分页面接入映射。
- 当前缺口（2026-04-25）：业务能力、模型弹药库、能力健康、评测端仍需继续减少技术术语，优先使用“业务能看懂”的描述。

6. `doing` 第三方 API 原子能力执行面
- 产物：`vendor-api-ops` MVP、OpenAI 类特殊网络能力接入准则、API Key 托管/轮换边界
- 验收：backend 通过 executor 调用 vendor-api-ops，Coze 工具箱仍只指向 backend，OpenAI 出网能力不依赖 Coze 主机
- 进展（2026-04-24）：已确认 Coze 主机直连 `api.openai.com` 超时；已定稿原子能力分类准则（`docs/standards/atomic-ability-boundary.md`）与 `vendor-api-ops` v2 方案（`docs/plans/2026-04-24-vendor-api-ops-mvp.md`）。代码侧已接入 `POST/GET /v1/invocations`、Key 隐藏读取、SQLite 持久化、KIE 真实 submit/poll adapter、OpenAI/OpenAI-compatible Images adapter、`vendor_api` executor、OpenAI 图片编辑能力 seed，以及 backend 对 Baidu/Volcengine/KIE/OpenAI 的 vendor-api 优先路由。
- 进展（2026-04-25）：已补 vendor-api-ops Key 级并发保护、最近调用统计、KIE 临时失败轻量重试；管理端“模型弹药库”展示 24h 调用数/成功率/错误分布，backend 代理新增 `/api/admin/vendor-api/usage/summary`。
- 进展（2026-04-25）：已补能力健康汇总与手动刷新接口，管理端“能力目录”展示正常/需关注/异常/未测试/需要复测；业务 OpenAPI 增加错误响应枚举，便于 Coze/业务方按错误码处理。
- 进展（2026-04-25）：能力健康清单增加筛选和 CSV 导出，可按需要复测、异常、未测试、超时未测等口径生成上线前验证清单。
- 进展（2026-04-25）：业务 API 支持图裂变/扩图常用参数顶层传入，同时保留 `inputs` 旧格式兼容，降低业务方接入理解成本。
- 进展（2026-04-25）：业务 API 运行记录补齐 `traceId/requestId/tenantId/clientId/channel/source`、耗时和成本/配额预留字段；底层能力日志的成本信息会回填到业务任务与步骤，为后续收费、灰度和对外 API 报表打基础。
- 进展（2026-04-25）：管理端业务能力页新增调用统计视角，后端提供 `/api/admin/business/usage-summary`，可按业务、版本、来源、业务方、客户端和追踪 ID 查看调用量、成功率、失败样本、平均耗时、成本和额度。
- 进展（2026-04-27）：模型弹药库新增 Key 池交互说明与 Secret Key 输入；`vendor-api-ops` 敏感接口支持服务 token 保护；“验证 Key”会优先使用 Key 池 active Key 访问厂商检查接口，并区分网络可达、缺 Key、鉴权失败。单条 Key 支持独立验证，结果写入 `metadata.lastCheck` 并在 Key 池列表展示最近验证状态。
- 进展（2026-04-27）：根据最新边界调整，第三方 Key 权威回收到中台 `api_keys` 表；`vendor-api-ops` 改为白名单保护的干净执行通道，支持中台随请求传入本次选中的 Key，并避免把请求级凭证写入明文调用记录。`vendor-api-ops` 本地 Key 加密能力仅作为历史兼容，不作为新能力接入路径。

7. `doing` 业务能力层与版本治理
- 产物：`/api/business/*` 稳定入口、业务版本/配方、默认版本切换、灰度、回滚、业务运行记录。
- 验收：花纹提取、图裂变和扩图可以不改业务方入参，通过中台切换底层版本；管理端能看到发布时间、默认版本、最近调用、失败样本和成本字段。
- 进展（2026-04-25）：图裂变、扩图样板业务已落地，业务运行统计已接入管理端；下一步是补认证/租户后，把业务方隔离、额度和对外 API 契约接上。
- 进展（2026-04-28）：业务版本治理补齐灰度命中预览和专用“设为默认版本”接口；灰度分流支持顶层 `tenantId/clientId/traceId/requestId`，管理端可在不提交真实任务的情况下验证业务方会命中哪个版本。
- 进展（2026-04-28）：业务版本治理补齐“回滚默认版本”闭环；默认版本切换会记录上一默认版，管理端可一键回滚，后端在缺少切换记录时会退到同业务最近的 active 非默认版本。
- 进展（2026-04-28）：图裂变、扩图补齐 active 非默认保底版本；新增业务版本安全垫检查脚本，发版前必须确认核心业务都有可回滚目标。
- 进展（2026-04-28）：新增业务方配置基础能力，支持按 `tenantId/clientId` 启停、限定可调用业务、日调用上限、日额度上限和并发上限；业务提交前会自动校验已配置业务方。
- 进展（2026-04-28）：管理端“业务能力”页新增接入方策略面板，可直接新增/编辑/启停业务方策略，并配置可用业务、并发上限、日调用上限和日额度上限。

## P2（增长与扩端，4-8 周）

1. `todo` 模板与复用机制
- 收藏模板、历史复跑、一键复用

2. `todo` 留存机制
- 结果对比、进度提示、回访提醒

3. `todo` 移动端/小程序 IA
- 先做 IA 与关键路径，不直接开发全功能

4. `todo` API 商业化策略
- 接口套餐、限流策略、配额展示

---

## 当前执行单（2026-05-06 精简版）

说明：本节是当前唯一有效 TODO。前面的 P0/P1/P2 是路线图和历史进展，不能直接当成开发清单。

0. `done` 阶段性清理与本地回归基线
- 已完成：本地临时产物清理、`.gitignore` 补充、真实口令/数据库示例口令清理、`backend/uv.lock` 纳入版本。
- 已完成：删除本地日志、pid、缓存、`dist`、`runtime`、历史 Coze 测试输出和历史客户端本地副本；保留 `.env`、`.venv`、`node_modules` 与编辑器状态目录。
- 已完成：后端全量测试、重点回归、管理端构建、测评端构建、敏感内容扫描、Alembic 单 head 检查。
- 不再作为当前 TODO：账单运营骨架、业务运行批量复测/排障清单、成本核算基础口径、能力调用“当前要做”、ComfyUI 超时降级。

1. `done` 统一认证与角色上线验证
- 目标：把账号、角色、租户/业务方隔离从“功能可用”推进到“上线可验收”。
- 已完成：
  - `GET /api/auth/scope-summary` 输出上线检查、业务 API 权限边界和角色边界。
  - 角色边界覆盖管理员、业务方账号、服务 Token、Coze 工具箱四类调用方，明确允许/禁止范围。
  - 发布 smoke 会校验账号风险、业务 API 权限边界和角色边界；缺失或未生效会阻断发版。
  - 管理端账号权限页展示“当前先处理什么 / 业务 API 权限边界 / 角色边界”，避免只靠文档判断。
- 验收：`backend/.venv/bin/python -m pytest backend/tests/test_auth_sessions_invites.py backend/tests/test_podi_release_smoke.py -q` 通过；`podi-admin-web npm run build` 通过。

2. `done` 管理端易用性最后收口
- 目标：业务人员默认先看到结论、风险和下一步，底层执行器、节点、模型参数默认降级到详情。
- 进展（2026-05-06）：账号权限页新增角色边界；ComfyUI 任务页新增“当前先处理什么”，把队列读不到、任务疑似卡住、下发偏慢、能力对齐失败转成中文动作建议。
- 进展（2026-05-06）：抽出管理端共享“处理建议卡片”，并接入业务能力、模型弹药库、能力目录、ComfyUI 任务页；四个核心页面顶部都统一为“结论 + 原因 + 下一步动作”。
- 进展（2026-05-06）：本地浏览器已走查业务能力、模型弹药库、能力目录、ComfyUI 任务页；四个页面均能先给业务判断和下一步动作，再进入明细配置。
- 后续归属：整体视觉重设计、测评端大包体拆分、底层字段进一步降噪，统一放到后续前端整体整改阶段。
- 验收：管理端核心页面不需要用户先理解执行器、工作流、厂商、字段结构等底层概念，也能判断下一步。

3. `done` 第三方模型治理细节
- 目标：火山、OpenAI、中转站、KIE 等第三方能力可以被中台稳定管理、验收、计价和上线。
- 进展（2026-05-06）：第三方模型上线门禁补齐 `egressVerified`；需要国际出口的模型不再永久带 warning，只有最近 7 天内缺少 active Key 的带密钥出网验证成功时才提示 `VENDOR_MODEL_GLOBAL_EGRESS_REQUIRED`。
- 进展（2026-05-06）：第三方模型门禁补齐首要处理动作，固定按启用模型、补密钥/验密钥、查出网、跑验收、补计价排序；管理端处理队列优先展示真正能执行的下一步。
- 进展（2026-05-06）：模型弹药库补齐批量处理和历史审计；支持批量启用/停用、批量记验收、批量应用计价策略，所有模型编辑和批量动作都会写入 `auditRecords`，普通编辑 `metadata` 不会误删验收记录。
- 进展（2026-05-06）：业务默认版本门禁已接上模型弹药库治理结果；第三方模型缺计价、缺模型验收、缺可用 Key、出网模型缺最近 7 天带密钥验证成功时，业务版本不能直接设默认、申请默认或审批默认。
- 已完成（2026-05-07）：结构开发已收口；KIE 余额恢复后的真实商业模型回归改为外部阻塞项，不继续占用当前开发 TODO。
- 验收：模型弹药库能直接告诉用户“这个模型能不能上线、缺什么、下一步处理什么”。

4. `done` 阶段版本交付准备（本地门禁）
- 目标：把当前大版本收口成可上线版本，而不是继续无限加功能。
- 进展（2026-05-06）：本地阶段门禁已复跑，后端全量 `434 passed, 23 warnings`，管理端构建通过，测评端构建通过但仍有已知 Vite 大包体警告，`git diff --check` 通过；构建产物和 pytest 缓存已清理。
- 进展（2026-05-07）：本地阶段门禁复跑完成，后端全量 `434 passed, 23 warnings`，管理端构建通过，测评端 `npm run lint` 与 `npm run build` 通过且无 Vite 大包体警告，Alembic 单 head、`git diff --check` 均通过；当前本地分支为 `main`，本地 HEAD 与 `origin/main` 一致，尚未推送本轮改动。
- 已完成（2026-05-07）：推送前本地检查已覆盖 `origin/main` 同步状态、迁移单 head、前后端构建、格式空白检查和敏感内容扫描。
- 发版动作：推送后仍需在服务器更新后跑 114 线上 smoke、三大业务真实巡检、Coze 工具箱链路抽测，并确认 114 环境变量已配置 `EVAL_ADMIN_TOKEN`。
- 验收：线上管理端能看到新版本，Coze 到中台能力链路可用，三大主业务至少完成一轮真实巡检。

5. `done` 测评端体验整理
- 触发条件：管理端最后收口完成并交付一版后再做。
- 已完成（2026-05-06）：测评端首页卡片已按业务变体重新降噪，优先展示“高质量新版 / 四方连续裂变 / 无提示词 / 商业模型”等可读名称，并补充发布时间、短工作流 ID、输入/输出类型、执行面和追踪状态。
- 已完成（2026-05-06）：`/api/evals/metrics/workflows` 增加近 72 小时运行概况，卡片直接显示“最近可用 / 最近失败 / 生成未回填 / 暂无运行”，避免用户进入工作台后才发现功能不可用。
- 已完成（2026-05-06）：测评端首页卡片已增加“看成功记录 / 看异常记录 / 看未回填”等直接入口，可从卡片进入对应工作台并自动聚焦历史记录；本地浏览器已验证成功样本入口与“查看全部记录”恢复动作。
- 已完成（2026-05-06）：测评端构建分包完成，移除前端 `ali-oss` SDK 依赖，上传改走后端 `/api/evals/uploads`；生产构建已不再出现 Vite 大包体警告。
- 已完成（2026-05-06）：测评端首页新增“业务分类总览”，5 个业务分类直接展示功能数量、近 72 小时成功次数和健康标签；本地浏览器已验证从分类总览切到“图延伸类”后 URL、标题和卡片数量同步正确。
- 已完成（2026-05-06）：测评端功能工作台顶部新增业务摘要，进入具体工作流后直接展示发布时间、输入方式、输出结果、任务追踪和近 72 小时健康状态；本地浏览器已验证图裂变高质量新版工作台渲染正常且无新增控制台错误。
- 已完成（2026-05-06）：测评端历史记录新增“提交 / 回填 / 最终 / 输出”四段状态条，成功、有非图片结果、等待结果、成功无回填和无结果会直接显式展示；本地浏览器已验证历史记录成功样本显示“已提交 / 回填成功 / 成功 / 已回填 1 张”。
- 已完成（2026-05-07）：测评端工作台主体改为“输入与运行 / 结果与排障”两块，提交按钮旁直接提示是否等待主图，结果图组件从旧暗色 Tailwind 样式切回当前浅色操作台体系；本地浏览器已验证图裂变高质量新版工作台加载正常且无新增控制台错误。
- 已完成（2026-05-07）：测评端历史复盘区新增汇总入口，可一键筛选成功、失败、运行中、成功无回填和未打分记录；同时修复状态筛选和“未打分”开关未参与列表过滤的问题，本地浏览器已验证点击“失败”后列表从 22 条收敛到 5 条。
- 已完成（2026-05-07）：测评端业务接入文档改为默认收起，普通测试路径不再被长代码块打断；打开新工具时自动恢复收起状态。
- 已完成（2026-05-07）：测评端最终本地走查完成，高质量新版工作台可正常进入，接入文档默认收起并支持展开/收起；`npm run lint` 与 `npm run build` 均通过，生产构建无 Vite 大包体警告。
- 已完成（2026-05-13）：功能页新增“多图试跑”轻量入口，支持一次上传多张主图并拆成多个独立任务；每张图仍独立 `runId`，结果区复用同一套原图/结果滑块对比组件。批量回归继续保留为发版验收和稳定性巡检入口。
- 剩余任务：无；后续只在阶段发版前纳入统一门禁复跑。

6. `blocked` 客户端重构
- 触发条件：中台和测评端主链路稳定后再进入。
- 当前结论：不占用本轮开发资源。

7. `later` P2 增长能力
- 范围：模板复用、历史复跑、结果对比、移动端/小程序 IA、API 商业化套餐策略。
- 当前结论：不是本阶段 TODO。

---

## 当前执行单（2026-05-07 续推进版）

说明：本节承接 2026-05-06 精简版，记录 2026-05-07 已完成的推进过程。由于颗粒度仍偏大，后续开发已改由文末“当前执行单（2026-05-07 细化版）”接管；本节不再作为新增开发的唯一执行清单。

1. `done` 战略指标面板产品化
- 目标：把原来偏技术的战略快照变成管理端总览能直接理解的“北极星 + 关键指标 + 下一步动作”。
- 已完成：
  - `/api/admin/dashboard/metrics` 的 `strategy` 增加 `north_star` 和 `indicators`，覆盖业务成功率、计费完整度、回调健康、扣费闭环、风险闭环。
  - 每个指标输出当前值、目标值、状态、原因和下一步动作，避免管理端只展示原始数字。
  - 战略周报 markdown 增加“北极星与 KPI”段落，方便后续定期导出和复盘。
  - 管理端总览页新增战略指标快照卡片，直接展示北极星、5 个 KPI 和中文处理建议。
  - 接口文档已同步补充 `north_star/indicators` 返回结构。
- 验收：
  - `backend/.venv/bin/python -m pytest backend/tests/test_admin_dashboard_release_governance.py -q` 通过：8 passed。
  - `npm run build` in `podi-admin-web` 通过。
  - 本地浏览器已走查管理端总览页，战略指标区正常渲染且无新增控制台 warning/error。

2. `doing` 管理端信息架构第二轮
- 目标：继续把管理端从“技术配置后台”整理成“先总后分”的业务运营控制台。
- 进展（2026-05-07）：管理端总览页新增“运营驾驶舱”，上线、业务、能力、成本、风险五个结论优先展示，并可跳转到业务能力、模型弹药库、能力目录、账单框架、能力调用等对应模块。
- 进展（2026-05-07）：本地浏览器已走查管理端总览页，运营驾驶舱和战略指标快照均正常渲染，控制台无新增 warning/error。
- 进展（2026-05-07）：模型弹药库新增“模型类型总览”，把图片、视频、文字/多模态、图像理解、待确认模型分开展示，避免第三方模型继续被默认理解成生图能力。
- 进展（2026-05-07）：本地浏览器已走查模型弹药库，模型类型总览正常渲染，控制台无新增 warning/error。
- 进展（2026-05-07）：管理端总览页新增“平台功能入口地图”，把业务能力、业务运行、能力评测、模型弹药库、原子能力、调用排障、ComfyUI 纳管、运行线路、账单、账号权限、发版自检和高级编排入口集中露出，避免功能做完但用户找不到。
- 进展（2026-05-07）：入口地图走查时发现账单框架在本地表未初始化时会触发 500；已将账单只读接口改为可降级空框架或“待初始化”报表，浏览器复测“账单与套餐”入口不再出现加载失败和控制台 500。
- 进展（2026-05-07）：高级模块直达逻辑已修复；用户从入口地图或直接访问 `#nav=billing`、`#nav=workflow-builder` 时会自动打开高级视图，不再被重定向回总览。
- 进展（2026-05-07）：本地浏览器逐个走查 13 个管理端入口：总览、业务能力、能力评测、模型弹药库、能力目录、能力调用、ComfyUI 纳管、运行线路、账单、账号权限、调度监控、系统配置、高级编排，均能打开且无明显加载失败。
- 进展（2026-05-07）：能力调用清单新增“输出类型概览”，当前筛选结果会按图片、视频、文字、结构化、资源、未回填分开展示；结果列同步增加输出类型标签，避免视频、VL 和文字能力继续被默认当成生图记录。
- 进展（2026-05-07）：本地浏览器已走查“能力调用”页面，输出类型概览正常渲染，当前页图片输出、视频输出、文字输出、结构化结果、资源输出和未回填标签均可见，控制台无新增 warning/error。
- 进展（2026-05-07）：管理端新增“API 开放”一级入口，把中台自有业务 API、原子能力 API 和 Coze 工具箱分开展示；总览入口地图同步新增“API 开放”，本地浏览器已验证可从总览跳转，控制台无新增 warning/error。
- 进展（2026-05-08）：总览页“运营驾驶舱”顶部新增一句话上线结论，直接说明“可以上线 / 待验收确认 / 暂不能上线”和原因；业务能力页新增“业务能力怎么用”，把上新版本、处理失败、业务接入、灰度回滚四条常见路径前置，减少用户先理解底层概念的成本。
- 下一步优先：
  - 总览页：继续压缩低优先级技术块，强化上线、业务、能力、成本、风险五类结论。
  - 业务能力页：让图裂变、扩图、花纹提取三条主业务的版本、默认、灰度、回滚、最近失败样本更直观。
  - 能力/模型页：继续减少执行器、厂商字段对普通用户的干扰，把底层细节降到详情区。
- 验收口径：用户不理解 ComfyUI、执行节点、厂商、字段结构，也能判断“现在能不能上线、哪里有风险、下一步做什么”。

3. `todo` 业务能力闭环第二轮
- 目标：围绕花纹提取、图裂变、扩图三条主业务，补齐业务级可观测、复测和回滚证据。
- 进展（2026-05-07）：管理端业务能力页新增“主业务编排地图”，按业务入口展示生产版本、灰度状态、备选版本、底层编排步骤和当前建议，让中台自有业务编排更接近 Coze 工作流的可读视角。
- 进展（2026-05-07）：修正业务能力页建议口径，默认版本未满足上线门禁或未验收时，会优先提示“先跑真实链路并记录验收”，不再和风险标签产生“未满足上线但入口稳定”的冲突。
- 进展（2026-05-07）：本地浏览器已走查业务能力页，花纹提取、图裂变、扩图三条主业务的编排地图正常渲染，控制台无新增 warning/error。
- 进展（2026-05-07）：业务调用详情新增完整闭环视图，把版本选择、能力下发、执行节点、输出回填、业务回调、计费扣减放在同一条链路里；计费异常、未定价、等待扣减会直接显示下一步建议。
- 进展（2026-05-07）：业务调用详情的步骤区改为“业务闭环步骤”卡片，直接展示每一步做了什么、结果是什么、命中哪个节点、排障编号是什么；原步骤表和原始请求/输出 JSON 已降级到折叠区。
- 进展（2026-05-07）：本地浏览器打开真实业务调用详情，已确认“计费扣减”“业务闭环步骤”“流程说明”正常显示，成功任务不再出现“成功但进度 33%”的矛盾摘要，控制台无新增 warning/error。
- 进展（2026-05-07）：最近业务调用区新增“业务复测闭环”卡片，把当前已加载问题、可复测数量、回调失败、排障清单、批量复测、回调重试和标记无需处理前置展示；原筛选条里的重复批量按钮已降噪。
- 进展（2026-05-07）：本地浏览器已走查业务能力页，业务复测闭环正常渲染，当前页问题统计和四个批量动作可见，控制台无新增 warning/error。
- 进展（2026-05-07）：业务版本卡片新增“上线证据”块，集中展示上线门禁、人工验收、证据运行、最近真实样本、底层治理、密钥、成本和出网状态；门禁主视图不再展示 `BUSINESS_RELEASE_*` 底层码，改为中文原因。
- 进展（2026-05-07）：本地浏览器已走查业务能力页，上线证据块正常渲染，未验收版本显示“缺少真实链路验收记录”，未出现底层门禁码，控制台无新增 warning/error。
- 进展（2026-05-07）：管理端总览“上线结论”新增“三条主业务上线证据”，直接聚合花纹提取、图裂变、扩图的默认版本、验收状态、最近真实样本和门禁原因；总览判断不再只看默认入口是否存在。
- 进展（2026-05-07）：本地浏览器已走查总览页，三条主业务上线证据正常渲染，并纳入“主业务上线证据”阻塞项；主视图未出现 `BUSINESS_RELEASE_*` 底层码，控制台无新增 warning/error。
- 进展（2026-05-07）：管理端总览新增“今日处理顺序”，把上线阻塞、主业务验收、巡检、模型能力风险、回调和排队按优先级整理成可点击处理项；本地浏览器已验证第一项可跳到业务能力页，控制台无新增 warning/error。
- 进展（2026-05-12）：接入 AI 团队新交付的图裂变能力包：新增 `vl_fission_control_card` 作为统一 VL 控制卡组件、新增 `comfyui_flux_strong_hq_softstyle_fission_control_v1` 作为 ComfyUI VL 控制卡裂变原子能力、新增 `vl_fission_generated_image_evaluate` 作为裂变生成图质量评估能力；业务层新增非默认候选版本 `biz_fission_v3_comfyui_vl_control_card`，不自动二次裂变，业务方按评估结果自行决定是否重试。
- 进展（2026-05-13）：GPT Image 2 + VL 控制版图裂变固定单次输出 1 张图，避免一个 `runId` 聚合多张图后造成回调、评分、重试和二次裂变歧义；`count`、保构图、保边框、保密度、风格迁移等内部控制项已从业务交付面隐藏，保留中台内部默认策略。
- 进展（2026-05-13）：接入 ComfyUI 裂变颜色锁定交付包 v2，新增非默认候选版本 `biz_fission_v4_comfyui_vl_colorlock` 和原子能力 `comfyui_flux_strong_hq_softstyle_fission_colorlock_v2`；`bili` 仍按既定公式映射 `denoise`，不写死，ColorMatch/IPAdapter 等颜色锁定强度按交付包固定。
- 计划：
  - 后续继续把线上真实巡检结果和能力复测结果沉淀到同一张上线证据视图，减少口头确认。

4. `todo` 项目清理与文档收敛
- 目标：继续清理废弃入口、历史口径、重复 TODO 和容易误导上线判断的内容。
- 进展（2026-05-07）：已清理本轮本地构建和测试缓存，包括管理端 `dist`、pytest 缓存、Python `__pycache__` 和 Playwright 临时日志；`find` 复查未再发现 `dist/.pytest_cache/__pycache__/*.log/*.pid`。
- 进展（2026-05-07）：已复跑 `git diff --check`，当前改动无空白格式错误；扫描到的“临时/占位”主要属于后续账单积分框架说明，暂不在本轮删除。
- 进展（2026-05-07）：已清理 `docs/plans/README.md` 过期口径，不再把旧客户端方案标为“现行骨架”；当前有效计划只保留管理端前端整理与 ComfyUI 并发路由专项，客户端方案明确降为历史资料。
- 进展（2026-05-07）：已补 `docs/weekly/2026-W16.md` 历史说明，明确当周 `podi-client-web` 主线判断只代表历史归档，不代表当前开发主线。
- 进展（2026-05-07）：已给三份高风险客户端单篇文档补历史说明：`docs/client/plans/2026-04-16-client-phase1-operating-model.md`、`docs/client/tech-review-2026-04-16/02-当前实现与代码主线.md`、`docs/plans/2026-03-19-style3d-client-rearchitecture-design.md`，避免用户绕过入口文档直接打开单篇时误用。
- 进展（2026-05-08）：入口文档断链检查扩展到平台边界、架构、业务模型、管理端、评测端、Coze、ComfyUI、测试、周报和发布门禁等模块入口，并新增专项测试，避免模块入口坏链路绕过发版源预检。
- 进展（2026-05-12）：新增 `docs/api/examples/fission-delivery-contract-2026-05-12.md`，沉淀两个图裂变交付接口和裂变评分接口的排队轮询兼容关系、类图关系、业务流图和参数聚合规则；明确 `bili` 是裂变幅度/重绘幅度，不再写成相似度。
- 进展（2026-05-12）：新增 `docs/weekly/2026-W20.md` 作为当日收口复盘，记录图裂变业务接口交付、测评端问题、发布流程反思、参数准则和明日优先级。
- 进展（2026-05-12）：新增 `docs/standards/delivery-methodology.md`，把今日反思固化为中台交付方法论，覆盖业务全景、契约先行、四层交付、测试闭环、发布记录、文档沉淀和 Codex 执行纪律。
- 进展（2026-05-13）：图裂变交付包 `podi_fission_delivery_20260513_v2.zip` 已按 GPT Image 2 单图口径重打包；每个接口继续独立目录、独立说明。该阶段仍包含调试脚本，2026-05-15 v4 已升级为 JSON 示例优先。
- 计划：
  - 当前 TODO 只保留文末执行单为有效开发入口。
  - 文档索引继续按“架构 / 接口 / 运维 / 测试 / 业务能力”归类。
  - 回归报告只记录真实执行过的测试，不再写未执行的假闭环。

---

## 当前执行单（2026-05-07 细化版）

说明：从现在开始，本节是唯一有效的新增开发 TODO。详细背景、问题归因和验收口径见 `docs/strategy/mid-platform-detailed-execution-plan-2026-05-07.md`。后续每完成一项，必须在本节标记状态，并同步必要文档与回归报告。

0. `done` 平台目标偏差盘点与后续方案
- 目标：确认当前中台和目标 AI 能力运营平台之间的真实差距，避免继续头疼医头、脚疼医脚。
- 已完成（2026-05-07）：新增 `docs/strategy/mid-platform-gap-and-roadmap-2026-05-07.md`，按业务层、原子能力层、调度执行层、接入层、管理端、测评端、商业化基础七个维度做差距分析。
- 当前判断：代码底座约 65%，平台闭环约 45%，用户可理解程度约 45%，稳定性工程约 55%，商业化准备约 35%。
- 后续执行：先按 A2/A3/A4 钉死 API 与契约，再按 B/C/D/E 补业务闭环、能力治理和 ComfyUI 稳定证据。

0.1. `done` 同步前端整改规则
- 目标：后续不再等所有能力做完才统一改前端，每个编号任务必须同步判断页面是否需要调整。
- 已完成（2026-05-07）：`docs/strategy/mid-platform-gap-and-roadmap-2026-05-07.md` 与 `docs/strategy/mid-platform-detailed-execution-plan-2026-05-07.md` 已写入同步前端规则。
- 已完成（2026-05-07）：管理端 `API 开放` 页新增“同步交付门禁”，直接展示页面、文档、错误、冒烟四个状态，作为 A2/A3/A4 的前端承接入口。
- 后续执行：每个 TODO 标记 `done` 前，必须同时确认对应管理端或测评端页面入口；若暂不做页面，必须写清原因。

### A. API 开放与入口一致性

1. `done` A1 固定三类 API 入口说明
- 目标：管理端能清楚区分“中台自有业务 API / 原子能力 API / Coze 工具箱 API”。
- 已完成（2026-05-07）：新增管理端 `API 开放` 一级入口，并在总览入口地图露出；本地浏览器已验证可从总览跳转。
- 已完成（2026-05-07）：`API 开放` 页已新增同步交付门禁，标出页面已露出、文档待核对、错误待补齐、冒烟待补齐。
- 进展（2026-05-08）：`API 开放` 页新增“业务方最短接入路径”，把选业务接口、提交任务、保存 runId、查询或接回调、按错误码处理前置成五步，不再要求业务方先理解完整接口表。
- 代码位置：`podi-admin-web/src/features/admin/integration/apiExposure.tsx`。
- 验收：用户能知道业务方优先用业务 API，开发接入方可用原子能力 API，历史 Coze 编排继续用 Coze 工具箱。

2. `done` A2 对齐 API 文档和页面展示
- 目标：页面展示的每个接口，在 `docs/api/INDEX.md` 和模块文档中都能找到说明。
- 已完成（2026-05-07）：`docs/api/INDEX.md` 已新增管理端“API 开放”页对应关系；`business`、`abilities`、`coze` 三个模块文档均补齐与页面接口清单的一一对应表。
- 已完成（2026-05-07）：Coze 文档补齐 ComfyUI 总工具箱、高质量图裂变、扩图、背景抠图、头部抠像、KIE 聚合工具箱等页面展示入口。
- 验收：`API 开放` 页面、`docs/api/modules/business.md`、`docs/api/modules/abilities.md`、`docs/api/modules/coze.md` 三处口径一致。

3. `done` A3 补齐 API 错误口径
- 目标：三类 API 的缺参、鉴权、队列满、依赖失败、超时都有错误码说明。
- 已完成（2026-05-07）：业务 API、原子能力 API、Coze 工具箱均补齐错误处理口径表，明确业务方或 Coze 工作流遇到缺参、鉴权、配额/并发限制、队列满、依赖失败、超时和成功无回填时如何处理。
- 已完成（2026-05-07）：已对照 `docs/standards/error-catalog.md` 核对当前引用错误码，未新增未登记错误码。
- 验收：新增或修改接口不存在“只有成功示例、没有错误示例”的情况。

4. `done` A4 补 API 冒烟清单
- 目标：形成可复跑的 API 自检清单，覆盖 OpenAPI、业务提交、任务查询、工具箱导入地址。
- 已完成（2026-05-07）：新增 `docs/testing/API_EXPOSURE_SMOKE_CHECKLIST.md`，覆盖健康检查、业务 OpenAPI、原子能力清单、原子能力表单选项、Coze 综合/ComfyUI/专项/KIE 工具箱，以及业务查询、Coze 查询、缺主图三类错误口径。
- 已完成（2026-05-07）：`docs/testing/README.md` 已把 API 开放冒烟清单纳入平时能力上线优先入口。
- 验收：阶段发版前能明确看到哪些 API 入口已测、哪些因环境未测。

### B. 管理端信息架构和功能暴露

5. `done` B1 做功能暴露台账
- 目标：管理端每个一级入口都有“解决什么问题、谁使用、下一步动作”。
- 当前发现：一级入口已覆盖总览、业务能力、API 开放、能力评测、模型弹药库、能力目录、能力调用、ComfyUI 纳管、运行线路、账单、账号权限、系统配置、高级编排。
- 已完成（2026-05-07）：总览页新增“功能暴露台账”，按侧栏 17 个一级入口逐项展示使用对象、先看什么、下一步动作、风险提示、当前状态和高级入口标记。
- 已完成（2026-05-07）：本地浏览器已走查总览页，确认功能暴露台账正常渲染，六个导航分组均可见，控制台无新增 warning/error。
- 验收：新增功能不能只存在于代码或二级角落，必须在总览或导航中可发现。

6. `done` B2 总览页继续先总后分
- 目标：用户先看到上线、业务、能力、成本、风险结论，再进入详情。
- 当前进展：总览已有运营驾驶舱、战略指标、主业务上线证据、今日处理顺序。
- 进展（2026-05-07）：总览页补齐“功能暴露台账”，让用户先从全局看到每个入口的用途和处理顺序，再进入具体模块。
- 进展（2026-05-07）：业务能力页补齐“三主业务闭环总表”，总览判断可继续和业务页主视图保持同一套“默认、验收、样本、回调、计费、回滚”口径。
- 已完成（2026-05-07）：总览页“功能暴露台账”改为默认折叠，首屏继续优先展示运营驾驶舱、今日处理顺序、功能入口地图和上线结论。
- 验收：用户不理解底层节点，也能判断能否上线和下一步处理项。

7. `done` B3 业务能力页聚焦三主业务
- 目标：花纹提取、图裂变、扩图能直接看到默认版本、灰度、回滚、最近失败、上线证据。
- 已完成（2026-05-07）：业务能力页新增“三主业务闭环总表”，三条主业务逐项展示当前默认版本、验收状态、最近真实样本、近 24 小时成功/失败/运行、回调风险、计费风险和回滚备选。
- 已完成（2026-05-07）：本地浏览器已走查业务能力页，确认花纹提取、图裂变、扩图及默认、验收、回调、计费、备选信息可见，控制台无新增 warning/error。
- 进展（2026-05-08）：业务能力页新增“三主业务当前结论”，把每条主业务压缩成当前状态、默认版本、当前卡点、下一步、验收/样本/回滚证据；同时修正回滚证据不足时仍显示“闭环正常”的口径，统一为“备选待验收 / 缺回滚”。
- 验收：三主业务每条都能回答“当前默认是谁、有没有验收、最近成功样本在哪里、失败怎么处理、怎么回滚”。

8. `done` B4 底层配置降噪
- 目标：执行器、节点、厂商字段默认收进详情区，不作为普通用户主提示。
- 进展（2026-05-07）：业务版本卡片主区不再直接展示底层能力和模型来源，改为默认收起的“底层信息：能力、模型、配方”；主区保留发布时间、最近结果、上线门禁和验收状态。
- 进展（2026-05-07）：能力目录表格不再把能力标识、工作流、执行线路、模型编号放在主提示里，改为默认收起的“查看底层”；主列保留业务分类、能力类型、供应来源和模型风险。
- 进展（2026-05-07）：本地浏览器已走查业务能力页和能力目录页，底层信息默认收起，控制台无新增 warning/error。
- 已完成（2026-05-07）：ComfyUI 服务器页新增“当前先处理什么”和“服务器健康总览”，先展示可接任务、主服务器、资源差异、读取状态和下一步动作；每台服务器卡片补充“可接任务 / 待刷新资源 / 资源对齐或缺失”普通判断，底层地址、版本、模型和插件明细仍保留给运维排障。
- 验证（2026-05-07）：`npm run build` in `podi-admin-web` 通过；本地浏览器已走查 ComfyUI 服务器页，页面显示 2 台纳管节点、2 台可接任务、1 台资源差异，控制台无 warning/error。
- 进展（2026-05-08）：能力目录新增“能力上线总览”和“优先处理能力”清单，把健康、复测、表单、模板、成本、模型状态合成“可接业务 / 暂不能接 / 需处理”的业务判断，底层执行器、工作流、模型编号继续收在详情区。
- 进展（2026-05-08）：能力调用页新增“能力调用排障总览”，把当前页调用记录按执行失败、回调失败、成功无回填、排队或执行中、已回填、证据不足分组，并给出中文处理动作；用户不需要先读原始响应也能判断先处理哪里。
- 验收：主提示使用业务语言，不直接把技术字段当用户动作。

9. `doing` B5 本地浏览器走查常态化
- 目标：每次改 UI 后走查对应页面并记录。
- 进展（2026-05-07）：本轮已走查总览、业务能力、能力目录，确认新增折叠区和降噪后的主信息正常渲染，控制台无新增 warning/error。
- 进展（2026-05-07）：C1 完成后复查总览和业务能力页，三主业务上线证据均显示 3 条，控制台无新增 warning/error。
- 进展（2026-05-07）：C3 完成后复查业务能力页，业务列表可见计费扣减阶段说明，当前浏览器控制台无 warning/error。
- 进展（2026-05-07）：C5 完成后复查“API 开放”页，业务 API 卡片已明确“不需要 Coze 工作流 ID”，提交示例和 runId 查询示例可见，三主业务 route-preview 入口已出现在表格中，控制台无新增 warning/error。
- 进展（2026-05-07）：D1 完成后复查业务能力页和能力目录，最近业务调用可见“1 张图 · 1 个资源”，详情链路证据可见“图/视频/文字/结构化/资源”五类计数，控制台无 warning/error。
- 进展（2026-05-07）：B4 完成后复查 ComfyUI 服务器页，顶部可见“当前先处理什么”和“服务器健康总览”，控制台无 warning/error。
- 进展（2026-05-07）：阶段性候选版本回归已复查管理端总览、业务能力、API 开放、模型弹药库、能力目录、能力调用、ComfyUI 服务器、ComfyUI 任务、账号权限、账单，以及测评端图裂变首页/高质量新版工作台；分页面复查控制台 warning/error 为 0。
- 进展（2026-05-08）：定期巡检报告固化和管理端信息架构二轮后复查总览页、业务能力页；总览页“一句话上线结论”可见，业务能力页“业务能力怎么用”可见，控制台 warning/error 为 0。
- 进展（2026-05-08）：能力目录页复查“能力上线总览 / 可接业务 / 优先处理能力”均可见，控制台 warning/error 为 0。
- 进展（2026-05-08）：API 开放页复查“业务方最短接入路径 / 保存 runId / 按错误码处理”均可见，控制台 warning/error 为 0。
- 进展（2026-05-08）：业务能力页复查“三主业务当前结论 / 当前卡点 / 下一步”均可见，回滚证据不足显示“备选待验收”，控制台 warning/error 为 0。
- 进展（2026-05-08）：能力调用页复查“能力调用排障总览 / 执行失败 / 回调失败 / 成功无回填 / 输出类型概览”均可见；点击“只看已回填”后分页和结果集正常更新，控制台 error/warning 为 0。
- 进展（2026-05-08）：抽出管理端通用“操作闭环”卡片并接入 API 开放、能力目录、业务能力、能力调用四个页面；复查四页均能看到“入口/优先级/下一步/完成标志”，控制台 error/warning 为 0。
- 进展（2026-05-08）：继续把“操作闭环”卡片接入模型弹药库和运行线路；模型页明确风险、模型口径、能力绑定、小流量验证，运行线路页明确用途、标签并发、健康验证、真实命中证据，控制台 error/warning 为 0。
- 进展（2026-05-08）：能力评测页新增“评测验收闭环”，明确工作流、样图参数、结果回填、评分留证四步；ComfyUI 资源页新增“ComfyUI 纳管闭环”，明确服务器、依赖、健康、调度四步。本地走查两页关键文案可见；本地业务运行记录超时和部分接口 500 为既有数据接口状态，未发现本次 UI 引入的渲染异常。
- 进展（2026-05-08）：账号权限页新增“账号权限闭环”，明确上线检查、业务方范围、邀请码、异常会话四步；账单框架页新增“账单运营闭环”，明确收费问题、套餐余量、收入成本、催收留痕四步。本地走查两页关键文案可见，最近一次控制台 error/warning 为 0。
- 进展（2026-05-09）：调度监控页新增“运行监控闭环”，明确队列、ComfyUI、最近失败、执行节点四步；系统配置页新增“环境核对闭环”，明确数据库、OSS、安全参数、Coze 集成四步。本地走查两页关键文案可见，最近一次控制台 error/warning 为 0。
- 进展（2026-05-09）：高级编排页新增“Coze 编排观察闭环”，明确画布、流程 ID、运行观察、沉淀业务能力四步；调度事件页新增“调度日志排障闭环”，明确任务 ID、事件类型、回执摘要、归口处理四步。本地走查两页关键文案可见，最近一次控制台 error/warning 为 0。
- 进展（2026-05-09）：历史密钥仓库页新增“历史密钥兼容闭环”，明确历史兼容、迁到模型弹药库、配额过期、删除留证四步；路由策略页新增“路由策略闭环”，明确业务入口、工作流模板、运行线路、优先级启停四步。本地走查两页关键文案可见，最近一次控制台 error/warning 为 0。
- 进展（2026-05-09）：API 开放页新增“业务方开通前检查”，明确身份边界、业务权限、路由预览、回调轮询和错误处理五项；本地走查关键文案可见，最近一次控制台 error/warning 为 0。
- 进展（2026-05-09）：候选版本页面走查已覆盖总览、业务能力、API 开放、模型弹药库、能力目录、能力调用、ComfyUI 纳管、路由策略、账号权限、账单框架 10 个入口；关键结论、风险和下一步文案均可见，最近一次控制台 error/warning 为 0。
- 验收：无新增控制台 error/warning；走查页面和结果写入回归报告。

### C. 三主业务闭环

10. `done` C1 三主业务上线证据统一
- 目标：花纹提取、图裂变、扩图每个默认版本都有验收状态、最近真实样本、失败原因和回滚版本。
- 已完成（2026-05-07）：新增统一计算方法 `businessReleaseEvidence.ts`，总览页和业务能力页共用同一套默认版本、验收、真实样本、回调、计费、回滚和建议判断。
- 已完成（2026-05-07）：管理端生产构建通过；本地浏览器已走查总览页“三条主业务上线证据”和业务能力页“三主业务闭环总表”，两处均显示花纹提取、图裂变、扩图三条证据。
- 验收：总览和业务能力页看到的上线结论一致。

11. `done` C2 真实巡检结果沉淀
- 目标：巡检结果进入上线证据，不只停留在脚本输出或聊天记录。
- 已完成（2026-05-07）：`backend/scripts/patrol_business_api.py` 新增 `--report` 和 `--record-release-patrol`，真实巡检可以写出 JSON 报告并沉淀到管理端发版巡检记录。
- 已完成（2026-05-07）：巡检摘要固定包含 `businessKeys/results/failedItems/acceptanceResults`，可追溯业务、runId、输出、执行节点证据和验收写入结果；记录命令时会隐藏 `--token`。
- 已完成（2026-05-07）：补充 `test_patrol_business_api.py` 专项测试，覆盖报告写入、发版巡检记录写入和 token 脱敏。
- 已完成（2026-05-08）：`scripts/run_podi_health_watch.sh` 统一写入 `reports/health-watch/` 报告；真实巡检默认追加管理端发版巡检记录，轻量 smoke、业务路由预览、评测健康和 production 测评巡检都有固定报告路径。
- 文档同步：`docs/release-preflight.md` 和 `docs/testing/API_EXPOSURE_SMOKE_CHECKLIST.md` 已补真实巡检沉淀命令。
- 验收：每次巡检后能追溯到业务、版本、runId、taskId、输出链接、失败原因。

12. `done` C3 回填失败可见
- 目标：成功但无图、回调失败、计费未扣、任务卡住都能在业务详情看到明确阶段。
- 已完成（2026-05-07）：业务运行问题分类新增“计费扣减问题”，成功但未定价、计费扣减失败、应计费但缺少套餐/钱包扣减流水都会进入可筛选问题类型。
- 已完成（2026-05-07）：结果回填问题优先级仍高于计费问题，避免“成功但无图”被误归类为计费；排障清单同步给出计费修复动作。
- 已完成（2026-05-07）：管理端业务运行问题筛选新增“计费扣减问题”，业务 API 文档同步补齐 `issue_category=billing`。
- 验证（2026-05-07）：`backend/.venv/bin/python -m pytest backend/tests/test_business_capability_admin.py -q` 通过，47 passed；`podi-admin-web` 生产构建通过。
- 验收：不再出现“后端完成了但用户看不到图、不知道卡在哪”的情况。

13. `done` C4 回滚路径可验收
- 目标：默认版本切换前能看到备选版本和最近通过证据。
- 已完成（2026-05-07）：上线证据和业务能力页不再只按“有 active 备选版本”判断回滚安全垫，改为要求备选版本同时满足启用、验收通过、最近成功真实样本有输出、上线门禁不阻塞。
- 已完成（2026-05-07）：版本对比与回滚面板新增“目标版本证据”，切默认或回滚前可直接看到目标版本是否具备验收与最近成功输出。
- 已完成（2026-05-07）：三主业务闭环总表和主业务编排地图会区分“可回滚”和“备选待验收”，避免把未验收备选误当安全垫。
- 验证（2026-05-07）：`podi-admin-web` 生产构建通过；本地浏览器打开业务能力页，确认“备选待验收”和“目标版本证据”可见，控制台无 warning/error。
- 验收：切默认不是口头动作，页面上能看到依据。

14. `done` C5 业务 API 不被 Coze 牵制
- 目标：同一业务可通过中台业务 API 直接提交和查询，不依赖 Coze 工作流。
- 已完成（2026-05-07）：新增业务 API 契约测试，覆盖 `/api/business/fission/runs` 直接提交和 `/api/business/runs/get` 查询，确认只依赖业务 API 的 `runId/taskId/source/channel/routeInfo`，不要求 Coze workflow ID。
- 已完成（2026-05-07）：管理端“API 开放”页补充 route-preview 三个入口，并在业务 API 卡片显示“提交后保存 runId，再轮询结果”，示例使用 `source=partner-api`、`channel=open-api`，明确不需要 Coze 工作流 ID。
- 已完成（2026-05-07）：`API_EXPOSURE_SMOKE_CHECKLIST` 新增不消耗额度的业务 route-preview 检查，花纹提取、图裂变、扩图均可先验证版本命中，不进入 ComfyUI 队列。
- 验证（2026-05-07）：`backend/.venv/bin/python -m pytest backend/tests/test_business_api_contract.py -q` 通过，6 passed；`npm run build` in `podi-admin-web` 通过；本地浏览器已走查 API 开放页，控制台无 warning/error。
- 验收：Coze 可以继续用，但不是唯一业务入口。

### D. 能力与模型治理

15. `done` D1 输出类型统一
- 目标：图片、视频、文字、结构化结果、普通资源、未回填分开展示。
- 已完成（2026-05-07）：能力调用清单已有输出类型概览；测评端指标、巡检和历史记录已按图片、视频、文字/VL、结构化和未回填分开展示。
- 已完成（2026-05-07）：能力测试结果摘要补齐文字输出计数，图片、视频、文字、结构化和普通资源都会进入“测试通过/确认回填”的判断。
- 已完成（2026-05-07）：业务运行链路补齐普通资源输出计数；资源类输出不再被归为“成功无回填”，业务详情的“输出回填”和“链路证据”会显示图、视频、文字、结构化、资源五类数量。
- 验证（2026-05-07）：`backend/.venv/bin/python -m pytest backend/tests/test_business_capability_admin.py -q` 通过，49 passed；`npm run build` in `podi-admin-web` 通过；本地浏览器已走查业务能力详情和能力目录，控制台无 warning/error。
- 验收：生视频、VL、文字能力不会被生图口径误判。

16. `done` D2 模型上线门禁
- 目标：第三方模型缺 Key、缺出网、缺计价、缺验收时，不能进入核心业务默认版本。
- 已完成（2026-05-07）：复查后端业务版本治理链路，默认版本门禁已读取第三方模型状态、模型验收、计价策略、运行密钥和出网验证结果；缺任一关键项时 `release_gate.canRelease=false`，设默认会返回 `BUSINESS_RELEASE_GATE_BLOCKED`。
- 已完成（2026-05-07）：新增专项测试覆盖“缺第三方 Key 不能设为默认”和“缺模型计价不能设为默认”，同时保留已有“模型未验收”“出网未验证”“模型弹药库门禁”用例。
- 验证（2026-05-07）：`backend/.venv/bin/python -m pytest backend/tests/test_business_capability_admin.py backend/tests/test_admin_vendor_api.py -q` 通过，67 passed；`py_compile` 检查 `business_runs.py/admin_vendor.py` 通过。
- 验收：模型治理问题不会绕过业务版本上线门禁。

17. `done` D3 Key 和调用统计可查
- 目标：每个 provider/model 能看到 Key 状态、最近调用、失败分布和成本字段。
- 已完成（2026-05-07）：复查 `/api/admin/vendor-api/governance/summary`、`/api/admin/vendor-api/usage/summary`、`/api/admin/vendor-api/keys` 和模型目录接口，已覆盖密钥状态、配额/冷却/耗尽/验证失败、最近调用、失败分布、模型计价和治理建议。
- 已完成（2026-05-07）：管理端“模型弹药库”已能看到模型上线处理队列、最近失败样本、密钥处理提示、厂商健康与风险、第三方调用统计和第三方密钥池；本地浏览器走查无 warning/error。
- 验证（2026-05-07）：`backend/.venv/bin/python -m pytest backend/tests/test_business_capability_admin.py backend/tests/test_admin_vendor_api.py -q` 已覆盖治理摘要、模型 release gate、Key 检查和调用统计降级，67 passed。
- 验收：第三方能力出问题时，不需要翻配置文件定位 Key、出网或余额状态。

18. `done` D4 新能力接入标准化
- 目标：新增模型或能力时，必须同步能力 schema、页面、文档、错误、测试。
- 已完成（2026-05-07）：`docs/testing/ABILITY_TEST_LEDGER.md` 已补齐新能力标准接入流程，按能力归类、能力定义、路由配置、页面露出、API 契约、错误口径、自动检查、实跑记录和交付记录九步执行。
- 已完成（2026-05-07）：台账已单独补充第三方 API / 模型接入门禁，覆盖模型目录、Key 管理、计价策略、验收记录、出网验证和统计可查。
- 已完成（2026-05-07）：台账已单独补充 ComfyUI 工作流接入门禁，覆盖 workflow seed、binding seed、节点说明、双机兼容、路由策略、输出回填和队列错误。
- 已完成（2026-05-07）：`docs/api/modules/abilities.md` 已引用能力测试台账，要求新增原子能力同批完成页面、文档、错误、测试和实跑证据。
- 验收：后续新增能力不再漏页面、漏错误、漏测试。

### E. ComfyUI 路由与稳定性

19. `done` E1 两台机器路由证据
- 目标：117.50.80.158 和 117.50.216.233 都能被真实任务命中，队列利用率可见。
- 进展（2026-05-07）：`/api/admin/comfyui/queue-summary` 已补近 24 小时真实命中证据；管理端 ComfyUI 任务页已显示“真实命中”和每台机器最近命中统计，能区分 158 / 233，不再用模糊的“117”称呼。
- 已完成（2026-05-07）：本地浏览器刷新后，管理端任务衔接诊断显示真实命中 `2/2`、近 24 小时任务 `322`，158 最近命中 `210` 条、233 最近命中 `112` 条，两台机器均有成功任务证据。
- 验收：不能只说“双机 20 队列”，必须有任务分布证据。

20. `done` E2 缺模型/缺节点检查
- 目标：任何工作流在 233 或 158 缺依赖时，发版前能被检查出来。
- 已完成（2026-05-07）：发版 smoke 默认阻断 ComfyUI 兼容性 failed/warning，并在失败详情里输出缺失节点名、缺失模型名和执行机 ID。
- 已完成（2026-05-07）：兼容性检查只把模型/LoRA/UNet/CLIP/VAE/IPAdapter 等资源字段作为发版阻断项，运行时输入图、方法选择、占位参数不再误判为缺模型。
- 已完成（2026-05-07）：本地浏览器刷新能力对齐检查后显示 active ComfyUI 能力 `14/14` 可运行、需关注 `0`、不可运行 `0`，控制台无新增 warning/error。
- 验收：缺依赖不会等用户手动测试才发现。

21. `done` E3 队列满返回清晰错误
- 目标：排满时返回可读错误和稍后重试建议，不出现无任务 ID 或长期 running。
- 已完成（2026-05-07）：ComfyUI 路由选择在所有候选节点达到队列上限时不再继续硬塞任务；统一能力调用返回 `429` 和 `Q1001/COMFYUI_QUEUE_FULL`，提示“当前生图队列已满，请稍后重试”。
- 已完成（2026-05-07）：新增后端测试覆盖“全节点满载不继续分配”。
- 验收：队列满是可恢复错误，不是事故级黑盒失败。

22. `done` E4 成功无回填阻断
- 目标：上游成功但无图片、无视频、无文本、无结构化结果时，不能当成功闭环。
- 已完成（2026-05-07）：能力任务、业务运行问题分类和业务闭环摘要已把文字与结构化结果计入有效输出，避免 VL/文字类能力被误判为“成功无回填”。
- 已完成（2026-05-07）：新增后端测试覆盖文字输出、结构化输出和业务运行问题分类。
- 验收：成功状态必须伴随可用输出或明确的结构化结果。

### F. 测评端回归

23. `done` F1 工作流目录可读
- 目标：用户能区分生产主入口、灰度版本、历史保留、辅助工具。
- 已完成（2026-05-07）：测评端卡片继续按业务角色展示“生产主入口 / 灰度验证 / 历史保留 / 辅助工具”，并补充“高质量新版”等业务变体名、发布时间、最近健康和近期输出类型。
- 已完成（2026-05-07）：本地浏览器已复查图裂变分类和高质量新版工作台，首页能看到“近期输出：图片3”，工作台能看到功能关键信息、结果与排障和历史汇总；浏览器控制台无 warning/error。
- 已完成（2026-05-08）：测评端工作流目录改为显式包含辅助工具，通用类可看到高清放大、DPI、图片打标签、LoRA 查询、队列监控等自检能力；默认公开目录治理不变，历史保留和停用版本仍隐藏。
- 已完成（2026-05-08）：通用类辅助工具展示继续降噪，图片打标签不再误显示为花纹提取，8K 高清放大和 DPI 增分按图片 URL 输出展示；辅助工具卡片增加“辅助验证工具”提示，避免被误当作业务主入口。
- 已完成（2026-05-08）：发布 smoke 新增内部测评目录检查，显式校验通用类辅助工具清单、目录角色和隐藏版本泄漏，避免线上再次出现“后台有能力但测评端看不到”的问题。
- 验收：类似图裂变多版本卡片不再长得一样。

24. `done` F2 全量工作流巡检
- 目标：active 工作流按角色限流巡检，失败原因能归类。
- 已完成（2026-05-07）：`patrol_eval_workflows.py` 保留 `--role production` 和 `--max-in-flight` 限流巡检策略，并新增 `outputKindSummary`，可统计图片、视频、文字/VL、结构化和无输出结果。
- 已完成（2026-05-07）：新增巡检脚本单测覆盖对象 URL 输出和输出类型汇总；`backend/.venv/bin/python -m pytest backend/tests/test_eval_review_api_contract.py backend/tests/test_patrol_eval_workflows.py -q` 通过，20 passed。
- 验收：巡检不会一次性打满 ComfyUI 队列。

25. `done` F3 非图片结果不误判
- 目标：视频、文字、VL、结构化结果不会被误判为无输出。
- 已完成（2026-05-07）：`/api/evals/metrics/workflows` 新增 `recentOutputKindCounts` 和 `lastRunOutputKind`，成功的文字、VL、结构化和视频结果会计入有效输出，不再被当作“成功无回填”。
- 已完成（2026-05-07）：测评端历史记录、最新结果和状态口径已按图片、视频、文字/VL、结构化、无输出分开展示；`npm run lint` 与 `npm run build` in `podi-eval-web` 均通过，生产构建无 Vite 大包体警告。
- 验收：测评端结果分类与能力调用日志一致。

### G. 文档、测试和交付门禁

26. `done` G1 唯一 TODO 细化
- 目标：后续只看本节新增开发，不再从旧路线图临时捞任务。
- 已完成（2026-05-07）：新增 `docs/strategy/mid-platform-detailed-execution-plan-2026-05-07.md`，把 API、管理端、三主业务、模型治理、ComfyUI、测评端、门禁拆成可验收小项。
- 已完成（2026-05-07）：旧“续推进版”已明确降为历史进展，本节成为唯一新增开发 TODO。

27. `done` G2 回归报告真实记录
- 目标：只记录实际跑过的测试和页面走查，不写未执行闭环。
- 已完成（2026-05-07）：本次只记录了实际完成的路由/文档/TODO 盘点、细化计划落文档、格式检查和临时产物清理；未伪造接口测试。
- 验收：报告能区分“已测”和“未测风险”。

28. `done` G3 代码清理常态化（本轮）
- 目标：每轮收尾清理 `dist`、缓存、日志、pid，并跑 `git diff --check`。
- 已完成（2026-05-07）：已执行 `git diff --check`，清理 `.playwright-mcp` 临时日志，并复查未发现 `dist/.pytest_cache/__pycache__/*.log/*.pid`。
- 进展（2026-05-07）：C3 收尾后已清理管理端 `dist`、pytest 缓存、Python `__pycache__` 和 Playwright 临时日志，并复跑 `git diff --check` 通过。
- 进展（2026-05-07）：C4 收尾后再次清理管理端 `dist` 和 Playwright 临时日志，复跑 `git diff --check` 通过，临时产物复查无输出。
- 进展（2026-05-07）：E1/E3/E4 专项后已清理构建产物、pytest 缓存、Python 缓存和浏览器临时日志，并复跑 `git diff --check` 通过，临时产物复查无输出。
- 进展（2026-05-07）：F1/F2/F3 专项后已清理测评端 `dist`、pytest 缓存、Python 缓存和浏览器临时日志，并复跑 `git diff --check` 通过，临时产物复查无输出。
- 进展（2026-05-07）：C5 专项后已清理管理端 `dist`、pytest 缓存、Python 缓存和浏览器临时日志，并复跑 `git diff --check` 通过，临时产物复查无输出。
- 进展（2026-05-07）：D2 专项后已清理 pytest 缓存、Python 缓存和浏览器临时日志，并复跑 `git diff --check` 通过，临时产物复查无输出。
- 进展（2026-05-07）：D3 复查后已清理浏览器临时日志、pytest 缓存和 Python 缓存，并复跑 `git diff --check` 通过，临时产物复查无输出。
- 进展（2026-05-07）：D4 文档专项后已清理前端构建产物、pytest 缓存、Python 缓存和浏览器临时日志，并复跑 `git diff --check` 通过，临时产物复查无输出。
- 进展（2026-05-07）：D1 输出类型专项后已清理管理端 `dist`、pytest 缓存、Python 缓存和浏览器临时日志，并复跑 `git diff --check` 通过，临时产物复查无输出。
- 进展（2026-05-07）：B4 ComfyUI 纳管页降噪后已清理管理端 `dist` 和浏览器临时日志，并复跑 `git diff --check` 通过，临时产物复查无输出。
- 进展（2026-05-07）：阶段性候选版本总回归后已清理管理端/测评端 `dist`、pytest 缓存、Python 缓存和浏览器临时日志，并复跑 `git diff --check` 通过，临时产物复查无输出。
- 进展（2026-05-07）：114 已更新到 `55799885` 并完成线上 smoke；三主业务真实巡检均成功并写入验收记录，Coze 容器到中台工具箱路径已通过 Docker 网关 smoke。严格账单门禁仍有历史 `billingIssues=37`，按后阶段账单框架风险记录，不阻断本轮“Coze 到能力”通路验收。
- 进展（2026-05-07）：计费门禁收口后已清理管理端 `dist`、pytest 缓存、Python 缓存和浏览器临时日志，并复跑 `git diff --check` 通过，临时产物复查无输出。
- 进展（2026-05-08）：定期巡检和文档入口检查专项后已清理 pytest 缓存、Python 缓存，并复跑 `git diff --check` 通过；入口断链脚本和健康巡检脚本语法检查均通过。
- 进展（2026-05-08）：管理端信息架构二轮页面走查后已清理管理端 `dist`、pytest 缓存和 Python 缓存，并复跑 `git diff --check` 通过。
- 进展（2026-05-08）：能力目录上线总览专项后已清理管理端 `dist`、浏览器临时日志、pytest 缓存和 Python 缓存，并复跑 `git diff --check` 通过，临时产物复查无输出。
- 进展（2026-05-08）：API 开放页最短接入路径专项后已清理管理端 `dist`、浏览器临时日志、pytest 缓存和 Python 缓存，并复跑 `git diff --check` 通过。
- 进展（2026-05-08）：业务能力页主业务结论专项后已复跑管理端 `lint/build` 和浏览器走查；后续收尾已清理本地 dev server、后端临时服务、管理端 `dist`、浏览器临时日志和缓存，并复跑 `git diff --check` 通过。
- 进展（2026-05-08）：能力调用页排障总览专项已复跑管理端 `lint/build` 和浏览器走查；已清理本地 dev server、管理端 `dist`、浏览器临时日志和缓存，并复跑 `git diff --check` 通过。
- 进展（2026-05-08）：能力评测与 ComfyUI 资源闭环专项已复跑管理端 `lint/build`；本地走查两页关键文案可见，已清理构建产物、浏览器临时日志和缓存，并复跑 `git diff --check`。
- 进展（2026-05-08）：下个版本候选回归已完成：后端全量测试 `456 passed, 24 warnings`，管理端 `lint/build` 通过，测评端 `lint/build` 通过且无 Vite 大包体警告；已清理管理端/测评端 `dist`、pytest 缓存、Python 缓存和浏览器临时日志，并复跑 `git diff --check` 通过。
- 进展（2026-05-08）：测评端通用类辅助工具专项已复跑 `test_eval_workflow_presentation.py`、`test_eval_workflow_response_metadata.py`、`test_podi_release_smoke.py`，合计 52 passed；测评端 `lint/build` 通过且无 Vite 大包体警告；已清理测评端 `dist`、pytest 缓存、Python 缓存和浏览器临时日志，并复跑 `git diff --check` 通过。
- 进展（2026-05-08）：新增 `scripts/package_release_archive.py` 作为手工补丁/静态产物固定打包入口，归档时去除 macOS 扩展属性和 AppleDouble 文件，并统一文件时间；已在 114 上试解包确认不再出现扩展属性或未来时间警告。
- 进展（2026-05-08）：管理端“操作闭环”组件专项已复跑 `npm run lint` 和 `npm run build`；本地后端和管理端联动走查 API 开放、能力目录、业务能力、能力调用四页，控制台 error/warning 为 0；收尾清理临时日志、构建产物和浏览器临时文件后复跑 `git diff --check`。
- 进展（2026-05-08）：模型弹药库和运行线路“操作闭环”专项已复跑管理端 `npm run lint` 和 `npm run build`；本地后端和管理端联动走查两页，控制台 error/warning 为 0；收尾清理临时日志、构建产物和浏览器临时文件后复跑 `git diff --check`。
- 进展（2026-05-08）：账号权限和账单框架“操作闭环”专项已复跑管理端 `npm run lint` 和 `npm run build`；本地后端和管理端联动走查两页，控制台 error/warning 为 0；已清理临时日志、构建产物和浏览器临时文件，并复跑 `git diff --check`。
- 进展（2026-05-09）：调度监控和系统配置“操作闭环”专项已复跑管理端 `npm run lint` 和 `npm run build`；本地后端和管理端联动走查两页，控制台 error/warning 为 0；已清理临时日志、构建产物和浏览器临时文件，并复跑 `git diff --check`。
- 进展（2026-05-09）：高级编排和调度事件“操作闭环”专项已复跑管理端 `npm run lint` 和 `npm run build`；本地后端和管理端联动走查两页，控制台 error/warning 为 0；已清理临时日志、构建产物和浏览器临时文件，并复跑 `git diff --check`。
- 进展（2026-05-09）：历史密钥仓库和路由策略“操作闭环”专项已复跑管理端 `npm run lint` 和 `npm run build`；本地后端和管理端联动走查两页，控制台 error/warning 为 0；已清理临时日志、构建产物和浏览器临时文件，并复跑 `git diff --check`。
- 进展（2026-05-09）：业务 API 接入检查专项已复跑管理端 `npm run lint` 和 `npm run build`；本地后端和管理端联动走查 API 开放页，控制台 error/warning 为 0；已清理临时日志、构建产物和浏览器临时文件，并复跑 `git diff --check`。
- 进展（2026-05-09）：候选版本管理端 10 入口走查后已清理本地 dev server、后端临时服务、浏览器临时文件和缓存，并复跑 `git diff --check`。
- 验收：提交前不夹带构建产物和缓存。

29. `done` G4 完成项必须标记
- 目标：每完成一个编号，更新 TODO 状态和进展，不靠聊天记录记忆。
- 当前进展：A1、A2、A3、A4、B1、B2、B3、B4、C1、C2、C3、C4、C5、D1、D2、D3、D4、E1、E2、E3、E4、F1、F2、F3、G1、G2、G3、G4 已按本轮真实完成情况标记；2026-05-08 定期巡检报告固化、文档入口断链检查扩展、管理端信息架构二轮页面走查、能力目录上线总览、API 开放最短接入路径、业务能力页主业务结论、能力调用页排障总览、测评端通用类辅助工具专项已补进 A1/C2/B3/B4/B5/F1/G3/项目清理记录；B5 作为常态化要求继续随每次前端改动记录。
- 验收：用户随时打开本文件能看到真实进度。

## 当前执行单（2026-05-09 候选版本收口）

说明：2026-05-07 细化版的实质编号已经基本完成；本节接管下一批开发，目标是把当前本地 7 个提交收口成可上线候选版本，而不是继续零散堆功能。

30. `done` H1 业务 API 接入前检查显性化
- 目标：业务方接入前能在管理端直接看到账号范围、业务权限、路由预览、回调/轮询和错误处理五个必查项。
- 已完成（2026-05-09）：`API 开放`页新增“业务方开通前检查”，避免只给接口地址却漏掉开通边界和上线前验证。
- 验证（2026-05-09）：管理端 `npm run lint` 和 `npm run build` 通过；本地浏览器走查 `API 开放` 页，确认“先开通业务方身份 / 确认可调用业务 / 先跑路由预览 / 回调和轮询都保留 / 提前接好错误处理”可见，控制台 error/warning 为 0。
- 验收：页面能直接说明业务方不选模型/工作流，平台侧负责版本、路由、调度、回填和错误解释。

31. `done` H2 管理端候选版本页面走查
- 目标：按总览、业务能力、API 开放、模型弹药库、能力目录、能力调用、ComfyUI 纳管、路由策略、账号权限、账单框架十个入口做阶段性走查。
- 已完成（2026-05-09）：本地浏览器批量走查 10 个入口，均命中对应首屏关键文案；最近一次控制台 error/warning 为 0。
- 验收：无新增控制台 error/warning；每个入口首屏能回答“当前状态、风险、下一步”。

32. `done` H3 候选版本本地总回归
- 目标：在准备更新前复跑后端关键测试、管理端构建、测评端构建、文档入口检查和 `git diff --check`。
- 已完成（2026-05-09）：后端全量测试 `468 passed, 24 warnings`；管理端 `npm run build` 通过；测评端 `npm run lint` 与 `npm run build` 通过且无 Vite 大包体警告；文档入口检查通过；Alembic 单 head 为 `20260504_add_package_catalogs`；已清理构建产物、缓存和浏览器临时文件，并复跑 `git diff --check` 通过。
- 验收：输出真实测试结果，不写未执行闭环。

33. `done` H4 更新前交付包和线上验证清单
- 目标：推送前整理本地提交范围、需要更新的服务器、线上 smoke、三主业务巡检和 Coze 工具箱抽测清单。
- 已完成（2026-05-09）：新增 `docs/testing/RELEASE_CANDIDATE_2026-05-09_ADMIN_FLOW.md`，明确本次候选版本范围、10 个本地提交、本地验证结果、需要更新的服务器、线上验证清单和回滚口径。
- 验收：我明确给出“可以更新”的信号后再进入线上更新。

## 当前执行单（2026-05-10 稳定性推进）

说明：候选版本已在 114 更新。本节接管下一批开发，优先把线上运维中暴露出的重复动作固化成工具和门禁，避免靠临时命令排查。

34. `done` I1 ComfyUI 重启后自检固化
- 目标：158 / 233 重启后，不靠人工临时 curl 判断服务是否恢复；提供无成本、可复跑、可留档的节点健康检查。
- 范围：检查每台 ComfyUI 的 `/system_stats`、`/queue`、`/object_info`，并可选检查 114 中台 `/api/coze/podi/comfyui/queue-summary` 是否已纳入路由。
- 已完成（2026-05-10）：新增 `backend/scripts/check_comfyui_node_health.py`，支持默认检查 158/233，也支持 `--executor id=url` 指定节点，输出 PASS/FAIL 和 JSON 报告。
- 已完成（2026-05-10）：补充专项单测 `backend/tests/test_check_comfyui_node_health.py`，覆盖节点参数解析、系统/队列/节点清单摘要、缺 GPU/缺节点阻断和中台路由汇总阻断。
- 已完成（2026-05-10）：`docs/release-preflight.md` 与 `docs/comfyui/README.md` 已补重启后自检命令和使用边界。
- 验证（2026-05-10）：158 重启后直连检查通过，`117.50.80.158:8079` 返回 ComfyUI `0.12.0`、RTX 5090、队列 0、节点 3230；114 上完整检查通过，中台 `queue-summary` 显示容量 20、空闲 20、supported 2、blocked 0。
- 验收：脚本不提交真实生图任务；能输出 PASS/FAIL 和 JSON 报告；文档说明何时使用、检查什么、不能替代什么。

35. `done` I2 管理端补充 ComfyUI 重启后检查入口
- 目标：运维同学在管理端 ComfyUI 纳管页就能看到重启后该先做什么，不再依赖翻文档或临时问命令。
- 已完成（2026-05-10）：`podi-admin-web` 的 ComfyUI 纳管页顶部新增“重启后检查”提示，明确检查命令、检查范围和使用边界。
- 验收：页面只暴露操作说明，不在前端直接执行服务器命令；真实检查仍由后端脚本或线上终端完成。

36. `done` I3 158 宕机后任务偏向 4090 排查
- 背景：2026-05-11 发现最近 ComfyUI 任务全部命中 4090 机器（117.50.216.233），怀疑 5090 机器（117.50.80.158）未参与路由。
- 排查结论（2026-05-11）：114 journal 显示 11:09-15:58 多次访问 `executor_comfyui_pattern_extract_158` 的 `/queue` 和 `object_info` 返回 `Connection refused`，健康巡检持续报 158 执行节点不可访问；该时间段内中台按保底策略自动避开 158，最近 24 小时 51 条 ComfyUI 任务全部命中 233。
- 恢复验证（2026-05-11）：158 重启后无成本自检通过，`117.50.80.158:8079` 返回 ComfyUI `0.12.0`、RTX 5090、队列 0、节点 3230；233 同时可用。线上执行节点表中 158/233 均为 active、并发 10、权重 1。
- 路由验证（2026-05-11）：后端无成本选择预演显示，158 恢复后印花提取、图裂变、扩图等能力已经能重新选到 158；16:00 后 journal 暂未再出现 158 连接拒绝。
- 实跑验证（2026-05-11）：按业务 API 真实跑花纹提取、图裂变、扩图各 1 条，均成功且都有输出回填，实际执行节点均为 `executor_comfyui_pattern_extract_158`；随后强制指定 233 跑 `comfyui_flux2_klein_9b_outpaint` 1 条，成功且返回 `executor_comfyui_seamless_117` 证据。最终队列汇总显示两台均空闲、route evidence covered=2、无诊断项。
- 后续观察：下一轮真实业务后复查 `/api/coze/podi/comfyui/queue-summary`，若 158 仍无新命中，再进入调度策略排查；当前不需要因本次偏流调整能力结构。

37. `done` I4 业务分类与接口形态标准定稿
- 背景：中台接口逐步扩张后，按 Coze、原子能力、测评 workflow、厂商模型分散展示会让业务方无法判断“该接哪个接口”。测评端“通用类”也需要继续拆细。
- 已完成（2026-05-11）：新增 `docs/standards/business-interface-taxonomy.md`，明确业务分类字典、原生业务 API / Coze 工具箱 / 原子能力 API / 管理端 API / 测评 API 的分层关系，以及测评端“通用类”拆分标准。
- 已完成（2026-05-11）：`docs/api/INDEX.md`、`docs/README.md`、`docs/standards/interface-consistency.md` 已接入该标准，后续新增接口必须先定业务分类，再定接口形态。

38. `done` I5 业务分类标准首轮落到前端
- 背景：管理端和测评端页面仍偏“技术列表”，测评端旧五类和“通用类”会让用户分不清功能差异。
- 已完成（2026-05-11）：测评端分类改为业务字典，旧“花纹提取类 / 图延伸类 / 四方连续类 / 通用类”会归并或拆分到“花纹提取 / 图裂变 / 扩图 / 连续图 / 抠图 / 图像融合 / 图像增强 / 图像理解 / 文本与提示词 / 生视频 / 平台工具”。
- 已完成（2026-05-11）：测评端首页只固定展示核心业务，其他分类有内容才出现；功能卡片弱化 workflow id、执行面、追踪等底层信息，改为默认收起“查看底层链路”。
- 已完成（2026-05-11）：管理端 API 开放页新增“按业务找接口”，同一业务下展示原生 API、Coze 工具箱和原子能力数量；原大段接入说明压缩成检查标签，降低首屏噪音。
- 验证（2026-05-11）：`podi-eval-web npm run lint` / `npm run build` 通过；`podi-admin-web npm run lint` / `npm run build` 通过；本地浏览器走查了管理端 API 开放页和测评端首页结构。由于本地 8099 后端未启动，浏览器里出现接口加载失败提示，但不影响本次展示层校验。

39. `done` I6 管理端能力评测页旧分类清理
- 背景：管理端内部“能力评测”页仍保留旧的五类分类和“通用类”默认值，会把新增工作流继续导向旧体系。
- 已完成（2026-05-12）：新增 `podi-admin-web/src/pages/AbilityEvaluation/evalBusinessCategories.ts`，管理端能力评测页与侧栏共用业务分类顺序和旧分类兼容映射。
- 已完成（2026-05-12）：新增工作流默认分类从“通用类”改为“图裂变”，分类下拉改为完整业务字典；侧栏标题从“能力分组”改为“业务分组”，默认不再直接暴露 workflow id。
- 已完成（2026-05-12）：同步更新 `docs/eval/eval-platform.md` 与 `docs/ABILITY_EVALUATION.md`，把旧分类说明改为业务分类说明。
- 验证（2026-05-12）：`podi-admin-web npm run lint` 通过。

40. `done` I7 能力目录业务分类落地
- 背景：能力目录页面已经降噪，但新增/编辑能力的分类下拉仍以“图片生成 / 图像处理”等技术分类为主，容易继续产生“通用能力桶”。
- 已完成（2026-05-12）：管理端能力分类选项改为业务字典优先，新增花纹提取、图裂变、扩图、连续图、抠图、图像融合、图像增强、图像理解、文本与提示词、生视频、平台工具；旧分类保留为“旧分类/待归类”兼容项。
- 已完成（2026-05-12）：能力编辑弹窗增加分类说明，明确新增能力先按业务意图归口，不按厂商、模型或 ComfyUI 工作流归口。
- 已完成（2026-05-12）：能力目录新增“按业务分类看能力”，按分类展示总数、可接业务、需处理、暂不能接和样例能力，旧分类会在页面上暴露出来，后续便于逐步清理。
- 进展（2026-05-12）：AI 团队 GPT Image 2 + VL 图裂变交付包已接入为 `biz_fission_v2_openai_gpt_image2_vl` 灰度候选版本；业务版本号 `gpt-image2-vl-v1`，流程为 VL 图案结构卡 -> 中台提示词编译 -> `openai_gpt_image_2_edit`，当前不自动替换默认图裂变。
- 进展（2026-05-13）：AI 团队 GPT Image 2 受控裂变 v2 交付包已接入为 `biz_fission_v5_openai_gpt_image2_controlled`，业务版本号 `gpt-image2-vl-v2`；核心变更为 VL 只输出客观识别卡，中台执行图案类型路由、定量提示词编译、固定单图输出和质量门禁审计元数据。测评端新增 `business_fission_gpt_image2_vl_v2`，并压缩业务任务历史列表返回，避免 GPT Image2 功能页打开时因大 JSON/长 prompt 卡顿。
- 进展（2026-05-14）：修正 GPT Image 2 图片编辑默认尺寸策略，`size` 默认改为 `auto` 并在 `auto/未指定` 时按原图尺寸回填最终 OSS 图片；只有业务方明确传固定尺寸时才改变输出画布。
- 验证（2026-05-12）：`podi-admin-web npm run lint` / `npm run build` 通过；`git diff --check` 通过。

41. `done` I8 114 控制面发布 SOP 固化
- 背景：近期每次上线都在构建、打包、上传、重启、确认版本、线上 smoke 之间反复尝试，发布流程缺少唯一入口和自动编排。
- 已完成（2026-05-12）：新增 `docs/standards/release-sop.md`，明确 114 控制面发布原则、标准命令、门禁、线上验证、117/233/vendor-api-ops 更新边界、失败处理、回滚和发布记录模板。
- 已完成（2026-05-12）：新增 `scripts/release_114_control_plane.sh`，把源检查、关键测试、前端 lint/build、干净打包、上传 114、保留线上 `.env/.venv`、迁移、服务重启、版本标记、远端健康检查和 smoke 固化为一个入口。
- 已完成（2026-05-12）：`scripts/package_release_archive.py` 默认排除 `.env`、`.venv`、`node_modules`、`__pycache__`、`.pytest_cache`、`.ruff_cache`、`.pyc/.pyo` 等非发布内容，降低手工打包污染风险。
- 已完成（2026-05-12）：`README.md`、`docs/README.md`、`docs/archive/202605/deploy-checklist.md`、`docs/standards/self-check-sop.md`、`docs/standards/issue-improvement-log.md` 均已指向统一发布 SOP。
- 验证（2026-05-12）：`bash -n scripts/release_114_control_plane.sh`、`python3 -m py_compile scripts/package_release_archive.py`、`python3 scripts/check_doc_entry_references.py`、`python3 -m pytest backend/tests/test_release_archive_packaging.py -q` 均通过。

下一步：暂停新增功能，先执行 2026-05-13 中台走查后的稳定性修复清单。

## 当前执行单（2026-05-13 中台走查收口）

说明：本节接管当前开发优先级。详细走查记录见 `docs/strategy/mid-platform-walkthrough-2026-05-13.md`。本轮不做新能力，先修复这两天暴露的中台逻辑、数据库、限流、日志收口和交付材料问题。

42. `done` J1 数据库查询与索引整改
- 背景：测评端 GPT Image 2 + VL 控制版曾触发 `BUSINESS_RUN_GET_FAILED`；线上直接查询最近图裂变业务记录也能复现 MySQL `Out of sort memory`。
- 范围：`business_runs`、`business_run_steps`、业务运行列表/详情、测评端业务运行查询。
- 已完成：
  - 新增 migration `20260513_add_business_run_query_indexes`，补 `business_runs(business_key, created_at)`、`business_runs(business_key, status, created_at)`、`business_run_steps(run_id, step_order, id)`。
  - 业务运行列表、分类统计、用量汇总改为先查轻字段 ID，再按需加载大 JSON。
  - 运行步骤改为按 `step_order, id` 的窄字段顺序加载，避免排序阶段拖入大 JSON。
- 验收：本地 Alembic 单 head、相关后端回归测试通过；线上需在本版本上线并执行 migration 后复核。

43. `done` J2 火山 VL 限流、退避重试与错误文案
- 背景：2026-05-13 16:28:48，`火山 · Doubao-Seed-2.0-lite VL` 触发上游 `RequestBurstTooFast / TooManyRequests`，能力日志 `40146`。
- 结论：这是上游模型请求突增保护，不是图片参数或 Key 错误。
- 已完成：
  - backend 调用 vendor-api-ops 时识别 `RequestBurstTooFast/TooManyRequests/429/5xx` 等可重试结果。
  - Doubao VL 默认最多 3 次带抖动退避重试，OpenAI/KIE 默认最多 2 次。
  - 测试覆盖 vendor-api 返回限流结果时的重试判定。
- 说明：更精细的 provider/model 全局平滑限速可放到 vendor-api-ops 调度增强，不阻塞本轮收口。

44. `done` J3 能力日志 pending 残留收口
- 背景：线上发现 `volcengine_doubao_seed_2_0_lite` 仍有少量 `pending` 日志残留，例如 `40159`、`40036`、`40034`。
- 影响：管理端能力调用列表容易让用户误以为任务仍在“输出填写中”。
- 已完成：
  - 能力日志列表读取前执行 pending reconciliation。
  - 超过阈值仍 pending 的日志按关联 `ability_task` 终态补齐。
  - 无对应任务的历史残留标记失败，并写入 `ABILITY_LOG_STALE_PENDING`。
- 验收：后端单测覆盖“从已完成 ability_task 回填 pending 日志”。

45. `done` J4 文字/VL/结构化结果展示口径修正
- 背景：文字类、VL 类能力在回调阶段显示“输出填写中”，容易被误解为异常。
- 已完成：
  - 管理端能力调用、能力测试、业务运行详情中的“回填”改为“结果入库/成功无结果”等通用结果语义。
  - 测评端运行阶段把回调文案改为“等待结果/结果处理中/结果已完成/结果失败”。
- 验收：管理端和测评端 lint 通过。

46. `done` J5 ComfyUI 图裂变质量样本包
- 背景：需要把本轮 ComfyUI 测试的原图、结果图、VL 内容和过程信息交给 ComfyUI 团队优化工作流质量。
- 已完成：生成 `deliverables/comfyui_fission_quality_pack_20260513.zip`。
- 内容：30 条 `comfyui-vl-control-v2` 成功样本、60 张原图/结果图、`summary.csv`、完整过程 JSON、VL 控制卡、ComfyUI 参数。
- 节点分布：`executor_comfyui_seamless_117` 14 条，`executor_comfyui_pattern_extract_158` 16 条。

47. `done` J6 样本包导出固定化
- 背景：本次样本包是临时导出，后续 AI/ComfyUI 团队会反复需要类似材料。
- 计划：增加脚本化导出能力，支持按业务版本、时间范围、状态、执行节点导出原图、结果图、参数、VL 内容和过程信息。
- 已完成：
  - 新增 `backend/scripts/export_business_sample_pack.py`，支持按业务、版本、时间、状态、来源、执行节点筛选导出。
  - 导出包固定包含 `summary.csv`、`manifest.json`、单 run 的 `run.json/process.json/vl.json/urls.json`，可选下载原图和结果图。
  - 新增 `docs/standards/business-sample-pack-export.md`，沉淀使用命令、输出结构和交付规则。
  - 敏感字段会脱敏，避免把 token/API Key 混入协作交付包。
- 验收：不再靠临时 SQL 和临时脚本手工打包。

48. `done` J7 三个业务接口交付材料拆分
- 背景：业务方需要直接拿走能运行的交付材料；三个接口放在一起容易看不懂。
- 范围：GPT Image 2 + VL 控制版、ComfyUI VL 控制卡版、裂变质量评估。
- 计划：每个接口独立目录，包含 `README.md`、请求示例、响应示例、错误码和轮询示例；Key 不进仓库，只进入临时交付包或单独安全通道。早期调试脚本仅作为可选工具，不作为正式交付口径。
- 已完成：
  - 裂变评分补齐业务包装入口 `POST /api/business/fission-evaluate/runs`，统一业务 Key、`runId` 和 `/api/business/runs/get` 轮询。
  - 新增交付包 `/Volumes/MAC 1/comfyui_zhuanjia/deliverables/podi_fission_delivery_20260514_v3.zip`。
  - 三个目录分别包含独立 README、`.env.example`、调试脚本、请求示例、成功响应、失败响应和参数说明；2026-05-15 v4 已移除默认脚本交付，改为完整 JSON 示例。
  - 真实 Key 不进仓库；交付包只保留占位符，Key 通过管理端或单独安全通道交付。
- 验收：业务开发拿到任一接口目录即可独立运行，不需要读其他接口说明；三个接口都采用业务 API Key + `runId` 轮询口径。

49. `done` J8 管理端第一轮视觉走查与误导提示修正
- 背景：中台页面存在“看起来很恐怖但不知道怎么处理”的错误展示，包括本地 systemd 不可读、接口加载超时、Key 来源判断不清、能力结果状态措辞混乱。
- 已完成：
  - 总览页将本地非 systemd 环境标为“本地不可读”，不再误算成线上巡检异常。
  - 全局模块加载失败改成“部分模块暂时没加载完整”，技术详情折叠展示。
  - 模型弹药库明确显示 Key 来源：中台密钥池、环境变量兜底或未配置。
  - 能力调用列表统一为“结果入库/成功无结果”，减少非图片能力误判。
- 后续：整体信息架构、页面视觉、错误处理建议继续归入前端整改阶段，不在本轮急上线。

50. `done` J9 每日早检流程固化
- 背景：每天开始开发前需要先查前一天业务、能力、测评和 API Key 是否有报错，并把数据按固定格式导出。
- 已完成：
  - 新增 `backend/scripts/morning_ops_check.py`，导出 `summary.md`、raw JSON、CSV 和 zip 包。
  - 新增 `docs/standards/morning-ops-check.md`，明确早检先于当天 TODO，且早检只读，不重启服务、不改配置。
  - `docs/README.md` 已加入每日早检入口。
- 2026-05-14 早检结论：
  - 2026-05-13 业务运行 188 条，业务失败 0 条。
  - 能力异常 6 条：4 条历史 pending 收口、1 条火山 VL 突增保护、1 条裂变评分巡检缺双图参数。
  - 测评异常 2 条：1 条裂变评分巡检误报，1 条测评等待超时但底层业务后续成功回图。
- 验收：早检数据包已生成在 114：`reports/morning-check/20260514.zip`。

51. `done` J10 测评等待超时与业务最终成功的状态归并
- 背景：`图裂变 · ComfyUI VL 控制卡版` 测评运行 `fa74aaffb62b41a085c5b443481c6675` 超过 30 分钟被标失败，但关联业务运行 `4c28df61ee904297a8c9d04f849bd519` 随后成功并回图。
- 计划：
  - 测评端和后端查询运行详情时，如果 `BUSINESS_RUN_TIMEOUT` 关联的业务 run 后续已成功，应归并显示为“后台已完成/测评等待超时后恢复”。
  - 运行列表保留原始测评超时证据，但不再把底层已成功的任务当成业务失败。
  - 评测巡检报告增加该类问题分类，避免和真实业务失败混淆。
- 已完成：
  - 后端对 `BUSINESS_RUN_TIMEOUT` 测评记录增加自动恢复：后台循环和运行详情查询都会重新核对关联 business run，业务已成功时回写测评成功和结果图。
  - 测评端列表接口和“带最新评分”的列表接口也会在返回前执行同样的恢复检查，避免首页/功能页继续显示旧失败记录。
  - 前端把残留 `BUSINESS_RUN_TIMEOUT` 错误压缩成“测评等待超时，可刷新恢复”的中文提示，不再展示内部字典原文。
  - 测评巡检新增 `BUSINESS_RUN_TIMEOUT_RECOVERED` 分类，该类记录不再阻断巡检结论。
  - 补充业务 run id 提取、列表恢复和巡检归类单测。
- 验收：用户在测评端能看到最终业务结果图和“等待超时后恢复”的解释。

52. `done` J11 项目 / 数据库 / OSS 清理审计固化
- 背景：项目目录、数据库历史状态和 OSS 测试对象会持续堆积，影响发版、排查和业务判断。
- 已完成：
  - 新增 `backend/scripts/audit_cleanup_candidates.py`，默认只读审计项目文件、数据库脏状态和 OSS 候选对象。
  - 新增 `docs/standards/cleanup-governance.md`，明确本地可删、数据库归档、OSS 删除的边界和流程。
  - 本地低风险产物已清理：构建产物、缓存、Playwright 日志、`.DS_Store` 和本地日志；复扫候选为 0。
  - 数据库审计未发现长时间未收口业务/能力/测评记录、孤儿业务步骤或过期仍 active Key。
  - OSS 抽样发现 `test/` 前缀候选对象 880 个，约 516MB；暂不直接删除，后续按小批量清理流程处理。
- 验收：清理报告已生成在 `reports/cleanup-audit/full-audit-with-oss-sample-v2`，本地复扫报告为 `reports/cleanup-audit/post-local-clean`。

## 历史执行单（2026-05-14 下一轮稳定与产品化）

说明：J 系列稳定性收口完成后，先补“清理可执行闭环、发布验证、页面降噪、测评巡检”四类工程化事项；暂不新增复杂业务能力。

53. `done` K1 OSS 清理复核清单
- 背景：首轮审计发现 OSS `test/` 前缀存在 880 个候选对象，不能直接删除，但需要形成可人工复核、可小批量执行的清单。
- 已完成：
  - `backend/scripts/audit_cleanup_candidates.py` 增加 OSS 候选分组汇总，按对象前缀和月份聚合。
  - 审计报告新增 `raw/oss_candidate_groups.json`。
  - 审计报告新增 `oss_delete_review_manifest.csv`，默认 `decision=review_required`、`delete_allowed=no`，只作为人工确认清单。
  - `docs/standards/cleanup-governance.md` 补充 OSS 复核流程。
- 验收：脚本仍然只读，不会删除 OSS 对象；首批删除仍必须人工确认且不超过 100 个。

54. `done` K2 候选版本上线前总回归
- 目标：把 J/K 已完成修复作为一个候选版本执行本地总回归，明确哪些测试已跑、哪些线上项必须等部署后验证。
- 范围：后端重点测试、测评端 lint/build、管理端必要 lint/build、文档引用检查、`git diff --check`、清理复扫。
- 已完成：
  - 后端重点回归通过，54 passed，12 warnings。
  - 管理端和测评端 `npm run lint`、`npm run build` 均通过。
  - 文档入口引用检查、Alembic 单 head、`git diff --check` 均通过。
  - 构建产物和测试缓存已清理，排除依赖目录后本地脏产物复扫为空。
  - 新增真实回归记录 `docs/testing/RELEASE_CANDIDATE_2026-05-14_STABILITY.md`。
- 验收：已明确本地通过与线上待验证边界，没有把未部署验证写成已通过。

55. `done` K3 线上更新后 SOP 复核
- 目标：部署 114 后按 SOP 复核健康、版本、业务 API、Coze 工具箱、测评端关键功能和后台日志。
- 范围：114 控制面；117.50.80.158 / 117.50.216.233 只做 ComfyUI 节点健康和路由证据确认。
- 已完成（2026-05-15）：114 控制面发布到 `d6c23053`；发布脚本完成 source gate、后端测试、管理端/测评端 lint、远端部署和 smoke。
- 线上 smoke 结果：`/health` 200、Coze OpenAPI server 为 `http://172.17.0.1:8099`、内部 `tasks/get` 返回 `TASK_NOT_FOUND`、ComfyUI 双节点容量 20 且空闲 20、公开测评目录 26 条、内部目录 33 条、三大业务 route-preview 正常。
- 验收：本次发布验证通过；漏检类问题后续仍按测评端业务回归清单继续复测。

56. `doing` K4 管理端视觉走查第二轮
- 目标：继续减少“看起来很恐怖但不知道怎么处理”的页面信息。
- 范围：总览、能力调用、业务能力、API 开放、模型弹药库、ComfyUI 纳管。
- 进展（2026-05-15）：接口调用页补充“接口交付状态”首屏结论，把可交付/需复核/暂缓交付、主要风险和下一步动作聚合展示；原始检查项改为折叠，避免用户先看到表格和技术细节。
- 进展（2026-05-15）：能力调用列表的错误摘要改为可读错误映射，补充内部接口、第三方密钥、数据库排序内存、上游请求保护等常见错误提示，避免直接展示恐怖的原始异常。
- 进展（2026-05-15）：能力调用列表默认从全量历史改为近 6 小时窗口，后端 `/api/admin/abilities/logs` 和单能力日志接口新增 `sinceHours`；管理端提供近 1/6/24/72 小时、近 7 天和全部历史筛选，避免 3 万多条历史记录拖慢首页和自动刷新。
- 进展（2026-05-15）：能力调用列表改为“列表轻量、详情再拉原始 JSON”，默认列表不再返回 `request_payload/response_payload/result_assets/callback_payload/callback_response`；本地 30 条列表响应从约 489KB 降到约 42KB，点击详情再请求单条完整排障内容。
- 进展（2026-05-15）：业务能力页新增“业务入口总控”，按花纹提取、图裂变、裂变评分、扩图逐行展示业务方调用入口、当前默认版本、近窗口调用、最近结果和下一步动作，减少在多个卡片之间来回找信息。
- 进展（2026-05-15）：ComfyUI 服务器纳管和任务诊断页补充机器身份口径，`117.50.80.158` 明确显示为 `5090 机器 · 158`，`117.50.216.233` 明确显示为 `4090 机器 · 233`，主服务器选择、总览、单机卡片和队列线路统一展示，避免排查路由时只看到“117”。
- 验收：每页首屏能回答“当前状态、主要风险、下一步动作”；技术错误默认折叠，不把原始异常作为主文案。

57. `doing` K5 测评端业务回归清单
- 目标：把测评端可交付给内部测试的功能形成固定清单，避免靠临时点击发现页面错误。
- 范围：两个图裂变业务接口、裂变评分、主线 Coze 工作流、非图片/VL/文字结果展示。
- 进展（2026-05-15）：本次先完成对外业务接口契约和交付包严格检查；测评端完整视觉/功能回归明天继续，重点补“每功能上线检查”和“结果图浏览交互”。
- 进展（2026-05-15）：业务方真实调用后已完成链路排查，报告见 `docs/testing/20260515-business-api-live-chain-check.md`；新 `comfyui-vl-control-v2` 接口 21/21 成功，发现业务方轮询偏密和 233 缺 `String` 节点两个隐患。
- 进展（2026-05-16）：线上视觉走查发现测评端工具详情页慢加载和结构化结果误判；已从根因修复：历史记录分页为每页 20 条并移除重复拉取循环，后端列表增加轻量结果类型摘要 `result_output_kind/result_has_output`，避免结构化 JSON 能力被显示成“成功无回填”。记录见 `docs/testing/20260515-online-visual-walkthrough.md`。
- 验收：形成可复跑清单，记录每项输入、预期输出、轮询方式、失败归因和页面截图或报告路径。

58. `done` K6 对外业务接口契约硬化与交付包 v4
- 背景：业务方反馈一次性返回 JSON 过重；同时今天 ComfyUI 参数错传说明上线检查没有逐功能卡死，正式业务接口也需要重新严查。
- 已完成：
  - `/api/business/fission/runs` 固定单图口径：两个裂变版本统一为一个请求、一张图、一个 `runId`。
  - 后端忽略外部批量字段：`count/n/batch_size/batchSize/generateCount/variantCount`，避免业务方误以为一次请求可以生成多图。
  - 查询响应默认轻量化，`expectedImageCount=1`；底层步骤和大 JSON 只在排障模式返回。
  - 三接口交付包 v4 已生成：`deliverables/podi_fission_business_delivery_20260514_v4.zip`。
  - 交付规范同步为“默认 JSON 样例，不默认交付 Python 脚本”。
- 验收：
  - `python3 -m pytest backend/tests/test_business_api_contract.py -q`：17 passed。
  - `python3 -m pytest backend/tests -q`：538 passed。
  - JSON 示例格式校验通过。
  - `python3 scripts/audit_external_api_boundaries.py --summary`：未分类接口 0。

59. `doing` K7 逐功能上线检查表
- 背景：业务方真实调用链路已通，但今天继续暴露出“基础健康通过不等于每个工作流节点完整”的问题。
- 目标：每个对外功能上线前都要逐项检查接口参数、默认值、枚举、测评端文案、实际 payload、执行节点、必需 ComfyUI 节点、OSS 回填、查询返回体和错误展示。
- 第一批范围：GPT Image 2 + VL 受控裂变、ComfyUI 颜色锁定裂变、裂变评分、旧四方连续裂变。
- 进展（2026-05-15）：新增 `docs/standards/per-feature-release-checklist.md`，把接口入口、入参字段、参数映射、默认值、执行节点、节点依赖、结果回填、页面展示、错误路径和交付材料固定成逐功能检查项；管理端“接口调用”页同步露出第一批四个功能的检查表。
- 进展（2026-05-15）：`podi_release_smoke.py` 增加 `per_feature_release_checklist` 门禁，防止后续删漏逐功能检查文档或漏掉 `String` 这类 workflow 节点差异提醒。
- 验收：形成可复用表格，并能发现 233 缺 `String` 这类 workflow 级节点差异。

60. `doing` K8 业务方轮询频率治理
- 背景：21 个新裂变任务产生 1071 次查询，部分 run 3 分钟内轮询 70+ 次。
- 目标：先在交付反馈中提醒按 `retryAfterSeconds` 或 5~10 秒轮询；再在管理端 Key 使用记录里增加“轮询偏密”提示。
- 进展（2026-05-15）：管理端“接口调用中心”按窗口计算轮询/提交比例，偏高时直接提示业务方按 `retryAfterSeconds` 或 5-10 秒间隔轮询；按 `runId` 聚合表继续显示 `POLLING_TOO_FREQUENT`。
- 验收：下一轮业务方测试时，同等任务量查询次数明显下降，且无查询错误。

61. `done` K9 233 ComfyUI 工作流级节点临时规避
- 背景：233 缺 `custom_nodes.comfyui_bmad_nodes` 的 `String` 节点，旧 `flux2_9b_liebian_sifang` 先失败后 fallback 到 158 成功。
- 目标：补齐 233 节点或调整该工作流路由策略；同时把 workflow 所需节点纳入健康检查。
- 进展（2026-05-15）：发布脚本已修复 smoke token 注入问题，远端 `podi_release_smoke.py` 将读取线上 `.env` 的服务 token，从而实际执行 `/api/admin/comfyui/workflow-compatibility`，避免缺节点检查因未鉴权被跳过。
- 进展（2026-05-15）：本地直连 `/object_info` 复核：158 `node_count=3230` 且 `String/KSampler/SaveImage` 均存在；233 `node_count=2094`，`KSampler/SaveImage` 存在但 `String` 仍缺失。队列接口两台均可达且当前 running/pending 为 0。代码门禁已能识别，实际恢复仍需要在 233 同步自定义节点。
- 进展（2026-05-15）：结合 233 安全加固现状，先不为少数低频业务改服务器权限；已将 `sifang_lianxu`、`huawen_kuotu`、`flux2_9b_liebian_sifang` 固定到 5090/158，并禁用这些 workflow 的 233 绑定。高频或耗时较长且 233 已验证正常的业务继续保留双机路由。
- 验收：依赖 `String` 的低频工作流不会再命中 233；如未来恢复双机，必须先确认 233 `/object_info/String` 返回节点信息。

62. `doing` L1 233 ComfyUI 白名单后同构恢复
- 背景：233 曾因 ComfyUI API 暴露被利用执行挖矿代码，后续通过低权限运行、systemd 加固、节点禁用和巡检降低风险；现在端口 `8079` 已改为白名单访问，可以在受控前提下恢复必要自定义节点。
- 原则：这是临时运维任务，不是平台架构改造；不要为单台服务器缺节点继续增加长期特殊路由。优先补齐 233，使 158/233 尽量保持同构。
- 当前已知缺口：`String`、`ComposeRGBAImageFromMask` 以及花纹扩图相关 `Text _O`、`Get Image Size` 需要重新复核；其中 `ComposeRGBAImageFromMask` 仍在 2026-05-16 release smoke 中暴露为 233 warning。
- 执行文档：`docs/comfyui/233-recovery-2026-05-16.md`。
- 进展（2026-05-16）：已确认 233 安全巡检 OK，ComfyUI 仍以低权限用户运行，`custom_nodes` 不恢复为运行时可写。
- 进展（2026-05-16）：已恢复 `comfyui_bmad_nodes`、`ComfyUI-LogicUtils`、`ComfyUI-QualityOfLifeSuit_Omar92`、`masquerade-nodes-comfyui`；未恢复高风险 `srl-nodes`。
- 进展（2026-05-16）：从 233 本机和 114 均确认 `String`、`ComposeRGBAImageFromMask`、`Text _O`、`Get Image Size`、`KSampler`、`SaveImage`、`LoadImage` 可见。
- 进展（2026-05-16）：114 `check_comfyui_node_health.py` 通过，双节点容量 20；release smoke 通过，`comfyui_workflow_compatibility total=16 ok=16 warnings=0 failed=0 servers=2`。
- 进展（2026-05-16）：强制 233 跑通“头部抠像”真实任务，输入图为历史失败样本，输出 OSS 为 `https://podiaidesign.oss-cn-hangzhou.aliyuncs.com/test/abilities/admin-comfyui/20260516/06413e32-1778887853.png`。
- 进展（2026-05-16）：恢复后自然流量已命中 233 并成功回填多条主线裂变，包含 `flux_strong_hq_softstyle_fission_colorlock_v2` ability log `41244` 和多条 `flux_strong_hq_softstyle_fission` success 记录。
- 进展（2026-05-16）：已强制 233 跑通 `flux2_9b_liebian_sifang`、`huawen_kuotu`、`sifang_lianxu`，输出分别为 `29ffc5e0-1778888248.png`、`0a7ee896-1778888402.png`、`fb9a25be-1778888539.png`。
- 进展（2026-05-16）：代码已移除这三条 workflow 的临时 158-only 限制，恢复 `executor_comfyui_seamless_117` 与 `executor_comfyui_pattern_extract_158` 双机队列路由；待下次部署后生效。
- 待办：
  - [x] 登陆 233 前先确认白名单仍生效，安全巡检最新结果为 OK。
  - [x] 复核 ComfyUI 运行用户、systemd 加固、`custom_nodes` 权限，不回到公网任意写入状态。
  - [x] 从已有节点目录恢复或修补 `comfyui_bmad_nodes`、`ComfyUI-LogicUtils`、`ComfyUI-QualityOfLifeSuit_Omar92`、`masquerade-nodes-comfyui` 等必要依赖。
  - [x] 用 `/object_info` 确认 `String`、`ComposeRGBAImageFromMask`、`Text _O`、`Get Image Size` 等关键节点可见。
  - [x] 回到 114 跑 `check_comfyui_node_health.py`、`podi_release_smoke.py` 和 workflow compatibility。
  - [x] 强制 233 跑旧四方连续裂变、花纹扩图、头部抠像、当前主线颜色锁定裂变，并记录 runId、executor、OSS 输出。
  - [x] 强制 233 测试通过后，再评估是否移除临时 158-only 路由限制。
- 验收：233 安全巡检 OK、关键节点齐全、强制 233 真实业务有 OSS 回填、平台侧没有新增长期复杂分支。

63. `doing` M0 2026-05-20 早检风险收敛
- 背景：2026-05-20 早检发现 GPT Image 2 裂变存在间歇性 `VENDOR_API_CLIENT_FORBIDDEN`，同时本地残留一个连接线上库的 `uvicorn app.main:app --reload --port 8099` 进程。该进程存在消费线上任务和写入线上日志的风险。
- 目标：
  - 防止本地后端误连线上库后消费线上任务。
  - 为 `vendor-api-ops` 增加来源审计，后续白名单拒绝能看到真实来源 IP、路径和调用时间。
  - 对昨日本地回归遗留的 `text_fission queued` 脏记录进行标记或收口。
- 验收：
  - 本地启动后端时，如果连接线上数据库且未显式声明安全模式，不会启动后台任务消费。
  - vendor-api 白名单拒绝日志能给出来源 IP 和接口。
  - 管理端/早检不再把本地回归脏记录误判成线上业务阻塞。

64. `done` M1 图编辑组件型业务能力 v1
- 背景：图编辑定位为“前端组件 + 中台业务 API + GPT Image 2 编辑能力”的完整业务。业务 Key 为 `image_edit`，默认版本为 `gpt-image2-editor-v1`，测评端分类为“图编辑”。
- 今日目标：
  - 完成组件工作台、业务 API、管理端链路、测评端入口和交付文档闭环。
  - 四种模式可真实测试：局部修改、参考图替换、删除修补、补色校正。
  - 尺寸规则、质量档位、轻量/调试返回、错误码和 API Key 边界一致。
- 待办：
  - [x] 复核 `/api/business/image-edit/runs`、`/api/business/image-edit/component-config` 和 `/api/business/runs/get` 的入参、出参、错误码。
  - [x] 测评端“图编辑”改为组件式工作台，支持主图、参考图、选择区域、编辑指令、技能、尺寸、质量和结果查看。
  - [x] 将图编辑交互从测评端主页面抽成 `features/image-edit` 组件边界，便于后续托管组件和源码组件共用。
  - [x] 将图编辑组件样式从测评端全局样式抽到 `features/image-edit/image-edit.css`，避免源码交付依赖全局页面。
  - [x] 对齐图编辑前后端参数枚举，质量档位统一为 `auto/preview/production/premium`，组件保留旧值展示兼容但不再主动提交旧值。
  - [x] 补充源码嵌入最小示例，明确业务方只替换上传函数、提交函数和 API Key 注入方式。
  - [x] 完成本地静态和契约回归：后端关键测试、vendor-api-ops 测试、管理端/测评端类型检查和生产构建均已通过。
  - [x] 补齐图编辑真实样本巡检脚本，支持显式跑四种模式并导出请求、轮询和最终结果。
  - [x] 管理端按 `runId` 串起入口请求、编译步骤、GPT Image 2 调用、OSS 回填、成本和错误；8 条真实样本已通过后台详情接口抽查，均可看到底层能力任务和 abilityLogId。
  - [x] 四个模式各跑至少 2 条真实 GPT Image 2 样本。
  - [x] 导出真实样本记录和内部客户接入文档；真实样本记录见 `docs/testing/20260522-image-edit-real-sample-regression.md`。
- 验收：测评端能完成真实图编辑任务；管理端能看清链路；交付材料足够业务方接入；真实样本可用于模型效果归因。

65. `doing` M0 v0.5 平台打磨：质量复盘闭环
- 背景：v0.5 聚焦“少就是多”的平台打磨，核心是业务流可视化、固定样例复跑、质量复盘、治理建议和运营/运维可执行的监控口径。
- 进展（2026-05-26）：固定样例库已支持 OSS 上传、JSON/CSV 批量导入、dryRun 预检查和版本历史；测评端可只读复用样例。
- 进展（2026-05-26）：同批质量对照已支持 `batchId` 聚合，管理端新增质量治理项编辑/归档和质量结果 CSV 导出，便于运营按样例批次复盘默认版/候选版。
- 待办：
  - [x] 固定样例库维护：上传、导入、历史追溯。
  - [x] 质量治理台账：新增、编辑、归档。
  - [x] 同批质量结果导出：窗口导出、batchId 导出。
  - [x] 完成浏览器走查和本地回归后更新验证结果。
  - [ ] v0.5 完整开发后再统一发布，不单独更新线上版本。
- 验收：运营能按样例批次复盘效果，能力工程师能从治理台账拿到分流/换 LoRA/workflow 的证据，运维能区分链路故障和效果问题。

*最后更新: 2026-06-04*
