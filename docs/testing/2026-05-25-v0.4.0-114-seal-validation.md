# 2026-05-25 v0.4.0 114 封版验证记录

## 结论

本版本可以封版。

- 平台版本：`v0.4.0`
- 封版 commit：`8033c843`
- 线上主机：114.55.0.56
- 封版时间：2026-05-25
- 范围：114 控制面 backend、管理端、测评端、文档和脚本。
- 不更新：4090/5090/ComfyUI 能力机。
- 不调整：扩图路由，不把扩图固定到 4090。

## 代码与发布源

- 本地分支：`main`
- 本地 HEAD：`8033c843`
- `origin/main`：`8033c843`
- GitHub fetch 复核：通过。
- 工作区在封版验证前保持干净；本文件为封版后记录。

说明：此前一次 `release_source_preflight` 因 GitHub 网络返回 `Empty reply from server` 未能完成 fetch；封版前已重试 `git fetch origin main --tags`，远端同步恢复正常。

## 核心业务真实巡检

命令在 114 本机执行，报告输出到：

```text
/srv/pod/deliverables/release_patrol/core_business_8033c843.json
```

结果：

| 业务 | runId | 状态 | 执行节点 |
| --- | --- | --- | --- |
| 花纹提取 pattern_extract | `cf3a045049364f55b279dd55751d70c2` | succeeded | ComfyUI 5090 · 158 · 117.50.80.158 |
| 图裂变 fission | `b98dca8e17724b458efe309211adc7ad` | succeeded | ComfyUI 5090 · 158 · 117.50.80.158 |
| 扩图 outpaint | `a64f4b1479474b66bef8e93166809d51` | succeeded | ComfyUI 4090 · 233 · 117.50.216.233 |

巡检结果已登记到发布巡检记录：

- patrolId：`patrol_9b406bf7e3d8`
- 状态：`passed`
- generatedAt：`2026-05-25T12:34:33Z`

## 158 扩图节点复核

158 节点已确认可执行扩图，不需要中台固定路由到 4090。

- workflow：`flux2_klein_9b_outpaint`
- executor：`executor_comfyui_pattern_extract_158`
- promptId：`c6e9ce59efb44f959b162df1abcb9d10`
- ability logId：`46261`
- 状态：success
- 耗时：`9101ms`
- 输出：

```text
https://podiaidesign.oss-cn-hangzhou.aliyuncs.com/test/abilities/admin-comfyui/20260525/b69d1e21-1779682619.png
```

兼容性复核：

- `workflow-compatibility`：`okCount=17 warningCount=0 failedCount=0`
- `queue-summary`：`supportedServers=1 unsupportedServers=0 backendBlockedServers=0`

## 图编辑业务巡检

报告输出到：

```text
/srv/pod/deliverables/image_edit_patrol_seal/
```

结果：

- caseKey：`canvas_outpaint_all_sides`
- runId：`759f0310c01c4eaf9787aaec4ff93f95`
- 状态：succeeded
- started：`2026-05-25T12:29:21Z`
- finished：`2026-05-25T12:30:35Z`
- 输出：

```text
https://podiaidesign.oss-cn-hangzhou.aliyuncs.com/test/abilities/podi-internal-patrol/20260525/0dc5db50-1779683428.png
```

## 发布 smoke

114 本机发布 smoke 全部通过。

关键结果：

- `comfyui_queue_summary`：servers=2 capacity=20 idle=20 utilization=0.0 diagnostics=-
- `comfyui_workflow_compatibility`：total=17 ok=17 warnings=0 failed=0 servers=2
- `business_usage_summary`：total=52 failed=4 running=0 queued=0 unresolved=0
- `business_api_usage_center`：total=1988 submit=50 poll=1497 callback=0 errors=8 runs=42 groups=20 attention=8
- `eval_operations_health`：healthy issues=0
- `per_feature_release_audit`：blocked=0 attention=1，attention 为 `legacy-seamless-fission`
- `commercial_report`：历史账单问题 observed-only，不作为当前版本阻断。

## systemd 真实巡检

手动触发 `podi-business-live-patrol.service` 后成功退出。

- 状态：success
- 最近执行时间：2026-05-25 20:41:23 CST
- 报告：

```text
/srv/pod/reports/health-watch/eval_production_20260525T123505Z.json
```

日志结论：

```text
output kinds: {'image': 6}
all eval workflows succeeded
PODI health watch completed: mode=live reports=/srv/pod/reports/health-watch tag=20260525T123505Z
```

健康看板复核：

- `podi-business-health-watch.timer`：healthy
- `podi-business-health-watch.service`：healthy
- `podi-business-live-patrol.timer`：healthy
- `podi-business-live-patrol.service`：healthy
- `podi-eval-health-watch.timer`：healthy
- `podi-eval-health-watch.service`：healthy
- `issues=[]`

## 管理端交互走查

线上管理端 `http://114.55.0.56:8199` 已完成主要页面用户动线检查。

覆盖页面：

- 总览
- 业务能力
- ComfyUI 资源
- API 暴露
- 能力评测

结果：

- 主要接口请求无 4xx/5xx。
- 浏览器控制台无明显生产运行时报错。
- 业务能力、资源页和接口页可正常加载和切换。

## 已覆盖错误场景

- 历史失败被后续成功覆盖时，不再作为封版阻断。
- 扩图能力不因 158 节点历史 `DrawMaskOnImage.opacity` 差异继续误判为全局不可用。
- ComfyUI 输出必须有 OSS 回填和执行节点证据。
- 业务巡检必须能提交、轮询、拿到最终输出。
- systemd 定时巡检的旧失败状态必须被新的成功执行覆盖。
- 管理端主要页面不得出现生产运行时错误或接口级 4xx/5xx。

## 保留风险

- 商业报表历史账单问题仍按 observed-only 观察，正式收费版本前需要恢复为强门禁。
- `legacy-seamless-fission` 仍为历史 attention 项，后续可归档或补验收记录。
- 权限治理中仍存在历史风险项，本版本未扩大鉴权面，后续按 auth/billing 路线处理。

## 发布后观察建议

封版后继续观察 30-60 分钟：

- `/api/business/*` 提交与轮询错误率。
- 业务运行 `failed/running/queued/unresolved`。
- ComfyUI 队列容量、idle 和 backendBlockedServers。
- OSS 回填是否持续正常。
- 管理端接口调用中心是否出现新增 5xx 或集中 4xx。
