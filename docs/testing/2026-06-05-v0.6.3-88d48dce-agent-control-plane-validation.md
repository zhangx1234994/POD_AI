# v0.6.3 线上补充验收：AI 改图助手与控制面基准

日期：2026-06-05

线上提交：`88d48dce`

版本口径：本次是 `v0.6.3 中台能力控制面硬化版` 的线上补充验收和 Agent Runtime 样板修正，不另起 `v0.6.1` 口径。

## 结论

当前线上 `88d48dce` 通过 v0.6.3 补充验收，可以继续作为 114 当前运行版本。

- AI 改图助手 5 组 Agent 验收全部通过。
- 控制面 loopback 20 并发全部通过。
- 公网控制面业务读接口全部通过；仅 `/health` 公网 p95 超 300ms，loopback p95 正常，判断为公网 RTT/链路波动，不作为服务端阻断。
- 业务运行没有 running/queued/unresolved 残留。
- 业务失败数 `failed=1` 已归因到受控测试样例图 404，不是代码、模型或队列故障。
- 接口调用中心 `errorCount=1` 已归因到模糊意图追问案例的预期 `409 AGENT_PLAN_REQUIRES_CLARIFICATION`。

## 服务状态

- `/srv/pod/DEPLOYED_COMMIT`：`88d48dce`
- `/health`：`{"status":"ok"}`
- `podi-backend / podi-admin-web / podi-eval-web`：均为 `active`
- 三个 systemd 服务 `NRestarts=0`
- ComfyUI 队列：`servers=2`，`totalCapacity=20`，`feedGapServers=0`，`backendBlockedServers=0`，`backendRunningSettlingServers=0`

## AI 改图助手 5 组验收

报告路径：

- `/srv/pod/reports/agent-v063-88d48dce-20260605.json`

结果：`ok=true`，`cases=5`

| 案例 | 结果 | 关键证据 |
| --- | --- | --- |
| 首轮改图 + 重复确认幂等 | 通过 | run `7ccd14fbb18e41a59074ba3ffac3bbc9` succeeded；重复 confirm 返回同一 runId；`baseImageRole=source_image` |
| 二轮续改基于上一轮结果 | 通过 | run `2a2d7485d77f4cc2a3dd59de3542edd4` succeeded；tool image URL 等于上一轮输出；`baseImageRole=previous_result`，`parentRunId=7ccd14fbb18e41a59074ba3ffac3bbc9` |
| 新任务隔离 | 通过 | 新 session `ags_5443df212900481bb4912ddaa647bdba`，run `1f19a38de09543c2a30b429f464f40d4` succeeded；`baseImageRole=source_image`，无 parentRunId |
| 模糊意图追问 | 通过 | 输入“改一下”后 `requiresClarification=true`，`missingFields=["editGoal"]`，confirm 返回预期 `409 AGENT_PLAN_REQUIRES_CLARIFICATION`，没有创建下游 run |
| 消息 requestId 幂等 | 通过 | 同一 session 同一 requestId 重复发消息返回同一 plan `agp_aa63cde569034be49111f8e492192f9c`；`baseImageRole=previous_result` |

验收结论：

- 前端只负责对话、图片预览、状态和确认。
- 后端负责会话、路由证据、上下文延续、幂等和确认前阻断。
- 当前 Agent MVP 符合“第一块 Agent Runtime 样板”的最小验收口径。

## 控制面 20 并发基准

Loopback 报告：

- `/srv/pod/reports/control-plane-88d48dce-loopback-20260605.json`

| 接口 | 成功率 | p95 | 阈值 | 结论 |
| --- | ---: | ---: | ---: | --- |
| `/health` | 25/25 | 57.47ms | 300ms | 通过 |
| `/api/admin/business/capabilities` | 25/25 | 447.98ms | 1500ms | 通过 |
| `/api/admin/business/usage-summary` | 25/25 | 120.57ms | 1500ms | 通过 |
| `/api/admin/business/api-key-usage` | 25/25 | 101.55ms | 1500ms | 通过 |
| `/api/admin/comfyui/queue-summary` | 25/25 | 63.28ms | 1500ms | 通过 |

公网报告：

- `/srv/pod/reports/control-plane-88d48dce-public-20260605.json`
- `/srv/pod/reports/control-plane-88d48dce-public-health-retry-20260605.json`

| 接口 | 成功率 | p95 | 阈值 | 结论 |
| --- | ---: | ---: | ---: | --- |
| `/health` | 25/25 | 392.00ms | 300ms | 公网链路超阈值，服务端 loopback 正常 |
| `/health` 重试 | 25/25 | 501.11ms | 300ms | 重试仍超阈值，归因为公网 RTT/链路波动 |
| `/api/admin/business/capabilities` | 25/25 | 1019.91ms | 1500ms | 通过 |
| `/api/admin/business/usage-summary` | 25/25 | 522.34ms | 1500ms | 通过 |
| `/api/admin/business/api-key-usage` | 25/25 | 605.03ms | 1500ms | 通过 |
| `/api/admin/comfyui/queue-summary` | 25/25 | 390.14ms | 1500ms | 通过 |

结论：

- 服务端控制面读接口符合 v0.6.3 响应速度和 20 并发门禁。
- 公网 `/health` p95 超 300ms 不是服务端处理慢，因 loopback p95 仅 57.47ms；后续公网观测仍保留，但不阻断本次封版。

## 业务运行与接口调用中心

业务 usage summary 最近 24 小时：

- `total=38`
- `failed=1`
- `running=0`
- `queued=0`
- `unresolvedIssues=[]`

`failed=1` 归因：

- run `e041ffe3617f4aa99544c4ffc80dea0b`
- 时间：2026-06-05 13:22 左右
- 来源：发布后受控测试
- 原因：测试源图 `https://podiaidesign.oss-cn-hangzhou.aliyuncs.com/test/abilities/2d53fbcf8c764fb3b1f6d7e0ef89d0a1/20260603/flower-pattern-source.png` 返回 OSS 404
- 后续：换用有效 OSS 样例后，AI 改图助手多轮真实链路全部成功

接口调用中心最近 24 小时：

- `total=1003`
- `successCount=1002`
- `errorCount=1`
- `uniqueRunCount=40`

`errorCount=1` 归因：

- 路径：`/api/business/image-edit-chat/sessions/{sessionId}/confirm`
- 状态：409
- 错误码：`AGENT_PLAN_REQUIRES_CLARIFICATION`
- 来源：模糊意图追问验收用例，属于预期错误路径，不是异常 4xx

## 保留风险

- 公网 `/health` p95 仍超过 300ms，需要继续用 loopback + 公网双视角区分网络 RTT 与服务端耗时。
- 商业报表的 `wallet_missing` 仍是计费治理问题，不影响本次 Agent 和控制面验收。
- 短 TTL 与并发折叠已经保护线上，但长期仍需要后台预聚合，不能把短 TTL 视作最终架构。
