# v0.6 收口 114 部署验证

验证时间：2026-06-07 18:18 +0800

验证性质：先完成候选包线上验证，再补齐干净提交、正式源门禁和正式 114 发布复核。

## 正式封版复核

复核时间：2026-06-07 18:59 +0800

最终提交：

- `524a9bfa chore: close v0.6 validation loop`

正式源门禁：

- 本地 `HEAD` 与 `origin/main` 一致。
- `release_source_preflight.sh` 通过：`PASS=6 WARN=0 FAIL=0`。
- 首次 GitHub fetch 受网络波动超时，重试后通过。

正式发布脚本：

```bash
GIT_FETCH_TIMEOUT_SECONDS=90 RUN_SOURCE_PREFLIGHT=1 RUN_TESTS=1 RUN_FRONTEND_LINT=1 RUN_FRONTEND_BUILD=1 RUN_SMOKE=1 INSTALL_DEPS=auto bash scripts/release_114_control_plane.sh
```

结果摘要：

- 后端发布测试：`140 passed`
- 管理端 lint：通过
- 测评端 lint：通过
- 管理端 build：通过
- 测评端 build：通过
- 远端迁移与 seed refresh：通过，无漂移
- 远端发布确认：`released_commit=524a9bfa`
- 远端 `/health`：`{"status":"ok"}`
- deploy preflight：`PASS=5 FAIL=0`
- release smoke：全部 PASS

正式发布 smoke 关键证据：

- ComfyUI queue summary：`servers=2 capacity=20 idle=20 utilization=0.0`
- ComfyUI workflow compatibility：`total=17 ok=17 warnings=0 failed=0`
- business usage summary：`total=9 failed=0 running=0 queued=0 unresolved=0`
- business API usage center：`errors=0 attention=0`
- backend log regression：`matches=0`
- eval operations health：`healthy issues=0`

额外公网 HTTP 点检：

- `http://114.55.0.56:8099/health`：`{"status":"ok"}`
- `http://114.55.0.56:8200/api/evals/workflow-versions?status=active`：返回 30 条

补充说明：

- 正式发布前一次脚本执行因 GitHub fetch 超时退出，并暴露 `TEMP_FILES[@]: unbound variable` 的清理分支问题；该问题未影响重试后的正式发布，但应进入后续发布脚本维护项。
- 正式发布后额外 SSH 组合点检出现本地 SSH 会话卡住/认证链路抖动；公网 HTTP 与发布脚本远端验证均通过，因此不归因为服务不可用。

## 部署范围

- 目标机器：`114.55.0.56`
- 目标目录：`/srv/pod`
- 候选验证记录 commit：`6ba720e3`
- 正式发布 commit：`524a9bfa`
- 更新范围：`backend/`、`docs/`、`scripts/`、`deploy/`、`podi-admin-web/dist`、`podi-eval-web/dist`

## 发布脚本结果

执行入口：

```bash
RUN_SOURCE_PREFLIGHT=0 RUN_TESTS=1 RUN_FRONTEND_LINT=1 RUN_FRONTEND_BUILD=1 RUN_SMOKE=1 INSTALL_DEPS=auto bash scripts/release_114_control_plane.sh
```

结果摘要：

- 后端关键测试：`140 passed`
- 管理端 TypeScript 检查：通过
- 测评端 TypeScript 检查：通过
- 管理端 build：通过
- 测评端 build：通过
- 远端 Alembic：通过
- 远端 workflow/ability seed refresh：无漂移
- `podi-backend` / `podi-admin-web` / `podi-eval-web`：均 active
- `/health`：`{"status":"ok"}`

## 固定样例导入

线上执行：

```bash
cd /srv/pod/backend
.venv/bin/python scripts/seed_default_quality_samples.py
```

结果：

- `total=5`
- `created=5`
- `failed=0`
- 测评端样例接口返回 5 条 `product_design` active 样例

## 产品设计真实链路

批量样例报告：`/srv/pod/reports/releases/20260607-v0.6-product-design-quality-samples.json`

5 个固定样例全部真实提交 `/api/business/product-design/runs`：

- `product-design-apparel-floral-v1`：成功，1 张结果图
- `product-design-home-textile-check-v1`：成功，1 张结果图
- `product-design-bag-pattern-v1`：成功，1 张结果图
- `product-design-packaging-floral-v1`：成功，1 张结果图
- `product-design-ecommerce-mockup-v1`：成功，1 张结果图

共同证据：

- 路由命中：`产品设计 · GPT Image 2 上品设计`
- 执行节点：`Vendor API Ops · Global Egress Node`
- 结果回填：自有 OSS URL
- 批量结果：`5/5 passed`

测评端页面补跑 1 条产品设计任务：

- 页面固定样例可填充图片 URL、产品类型、设计 brief、场景、尺寸、质量、格式。
- 页面提交后状态由生成中进入成功。
- 中台任务：页面显示 `f333da55fd584300be352919398...`
- 结果区显示已回填 1 张，并支持原图/结果图对比。

业务摘要复核：

- `product_design` 近 2 小时：`total=6`、`succeeded=6`、`failed=0`、`running=0`、`queued=0`
- 成功率：`100%`
- 平均耗时：约 `104.5s`
- p95 主执行耗时：约 `172.5s`
- 结果入库：`stored=5`（业务摘要 stage evidence 采样窗口内）

## 页面交互验证

测评端 `http://114.55.0.56:8200/`：

- 图编辑分类：
  - “AI 改图助手”和“直接图编辑”作为两个独立能力入口展示。
  - 版本列表默认展开，可见 2 个可测版本。
  - 辅助能力导航仍保留。
  - 未出现“业务方验收入口”旧区域。
  - 控制台错误：0。
- 产品设计分类：
  - 固定样例 5 个可见。
  - “跑固定样例”和“跑推荐版本”入口可见。
  - 版本列表默认展开。
  - 辅助能力导航仍保留。
  - 控制台错误：0。
- 产品设计工作台：
  - 执行标签为 `中台业务编排 · GPT Image 2`。
  - 任务追踪为 `中台 runId 追踪`。
  - 未出现 `ComfyUI 执行面`、`当前主要靠 Coze 输出追踪`、`项目` 旧口径。

## 线上健康与日志

发布后 smoke：

- `health`：PASS
- Coze OpenAPI server：`http://172.17.0.1:8099`，PASS
- ComfyUI queue summary：`servers=2 capacity=20 idle=20 utilization=0.0`
- ComfyUI workflow compatibility：`total=17 ok=17 warnings=0 failed=0`
- business usage summary：`failed=0 running=0 queued=0 unresolved=0`
- business API usage center：`errors=0 attention=0`
- backend log regression：`matches=0`

ComfyUI 节点健康：

- `executor_comfyui_pattern_extract_158`：ok，队列空，缺失节点 0
- `executor_comfyui_seamless_117`：ok，队列空，缺失节点 0
- 后端汇总：`supportedServers=2`、`backendBlockedServers=0`

关键日志扫描：

- `QueuePool`：0
- `business run finalize loop failed`：0
- `OperationalError`：0
- `Deadlock`：0
- `Lock wait timeout`：0
- `Traceback`：0
- `ERROR`：0

## 保留风险

- 固定样例输入图片仍位于 `test/abilities/...` OSS 路径，后续建议迁移到稳定的 `quality/product_design/...` 样例目录。
- smoke 中 `per_feature_release_audit` 仍显示历史 `blocked=3`、`attention=1`；当前脚本不阻断，但封版报告必须列为治理项。
- 商业计费仍是 observed-only 阶段，`commercial_report` 存在 `wallet_missing` 历史/现阶段账单问题；不阻断本次能力验收，但正式收费版本必须收紧门禁。
- 测评端页面提交的 1 条任务在业务摘要中显示 `source=eval`，tenant/client 未标记；不影响成功链路，但后续可优化测评端观测归因。
- 发布脚本早退出清理分支存在 `TEMP_FILES[@]: unbound variable`，建议在下一个维护补丁中修复，降低网络波动时的误导信息。

## 结论

v0.6 收口版本已具备封版条件：干净提交、正式源门禁、114 发布、服务健康、产品设计固定样例、测评端页面提交、结果回填、接口调用中心和日志均通过。

当前结论：可以封版。上述保留风险不阻断本次 v0.6 收口，但需要进入后续维护和 v0.7 前置治理。
