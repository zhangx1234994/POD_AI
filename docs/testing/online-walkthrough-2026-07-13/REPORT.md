# AI创品线上功能走查报告（2026-07-13）

## 范围

- 环境：`https://aichuangpin.com`，生产 ECS `47.110.231.222`。
- 发布基线：`c6ba1dfb58d039b14a66ab0348f46ae184e13b39`。
- 覆盖：前台路由、未登录设计保护、移动端布局、能力目录、订单同步数据、服务健康。
- 不把未登录或未经过真实支付/短信授权的场景记为通过。

## 已通过

| 项目 | 证据 | 结论 |
| --- | --- | --- |
| 生产发布基线 | 服务器 `/srv/podi/prelaunch/app/DEPLOYED_COMMIT` 已写入本次 Git 提交 | 线上版本可追溯，禁止再直接在服务器改源代码 |
| 服务健康 | `podi-backend`、`podi-business-api` 重启后均返回健康状态 | 通过 |
| 登录入口 | `/login` 进入手机号验证码登录页，而不是首页 | 通过 |
| 未登录 AI 草稿 | 未登录用户提交设计描述后跳转 `/account`，`sessionStorage` 保留产品 ID、描述和保存时间 | 通过，不会丢失描述或自动发起付费生成 |
| 商品页 | 30 个可试做商品正常加载；马克杯未出现在当前列表 | 通过 |
| 移动端 | 390px 视口下产品页和登录页均无横向滚动，底部导航完整显示 | 通过 |
| Image2 中台目录 | `packy_gpt_image_2_generate` 与 `packy_gpt_image_2_edit` 均为 active；业务端默认路由已改为这两个能力 | 通过 |
| 旧能力治理 | 已从可调用目录隐藏遗留 OpenAI 图像和 Seedream 能力；KIE 保留为独立中台能力，不是业务默认 | 通过 |
| 订单数据 | 46 个本地订单，42 个已有蜂鸟供应链单号 | 通过现有推送链路 |

## 已发现问题

### P1：蜂鸟效果图尚未回填到本地订单

- 42 个已推送订单中，`imageSource=supplier_render` 的数量为 0。
- 目前蜂鸟订单查询回包只有 `orderDetailList`、状态和运单字段；`orderDetailList` 内没有效果图 URL。
- 本地已具备 `effectImageUrl`/`renderImages` 的保存和替换逻辑，但现有查询接口未返回对应字段，不能凭空把商品图当成供应链效果图。

**下一步：**向蜂鸟确认效果图所在的查询接口或字段名，并提供一个脱敏真实回包；随后补充解析、落 OSS、订单/设计篮回填和回归测试。

### P1：真实登录后的 AI 设计生成尚未完成发布后复测

- 中台 Packy Image2 已完成真实调用验证，业务默认路由也已切换。
- 本次线上走查没有代替真实用户发送短信或取得用户登录会话，因此未执行“描述 -> VL 规划 -> Image2 生成 -> 生产画布 -> 设计篮”的最终用户请求。

**下一步：**使用一个已登录测试账号完成一条真实无图生成和一条带图编辑，核对积分、任务、产图尺寸、设计篮和失败提示。

### P2：前端首屏资源仍偏大

- 构建产物初始 JavaScript 约 1.14 MB（gzip 约 320 KB），3D 预览模块已延迟加载但产品资源仍需要继续拆分/CDN 化。

### P2：运营后台公开审核和投诉页面缺少真实数据回归

- 当前数据为 0 条公开申请、0 条投诉，接口读取正常；无真实样本时不能判断审核、驳回、投诉处理交互。

## 本次修复

1. 将当前生产客户端和业务 API 源码纳入 Git，生产发布必须由同一 Git 提交构建。
2. 新增 `DEPLOYED_COMMIT` 发布标记和发布真源规范。
3. 修复 `/login` 路由。
4. 修复未登录 AI 设计描述丢失。
5. 业务端默认 Image2 能力改为中台托管的 Packy GPT Image 2；遗留 OpenAI/Seedream 能力下线。

## 本轮不做的事情

- 不绕过短信、登录、支付和用户授权伪造真实用户操作。
- 不把蜂鸟前台展示的效果图假设为当前 API 已返回的数据。
- 不将供应商效果图、第三方临时链接直接作为长期对外资产；接到 URL 后必须先落自有 OSS。
