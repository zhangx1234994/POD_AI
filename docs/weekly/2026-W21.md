# 周报 · 2026-W21

## 2026-05-20 日计划：图编辑组件型业务能力 v1

今天主线固定为“图编辑”内部可交付版本。图编辑不是单个接口，而是前端组件工作台、中台业务 API、GPT Image 2 图片编辑能力、测评端、管理端链路和交付文档的一整套业务能力。

### 今日执行顺序

1. 先收住早检风险：
   - 防止本地后端误连线上库后消费线上任务。
   - 为 `vendor-api-ops` 增加来源审计，后续白名单拒绝能直接看到真实来源。
   - 标记或清理昨日本地回归留下的 `text_fission queued` 脏记录。
2. 完成图编辑业务闭环：
   - 复核 `/api/business/image-edit/runs`、`/api/business/image-edit/component-config`、`/api/business/runs/get`。
   - 测评端“图编辑”以组件工作台呈现，不按普通表单堆字段。
   - 管理端按 `runId` 展示入口请求、编译步骤、GPT Image 2 调用、OSS 回填、成本和错误。
3. 做真实模型验证与交付沉淀：
   - 四种模式各至少 2 条真实任务：局部修改、参考图替换、删除修补、补色校正。
   - 每条导出原图、参考图、选择/蒙版、最终提示词、请求摘要、结果图、耗时和成本。
   - 输出内部客户接入材料：托管组件、源码接入、API Key、提交和轮询示例、错误码、尺寸规则。

### 今日完成标准

- 测评端可让内部同学直接测试图编辑。
- 业务方如果要接入，有清晰的 API 和组件说明。
- 管理端能按 `runId` 看清图编辑整条链路。
- 真实 GPT Image 2 样本可用于判断效果和后续提示词/交互优化。

### 今日过程记录

- 2026-05-20 12:30：测评端图编辑入口完成一次视觉走查修正。即使线上功能列表还带旧治理字段，前端也会按图编辑业务兜底展示为“主图 + 标注 + 参考图 + 编辑指令”“中台业务编排 · GPT Image 2”“中台 runId 追踪”，不再混入 ComfyUI/Coze 旧口径。
- 本地校验：`podi-eval-web` 类型检查通过，生产构建通过；本地浏览器已打开图编辑工作台并确认关键文案生效。
- 2026-05-20 12:45：新增 `docs/api/examples/image-edit-internal-handoff.md`，把图编辑内部接入所需的 API Key、提交、轮询、参数、枚举、错误和封版检查单独成文。
- 2026-05-20 13:10：测评端补齐 `/image-edit` 托管组件入口。内部客户打开该路径会直接进入图编辑工作台，并默认展开业务接入说明，避免先理解测评端分类和工具列表。
- 本地校验：`/image-edit` 浏览器打开后自动进入“图编辑 · GPT Image 2 通用改图”，显示“主图 + 标注 + 参考图 + 编辑指令”“中台 runId 追踪”，未再显示 ComfyUI 执行面。
- 2026-05-20 13:25：完成图编辑本地回归第一轮。后端业务契约、评测 seed、路由治理、OpenAI 尺寸策略和运行安全测试通过；测评端/管理端类型检查与生产构建通过；vendor-api-ops 回归通过。当前未完成项是四种图编辑模式的真实 GPT Image 2 样本测试和管理端登录态下的链路视觉复核。
- 2026-05-20 13:40：新增 `backend/scripts/patrol_image_edit_business.py`。该脚本独立于默认巡检，显式运行四种图编辑模式，并支持 `--repeat 2` 一次导出封版所需的 8 条真实样本；每条样本包含请求、提交响应、轮询记录、最终结果和汇总，便于上线后做效果归因。
- 2026-05-20 13:50：完成 `/image-edit` 本地浏览器走查。页面能自动进入图编辑工作台，显示图编辑标题、组件工作台、业务 API 说明、中台编排和 runId 追踪；未出现 ComfyUI 执行面或旧 Coze 追踪文案。
- 2026-05-20 16:40：图编辑前端从测评端大页面中抽出为组件能力雏形。新增 `podi-eval-web/src/features/image-edit/model.ts` 作为交互枚举和参数协议真源，新增 `ImageEditWorkbench.tsx` 作为可嵌入工作台，并补充组件目录 README；测评端图编辑入口改为复用该组件，不再继续在 `App.tsx` 内堆叠分散步骤卡片。
- 2026-05-20 17:05：补齐图编辑组件源码交付边界。组件样式已从测评端全局 `index.css` 拆到 `features/image-edit/image-edit.css`，后续交付源码时按 `model.ts + ImageEditWorkbench.tsx + image-edit.css + README.md` 一组提供。
- 2026-05-20 17:25：修正图编辑组件与后端的质量档位口径，前端统一使用 `auto/preview/production/premium`；补充源码嵌入最小示例和自定义尺寸输入，避免业务方二次封装时再造一套参数。
- 2026-05-20 17:30：生成图编辑组件源码交付包 `deliverables/image_edit_component_handoff_20260520_v1.zip`，包含组件源码、样式、模型协议、组件 README 和内部接口接入文档；压缩包不包含真实 API Key。
- 本地校验：`podi-eval-web` 类型检查通过。后续还需生产构建和浏览器走查确认视觉交互。

*最后更新: 2026-05-20*
