diff --git a/release/deployment/docker-compose/.env b/release/deployment/docker-compose/.env
index 95964789..d016dc0b 100644
--- a/release/deployment/docker-compose/.env
+++ b/release/deployment/docker-compose/.env
@@ -3,7 +3,7 @@ COZE_LOOP_APP_IMAGE_REGISTRY=docker.io
 COZE_LOOP_APP_IMAGE_REPOSITORY=cozedev
 COZE_LOOP_APP_IMAGE_NAME=coze-loop
 COZE_LOOP_APP_IMAGE_TAG=1.4.1
-COZE_LOOP_APP_OPENAPI_PORT=8888
+COZE_LOOP_APP_OPENAPI_PORT=8890
 COZE_LOOP_APP_DEBUG_PORT=40000
 COZE_LOOP_PYTHON_FAAS_IMAGE_REGISTRY=docker.io
 COZE_LOOP_PYTHON_FAAS_IMAGE_REPOSITORY=cozedev
@@ -100,4 +100,4 @@ NUMPY_VERSION=>=1.24.0
 PANDAS_VERSION=>=1.5.0
 JSONSCHEMA_VERSION=>=4.0.0
 SCIPY_VERSION=>=1.10.0
-SKLEARN_VERSION=>=1.3.0
\ No newline at end of file
+SKLEARN_VERSION=>=1.3.0
diff --git a/release/deployment/docker-compose/bootstrap/rmq-init/entrypoint.sh b/release/deployment/docker-compose/bootstrap/rmq-init/entrypoint.sh
index 7f3eb797..c8d1ca19 100755
--- a/release/deployment/docker-compose/bootstrap/rmq-init/entrypoint.sh
+++ b/release/deployment/docker-compose/bootstrap/rmq-init/entrypoint.sh
@@ -3,6 +3,24 @@
 exec 2>&1
 set -e
 
+retry() {
+  # retry <attempts> <sleep_seconds> <cmd...>
+  local attempts="$1"
+  local sleep_s="$2"
+  shift 2
+  local i=1
+  while true; do
+    if "$@"; then
+      return 0
+    fi
+    if [ "$i" -ge "$attempts" ]; then
+      return 1
+    fi
+    i=$((i + 1))
+    sleep "$sleep_s"
+  done
+}
+
 print_banner() {
   msg="$1"
   side=30
@@ -48,37 +66,37 @@ done
 i=1
 for topic in "${!topics[@]}"; do
   ii=$i
-  (
-    echo "+ Check if topic#$ii('$topic') exists..."
-    if ! "${MQADMIN_CMD}" topicList -n "${MQNAMESRV_ADDR}" | grep -q "^$topic$"; then
-      echo "[+] Topic#$ii('$topic') not exists, now creating..."
-      "${MQADMIN_CMD}" updateTopic -n "${MQNAMESRV_ADDR}" -c DefaultCluster -t "$topic" -r 8 -w 8
-    else
-      echo "[-] Topic#$ii('$topic') already exists."
+  echo "+ Check if topic#$ii('$topic') exists..."
+  if ! "${MQADMIN_CMD}" topicList -n "${MQNAMESRV_ADDR}" | grep -q "^$topic$"; then
+    echo "[+] Topic#$ii('$topic') not exists, now creating..."
+    # mqadmin may timeout briefly while the broker is warming up; retry to avoid partial init.
+    if ! retry 15 2 "${MQADMIN_CMD}" updateTopic -n "${MQNAMESRV_ADDR}" -c DefaultCluster -t "$topic" -r 8 -w 8; then
+      echo "[ERROR] Failed to create topic '$topic' after retries"
+      exit 1
     fi
+  else
+    echo "[-] Topic#$ii('$topic') already exists."
+  fi
 
-    IFS=',' read -ra consumer_groups <<< "${topics[$topic]}"
-    j=1
-    for group in "${consumer_groups[@]}"; do
-      echo "++ Check if consumer#$ii-$j('$group') exists..."
-      if ! "${MQADMIN_CMD}" consumerProgress -n "${MQNAMESRV_ADDR}" | grep -q "^$group$"; then
-        echo "[++] Consumer#$ii-$j('$group') not exists, now creating..."
-        "${MQADMIN_CMD}" updateSubGroup -n "${MQNAMESRV_ADDR}" -c DefaultCluster -g "$group"
+  IFS=',' read -ra consumer_groups <<< "${topics[$topic]}"
+  j=1
+  for group in "${consumer_groups[@]}"; do
+    echo "++ Check if consumer#$ii-$j('$group') exists..."
+    if ! "${MQADMIN_CMD}" consumerProgress -n "${MQNAMESRV_ADDR}" 2>/dev/null | grep -q "^$group$"; then
+      echo "[++] Consumer#$ii-$j('$group') not exists, now creating..."
+      retry 15 2 "${MQADMIN_CMD}" updateSubGroup -n "${MQNAMESRV_ADDR}" -c DefaultCluster -g "$group" || true
 
-        retry_topic="%RETRY%$group"
-        echo "[+++] Consumer#$ii-$j('$group')'s related retry topic('$retry_topic') is creating..."
-        "${MQADMIN_CMD}" updateTopic -n "${MQNAMESRV_ADDR}" -c DefaultCluster -t "$retry_topic" -r 8 -w 8
-      else
-        echo "[--] Consumer#$ii-$j('$group')' already exists."
-      fi
-      j=$((j + 1))
-    done
+      retry_topic="%RETRY%$group"
+      echo "[+++] Consumer#$ii-$j('$group')'s related retry topic('$retry_topic') is creating..."
+      retry 15 2 "${MQADMIN_CMD}" updateTopic -n "${MQNAMESRV_ADDR}" -c DefaultCluster -t "$retry_topic" -r 8 -w 8 || true
+    else
+      echo "[--] Consumer#$ii-$j('$group') already exists."
+    fi
+    j=$((j + 1))
+  done
 
-    echo "+ Topic#$ii('$topic') is ready! (with it's consumers and retry topics)"
-  ) &
+  echo "+ Topic#$ii('$topic') is ready!"
   i=$((i + 1))
 done
 
-wait
-
-print_banner "Completed!"
\ No newline at end of file
+print_banner "Completed!"
diff --git a/release/deployment/docker-compose/docker-compose.yml b/release/deployment/docker-compose/docker-compose.yml
index cd30870a..246c5a8a 100644
--- a/release/deployment/docker-compose/docker-compose.yml
+++ b/release/deployment/docker-compose/docker-compose.yml
@@ -224,6 +224,9 @@ services:
     container_name: "coze-loop-rmq-namesrv"
     image: "${COZE_LOOP_RMQ_IMAGE_REGISTRY}/${COZE_LOOP_RMQ_IMAGE_REPOSITORY}/${COZE_LOOP_RMQ_IMAGE_NAME}:${COZE_LOOP_RMQ_IMAGE_TAG}"
     restart: always
+    environment:
+      # Keep the JVM small for single-host local deployments (default settings can OOM on 8-16GB machines).
+      JAVA_OPT_EXT: "-Xms256m -Xmx512m -Xmn256m"
     networks:
       - coze-loop-network
     volumes:
@@ -243,6 +246,9 @@ services:
     container_name: "coze-loop-rmq-broker"
     image: "${COZE_LOOP_RMQ_IMAGE_REGISTRY}/${COZE_LOOP_RMQ_IMAGE_REPOSITORY}/${COZE_LOOP_RMQ_IMAGE_NAME}:${COZE_LOOP_RMQ_IMAGE_TAG}"
     restart: no
+    environment:
+      # Keep the JVM small for single-host local deployments (default settings can OOM on 8-16GB machines).
+      JAVA_OPT_EXT: "-Xms512m -Xmx1024m -Xmn512m"
     networks:
       - coze-loop-network
     volumes:

